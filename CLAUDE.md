# プロジェクト概要

ボードゲーム「7 SHADES OF S:EVEN」リメイク（仮）のデジタル版アプリ。
姉妹プロジェクトとして「7 SHADES OF S:EVEN 戦績管理システム」（対戦記録・ランキングの管理サイト、
単一の`index.html`）が別リポジトリで存在する。**2026-08-28時点の実際の場所は
`G:\マイドライブ\Cloud Code　作業フォルダ\`**（GitHub: `asobuzzasobazunihairarenai-dot/BATTLE-log`。
以前このファイルに書いていた「親フォルダ直下」は古い情報だった）。
**「無関係」ではなく密接に連携している**: 同じSupabaseプロジェクト（`prnddzrnblfysggiuzmo`）を共有し、
本アプリが `players` / `matches` / `match_feedback_replies` テーブルへ直接書き込む（対戦登録・コメント）。
`players.user_id` に本アプリのSupabaseアカウントが紐づく。両サイトとも同じGitHub Pagesのオリジンなので、
本アプリでログインしていれば戦績システム側も自動的にログイン状態になる（続き301の「マイ戦績」参照）。

## ゴールと段階

- 最終ベンチマークは MTGA（Magic: The Gathering Arena）＝カード効果を完全に自動処理する形。
- ただしいきなりそこは目指さず、まずはユドンリウムコネクト（ユドコネ）のような
  「アナログコンポーネントを画面に移植しただけ」の形を目指す。カード効果はテキスト表示のみで、
  処理はプレイヤーの自己申告に委ねる。
- ルールブックやカード効果は今後変更される可能性がある前提で設計する。

## 対戦形態（現状の合意事項）

- 最終的にはオンライン非同期対戦（同時接続ではなく、各プレイヤーが自分のタイミングで手番を進める形）を目指す。
- 実装は1人用（対戦相手なしのテストプレイ用）から始める。
- ただし「1人用→オンライン非同期」の移行で手戻りが出ないよう、最初から下記のアーキテクチャ方針を徹底する。

## アーキテクチャ方針

- ゲーム状態（盤面・手札・山札・フェーズなど）を1つのシリアライズ可能なオブジェクト（JSON）として持つ。
- 状態変更は全て「アクション」として表現し、`現在の状態 + アクション → 次の状態` の純粋な形にする
  （Reduxライクな設計）。1人用の段階ではアクションをローカルで即時適用するだけにしておき、
  オンライン非同期化する際は同じアクションをバックエンドに保存して同期する流れにそのまま乗せる。
- カード効果・カードデータはコードに埋め込まず、外部データ（JSON等）として持つ（変更に強くするため）。

## 技術スタック（現状の合意事項）

- 姉妹プロジェクト（戦績管理システム）に倣い、ビルド不要の単一HTML＋Tailwind CSS（CDN）＋Supabase
  （オンライン非同期対戦のバックエンド）という軽量構成を踏襲する。
- プラットフォームはデスクトップアプリを最終形としたいが、まずはブラウザで動くものを作り、
  将来的にTauri等でラップしてデスクトップアプリ化する想定。スマホとPCのクロスプレイも目標。
- GitHubリポジトリ: https://github.com/asobuzzasobazunihairarenai-dot/asobuzzasobazunihairarenai-dot.github.io
  （公開URLは **https://seven.asobuzz.net/**。2026-09-07に独自ドメインへ移行し、リポジトリ名からゲーム名を外した＝続き474〜476）
  （デフォルトブランチは`main`）。**コミットするたびに`git push`まで行う運用**（ユーザーから明示的に指示済み）。
  実物のプレイマット画像(`assets/playmat.png`)はコピーされたくないという要望があり、`.gitignore`で
  除外した上でgit履歴からも完全に削除済み（`git filter-branch`で過去コミットからも消してある）。

## 作業ルール（Claude Code向け・新しいセッションでも必ず引き継ぐこと）

- **コミットのたびに`git push`まで行う**（上記参照、ユーザーから明示的に指示済み）。
- **`git add -A` / `git add .` を使わない。コミットするファイルは必ず1つずつ明示して `git add` する**
  （2026-09-06・続き452の実害）。このリポジトリは**公開**で、作業フォルダには gitignore し切れていない
  非公開の資料（原稿・素材・メモ）が置かれることがある。実際に `git add -A` が
  `ストーリー/STORY（部外秘）.txt` を巻き込んで公開リポジトリへ push してしまい、履歴の書き換えと
  強制pushで消す羽目になった。**`.gitignore` に書いてあるから大丈夫、と考えないこと**——
  ルート直下の `/*.txt` は**サブフォルダには効かない**（これが今回の抜け道だった）。
  コミット前に `git status --short` を必ず読み、**身に覚えのないファイルが1つでもあれば手を止めて
  ユーザーに確認する**。
- **プレイヤーから見える変更をしたら、アプリ内の「お知らせ／更新情報」（[src/changelog.js](src/changelog.js)）も
  随時更新する**（ユーザーから明示的に指示済み、2026-08-29）。CHANGELOG の先頭に `{ date, items, itemsEn }` を
  1件追記する。**`items`（日本語）と `itemsEn`（英語）の両方を、同じ順番・同じ件数で書く**。表示は
  現在の言語の方だけ（併記はしない）。内容は「プレイヤーに何が変わったか」を、内部用語を使わない
  言い方で書く（不具合番号・関数名・ファイル名は書かない）。開発者向けの詳細はこの CLAUDE.md の
  変更履歴の方に書く。
- **コミットの前に、このCLAUDE.md末尾へ変更履歴を1エントリ追記する**。フォーマットは
  `### YYYY-MM-DD（続きNN）：タイトル`（NNは既存の最大値+1、既存エントリを`grep 続き`等で
  確認してから採番する。過去分は[docs/dev-log.md](docs/dev-log.md)にもあるので、最大値は
  両方を見て採番すること）。何を・なぜ変えたかを、後から読む人（未来の自分含む）が経緯を
  追えるように書く。
- **CLAUDE.mdを肥大化させない（毎セッション全文が読み込まれコンテキストを圧迫するため。
  2026-08-28に続き290以前を、2026-09-05に続き385以前を、2026-09-06に続き432以前を、2026-09-09に続き486以前を[docs/dev-log.md](docs/dev-log.md)へ切り出し済み）**。本体には
  「概要・アーキテクチャ方針・作業ルール・Phase1の基盤教訓/ハマりどころ・直近の変更履歴」だけを
  残す。日付付き変更履歴（`### YYYY-MM-DD（続きNN）`）がおよそ40エントリを超えて溜まってきたら、
  古い方を[docs/dev-log.md](docs/dev-log.md)の**末尾（続き260の後）へ時系列順で追記する形で**移す
  （内容は消さず移動するだけ。dev-log.mdは古い順＝続き1が先頭・最新が末尾）。過去の経緯・
  不具合の詳細を調べる時はdocs/dev-log.mdをgrepする。
- **`supabase/functions/so7-apply-action.ts`を変更した場合は、ユーザーへの手動再デプロイが
  必要かどうかを必ず明示的に伝える**（Supabaseダッシュボードの Edge Functions 画面へ
  手動でコピペしてデプロイする運用のため、コード変更だけでは本番に反映されない）。
  単純なパススルー等で再デプロイ不要と判断した場合も、その判断自体をユーザーに伝えること。
- **公開済みの `src/*.js` は削除しない**（2026-09-05・続き423の実害）。「もう誰もimportしていない」のは
  こちらのリポジトリの話で、**利用者の端末のキャッシュには古いimportが生きている**。消すとその端末で
  404 →「Importing a module script failed」で**機能が丸ごと起動しなくなり**、しかも読み込みに失敗した
  ファイルは更新時の再取得の対象からも外れるので**永久に直らない**。使わなくなったら中身を空にするか、
  経緯をコメントで残して**ファイルは置いたままにする**。
- **`supabase_setup_so7.sql`にSQLを追記した場合は、ユーザーがSupabaseダッシュボードの
  SQL Editorで手動実行する必要があることを伝える**（このリポジトリからは直接適用できない）。
- 上記のルール自体は本ファイル（CLAUDE.md）に書かれているため、リポジトリ内で作業する
  限り、会話セッションが変わっても自動的に読み込まれ引き継がれる。

## 資料

- **注（2026-09-07・続き474）**: `docs/rulebook.md` と `docs/cards.md`、および `説明書校正/` は
  **git管理から外した**（アナログ版の中身そのものなので公開リポジトリに置かない）。**手元には
  そのまま在る**ので今までどおり読める。控えは `G:\マイドライブ\SEVEN非公開資料バックアップ\`。

- ルールブック: [docs/rulebook.md](docs/rulebook.md)（受領済み・テキスト化済み）
  - 元PDF `説明書.pdf`／`説明書.txt`はプロジェクト直下にあるが `.gitignore` でPDFはgit管理対象外（ローカル参照のみ）。
  - カード集（12〜14ページ）の補足説明は色ごとにまとまっているが、個別カード名との対応はcards.mdと突き合わせ済み。
- カード効果テキスト: [docs/cards.md](docs/cards.md)（受領完了・整理済み）
  - 全33種類（ファーストカード7種、エターナルカード7種、通常カード19種）。
  - 黒カードはルールブック記載（3種類）が誤りで、正しくは2種類（ユーザー確認済み）。
- 盤面レイアウト参考画像: [docs/board-reference/](docs/board-reference/)（ユドンリウムコネクト版の実際のスクリーンショット3枚。準備前/準備完了/ゲーム途中）
  - "Face Down Card Area" "Hands Area"（自分だけ中身が見える表示切替）はユドンリウムコネクトというツール自体の汎用機能であり、本ゲーム固有のルールではない（ユーザー確認済み、設計には反映しない）。

## 設計

- ゲーム状態データ構造: [docs/game-state-design.md](docs/game-state-design.md)
  - GameState / Board / Player / CardDefinition / CardInstance / Action の設計（v1、ユーザー合意済み）。
  - 乱数処理の同期方針: 結果をアクションのpayloadに確定値として含める（1人用⇄オンライン非同期で同じロジックを使うため）。

## Phase 1 実装状況

- 方針: CSS疑似3D（`perspective`+`rotateX`）で開始。3D化が必要になった場合は描画層（`src/main.js`の描画処理）だけを差し替える想定で、ゲームロジックとは分離して実装する。
- モバイル対応: PC（横長ブラウザ）優先。スマホ横向きは意識するが、実機での細かい調整はPhase1完了後に回す（現状、縦幅が狭い画面では上下のプレイヤーゾーンが見切れる）。
- ローカル確認用サーバー: ルートの `.claude/launch.json` に `7shades-digital` という設定名で追加済み（`npx serve 7shades-digital -l 3001`）。ルート直下の`static-serve`(3000番)は姉妹プロジェクト用なのでポートを分けている。
- 静的レイアウト（盤面7x7・4人分のロックエリア/手札ゾーン・共有山札等）を [index.html](index.html) / [src/style.css](src/style.css) / [src/main.js](src/main.js) に実装済み。
- **注意（ハマりどころ）**: CSS疑似3Dの箱に `class="table"` を付けたところ、Tailwind CDNの組み込みユーティリティクラス`table`（`display:table`）と衝突し、`display:grid`が上書きされるバグが発生した。Tailwind使用時はカスタムクラス名がTailwindの既存ユーティリティ名（`table`, `hidden`, `flex`, `grid`, `block`, `container`等）と衝突しないよう注意する（今回は`game-table`に変更して回避）。
- 座席配置: 手前(南)=A(自分)、左(西)=B、奥(北)=C、右(東)=D。盤面を上から見て時計回り(A→B→C→D)がルールブックの「時計回り」ターン順とそのまま一致するよう統一した。
- 手札は「テーブルに置く」のではなく「持ち上げて扇状に持っている」見た目。自分(A)は`rotateX(-40deg)+translateZ`で大きく持ち上げ扇状に開く（上下方向）。他プレイヤー(B/C/D)は逆に`rotateX(18deg)`で盤面側へ伏せ、裏向き柄（`.is-facedown`）にして密集させる（MTGA参考画像の対面プレイヤー表現に合わせた）。全員分カードごとに個別回転＋位置をJS側で計算する扇形式（`layoutFan`参照）。左右(B/D)も上下(A/C)と同じ考え方で縦方向に扇状に開く（扇コンテナ全体を90度回転させる旧方式はカード自体が横倒しになるバグがあったため、個々のカードにのみ回転をかける方式にした）。弧の向き（外側のカードが盤面から逃げる方向）は`ARC_SIGN`で座席ごとに定義。
- 手札・駒・山札系のカードはすべて正方形（このゲームの物理カードが正方形のため）。
- **駒の見た目は何度か作り直した**（試行錯誤の記録）:
  1. 3面をpreserve-3dで組む真の3D立方体 → 盤面のrotateX(42deg)との兼ね合いで正面以外がほぼ潰れる。打ち消し回転（ビルボード化）も試したが逆に正面が潰れて失敗。
  2. 2DのskewX/skewYで組む疑似アイソメトリック立方体（top/front/sideの3枚）→ 見た目は良くなったが、駒がセルより大きくはみ出すため「DOM順で後に来る隣のセル」に隠され、脚だけ残る崩れ方をした（`.cell`同士が同じスタッキング文脈でDOM順に描画されるため）。駒のあるセルに`z-index`を与えて一旦解決したが、複数面構成自体が壊れやすかった。
  3. ユドナリウム(TK11235/udonarium、Angular+TypeScript製、three.js等は不使用でCSSベース)の`game-character`コンポーネントを調査し、非対称な太さ・濃淡のボーダーだけで立体感を出す手法を試した（`.pedestal-grab-border`参考）。頑丈だが、ユーザーから「駒はユドナリウムでいう『地形』に該当する」と指摘があり、参照コンポーネントが違うと判明。
  4. **`terrain`（地形）コンポーネントを調査し、その技法をそのまま移植**。「床」を`translateZ(height)`で持ち上げ、4枚の壁を角ごとに異なる`transform-origin`+`rotateZ`/`rotateX`の組み合わせで側面に貼り付ける、正しい組み方のpreserve-3d立方体（`buildCubePiece`参照、CSSクラスは`.piece-top`/`.piece-wall-front`/`.piece-wall-back`/`.piece-wall-left`/`.piece-wall-right`）。移植直後、壁の高さが常にゼロに潰れる新たなバグが発生したが、原因は**`.piece`に付けていた`filter: drop-shadow()`**だった。CSSの`filter`は要素とその子孫を強制的に2D平坦化する仕様があり、`transform-style: preserve-3d`を無効化してしまう。影を`box-shadow`に変更して解決。この技法なら、駒がどの位置にあってもどの面がどれだけ見えるかはブラウザの3D計算が自動でやってくれるため、以前のようなセル位置に応じたJS側の手動調整（leanFactor）は不要になった。
  - 教訓1: 見た目を真似たい対象（ユドコネ）が実際どう実装されているかをオープンソースで確認する時は、コンポーネント名（駒＝character/terrainのどちらか）を早い段階でユーザーに確認する。
  - 教訓2: `transform-style: preserve-3d`を使う要素には`filter`（drop-shadow等）を付けない。影が欲しい場合は`box-shadow`を使う。
  - 教訓3: `backface-visibility: hidden`もユドナリウム本家では設定されていた（移植時に見落としていた）。真の3D構成を組む際は本家CSSを可能な限り忠実に移植する。
  - 教訓4: 強めの`perspective`（近いカメラ）の下では、駒の上面（カメラに近い＝拡大率が高い）と側面（奥行きがあり拡大率が異なる）の間にわずかな継ぎ目の隙間ができることがある。側面の高さを実際の駒の高さより少し（0.15rem程度）大きくして上面と重ねることで隙間を隠せる。
- 自分の手札カード（`.hand-card.is-self`）は視認性のため他プレイヤーの手札より大きく表示している。それに合わせて`.game-table`のgrid-template-rows（手前の行のみ拡大）、`.zone-bottom .hand-area`の高さ、扇の間隔(layoutFanのspacing)も調整済み。
- 盤面の奥行き感（台形っぽさ）は、`perspective`の値を小さくする（=カメラを近づける）ことで自然に強調できる。手動で盤面を台形に変形するのではなく、既存の3D構成（`perspective`+`rotateX`）のパラメータ調整で対応した（`perspective: 2200px→1150px`）。
- 山札は左上、エターナルカードは左下、捨て場は右下（山札と対角）に配置済み。
- 相手（B/C/D）の手札の弧（外側のカードを盤面から遠ざける演出）は、3D変形と組み合わさると非対称に見える不具合が出たため撤去し、回転のみのシンプルな扇にした（自分の手札は弧を維持）。
- 山札・エターナルカード・捨て場は、枚数に応じて厚みを持つ「積み重ね」表示にした（`.stack`/`.stack-top`/`.stack-front`、`buildCardStack`参照）。駒(`.piece`)と同じ「床+壁」の考え方だが面は上面と正面の2枚のみ。1枚あたり0.6px・最低2.4pxの厚み。初期山札は63枚（通常カード112枚－盤面49枚）、エターナルカードは7枚、捨て場は0枚。将来、盤面マスのカードスタックにも同じ`buildCardStack`を流用する想定。
- ウィンドウサイズが変わっても手札等が見切れないよう、`fitTableToViewport()`で`.game-table`の`scale`をJS側から動的に調整するようにした（`--table-tilt`というCSS変数でrotateX角度をCSS/JS間で共有し、scaleだけを画面サイズに応じて計算）。`resize`イベントでも再計算する。最大1.15倍まで拡大、それ以上大きい画面では頭打ちにしている。
  - **ハマりどころ**: `scale(s)`はX/Y軸の2軸しか縮小しない。駒の高さ等`translateZ`で表現している奥行き(Z軸)はscale()の対象外のため絶対サイズのまま変わらず、ウィンドウを小さくして盤面のX/Y footprintだけ縮むと、駒が奥行き方向にだけ間延びして「縦に伸びた柱」のように見えるバグが発生した。`scale(s)`を`scale3d(s, s, s)`に変えてZ軸も同じ倍率で縮小するようにして解決。3D変形が絡む要素をJSでリサイズする時は、`scale()`ではなく`scale3d()`を使うべき、という教訓。
- ローカルネットワーク内の実機（iPhone等）から確認する場合は `http://localhost:3001` ではなくPCのLAN IPアドレス（例: `http://192.168.10.137:3001`、環境により変わる）でアクセスする必要がある。`localhost`は「そのデバイス自身」を指すため他端末からは繋がらない。
- プレイヤーBの手札は`rotate(180deg)`で回転させている（`.zone-left .hand-fan.is-opponent`）。
- `.zone`（手札エリア・山札エリア等を囲っていた四角い枠）の背景・ボーダー・box-shadowは削除し、透明なレイアウト用コンテナのみにした。
- カラーパレットは実際のゲームの色番号に更新済み: 赤`#c70025` 橙`#ee781f` 黄`#fabe00` 緑`#22ac38` 青`#1bb8ce` 桃`#f19ec2` 紫`#915da3`（`:root`の`--color-*`参照）。
- **実物のプレイマット画像を使用**: `画像素材/プレイマット.png`（1080x1080、gitignore対象＝ローカルのみ）を`assets/playmat.png`にコピー（gitで管理、こちらはコミット対象）。画像はパレット形式(indexed PNG)でPythonが使えない環境だったため、Node.jsの`zlib`でPNGを自前デコードして構造を解析しようとしたが、画像内の細かい模様・陰影のせいで自動検出は不確実だった。最終的に「実際にブラウザで描画して目視確認・調整」という方法で位置合わせした。
  - 教訓5: 複雑なテクスチャを含む画像のレイアウト解析は、ピクセル解析より実際にブラウザで描画して目視確認・調整する方が早い場合がある。特に3D変形（rotateX）がかかった状態では正確な比較ができないので、位置合わせ時は`game-table`の`transform`を一時的に`none`にして平面化する。
  - **設計変更（重要）**: 当初`.arena`の`background-image`として実装したが、2つの問題が判明した。(1) `background-size`で拡大すると、はみ出した部分が要素の外側として切り取られ見切れる。(2) サイズ基準が「アリーナ全体（ロック帯の太さを含む可変サイズの箱）」だったため、`--lock-thickness`を変えるとアリーナの箱ごと変わり、画像も道連れで動いてしまう。これを解決するため、画像は`.playmat-bg`という独立した絶対配置要素にし、サイズ基準を「盤面(`.board`)の固定サイズ」（`--lock-thickness`に影響されない）にした上で、`background-size`ではなく`transform: scale()`で拡大するようにした（`transform`は要素の外側にはみ出しても切り取られない。`transform-origin: center`にしているので盤面中央が拡大縮小の起点になる）。
- **管理者モード（[src/admin.js](src/admin.js)）を追加**: 画面左上の「⚙ 管理者モード」ボタンから、プレイマット画像・山札・捨て場・エターナルカードそれぞれの位置合わせ用パラメータ（`拡大率`/`位置X`/`位置Y`、対応するCSS変数は`--playmat-*`/`--deck-*`/`--discard-*`/`--eternal-*`）とロックエリアの`--lock-thickness`/`--arena-gap`を、グループ分けされたスライダーでその場調整できるようにした。ユーザーが微調整して「出力をコピー」した`:root { ... }`のテキストをもらったら、その値を[src/style.css](src/style.css)の対応する`:root`変数にそのまま反映する運用。開発用ツールなので常時表示可能にしてあり、ゲーム本編のUIではない（将来的に本番ビルドでは非表示にするか検討）。
  - **注意（ハマりどころ、最終的に解決）**: `.playmat-bg`に`z-index: 0`を付けたところ、ロックエリアと盤面（`position`指定のない通常のグリッド要素）より手前に描画されてしまい、ロックエリアが完全に隠れて見えなくなるバグが発生した。`position: absolute`な要素に`z-index`を明示的に数値指定すると新しいスタッキングコンテキストができ、`position`指定のない通常のフロー要素より優先して手前に描画されるため（`z-index: auto`と`0`は同じに見えて挙動が違う）。一旦`z-index`を外し、DOM順（一番最初の子要素＝背面）に任せる方式に戻して解決したが、**これは対症療法に過ぎず、後に別の形で再発した**（詳細は下記「ロックエリアが見えない問題・真の決着」参照）。
  - 山札・捨て場・エターナルカードの`.stack`も同じ考え方（`transform: scale/translate`、`transform-origin: center`）で独立に拡大縮小・位置調整できるようにした。親の`.zone`が`overflow: hidden`だったため、`.pile-zone`に`overflow: visible`を明示して上書きしている（そうしないと拡大した山が見切れる）。
  - 山札等のpos-x/pos-yは当初`%`（自分のサイズ基準、3.2rem角の山では±100%でも±3.2rem程度しか動かせない）にしていたが、プレイマットの角まで動かすには全然足りないと分かったため`rem`（絶対量、±25rem）に変更した。中心からの移動量が大きい要素には`%`ではなく絶対単位を使うべき、という教訓。
  - **ロックエリアが「見えない」問題の真因（ハマりどころ）**: z-index修正後も「ロックエリアが見当たらない」という報告があった。調査の結果、要素自体は存在し正しい位置・色で描画されていたが（`elementsFromPoint`で最前面にあることを確認済み）、3D遠近感（`perspective: 1150px`の近いカメラ + `rotateX(42deg)`の傾き）によって薄い帯（`--lock-thickness`が数remでも）が画面上ではわずか数ピクセルの高さに圧縮され、実質見えなくなっていた。box-shadowでグローを加えても、影も要素と一緒に3D変形で圧縮されるため効果が薄かった。最終的に`--lock-thickness`のデフォルトを3.2remまで上げ、管理者モードのスライダー上限も6remまで拡張して、ユーザーが実際に見ながら十分な太さを選べるようにした（奥の辺＝カメラから遠い辺ほど強く圧縮されるため、上下と左右で必要な太さが異なる可能性がある）。
  - **山札・エターナル・捨て場がプレイマットの下に隠れる問題（ハマりどころ）**: `render()`内で`buildArena()`（プレイマット画像を含む）を各種ゾーンより「後」に`appendChild`していたため、`.arena`が`game-table`内でDOM順的に最後＝最前面に描画されていた。これは`.arena`自身が`position`指定のない通常のグリッド要素であり、`z-index`を使わないスタッキングでは兄弟要素間の重なりは単純にDOM順（後の要素が前面）で決まるため。管理者モードで山札等をプレイマットの位置まで動かすと、画面上で座標が重なった際にプレイマット側が前面に来て隠してしまっていた。`buildArena()`を`render()`内で一番最初に`appendChild`するよう順序を入れ替えて解決（プレイマットが常に一番背面＝盤面のマス目の枠線と同じ「高さ」で最前面に表示される、というユーザーの提案通りの設計にした）。
  - **手札の位置を4人独立で調整可能にした**: `--hand-a/b/c/d-pos-x/y`（範囲±10rem）を追加し、各プレイヤーの手札を盤面中心から個別にオフセットできるようにした（各`.zone-*`のセレクタで対応する`translate()`を追加）。
  - **ロックエリアをCSS Gridのトラックから完全に切り離し、独立配置の帯に再設計した（重要・アーキテクチャ変更）**: 当初は`.arena`のCSS Gridで`--lock-thickness-top/bottom/left/right`を列・行のトラックサイズとして使う実装だった（1つの変数から4辺独立の変数に分割済みだった）。しかしこの実装だと、ロック帯を1辺でも動かす（太さを変える）と`.arena`自体のグリッドサイズが変わり、それに連動して49マス・駒・他の3辺のロックエリア・両プレイヤーの手札・山札・捨て場・エターナルなど、画面上のほぼ全要素が一緒に動いてしまう不具合が発生した（ユーザー報告：「ロックエリアを調整しようとすると...それぞれの位置が追従して動いてしまいます」）。加えて、スロットの縦横比が正方形でなく「少し長方形」になってしまう問題もあった。
    - **根本原因**: ロック帯の太さという「見た目の調整値」を、CSS Gridの「レイアウトを決めるトラックサイズ」に直結させていたこと。トラックサイズを変えれば、当然そのグリッドに属する全要素・さらにその外側で暗黙に連動する要素（`.game-table`側の他のゾーン）まで再配置される。
    - **解決策**: ロックエリアを`.arena`のグリッドから完全に外し、`.playmat-bg`と同じ「`position: absolute` + `transform: translate()`」方式の独立レイヤーに変更した。デフォルト位置は`.board`の実サイズ（`--board-size`という共通変数に切り出した）を基準に、盤面の外側にちょうど1マス分（`--cell`）密着させる形で`margin-top`/`margin-left`により静的に計算する（マスと同じ大きさの帯が49マスの各辺にそのまま隙間なく並ぶ、というユーザーの要望通りのデフォルト）。ユーザーが動かす分は`transform: translate(var(--lock-*-pos-x), var(--lock-*-pos-y))`だけが担当する。`transform`は要素を「見た目上」動かすだけでレイアウト（他要素の位置計算）には一切影響しないため、1辺をどれだけ動かしても他の要素は完全に不動になる。
    - これにより「太さ」の概念自体が不要になった（デフォルトが常にマスと同じ正方形サイズのため）。管理者モードの「ロックエリア」グループは太さ4種＋間隔1種の計5スライダーから、辺ごとの位置X/Y計8スライダー（`--lock-top/bottom/left/right-pos-x/y`、範囲±10rem）に置き換えた。
    - **教訓**: 「調整可能にしたい値」が「レイアウトを決める値（グリッドのトラックサイズ、フレックスの基準サイズなど）」と一致していると、その値をいじるたびに無関係な要素まで連動して動く事故が起きやすい。見た目の微調整用途なら、最初から`transform`（レイアウトに影響しない）で実装するのが安全（プレイマット・山札・捨て場・エターナル・手札は最初からこの方式だったため無事だった。ロックエリアだけ後から同じ方式に作り直す形になった）。
  - 現在のデフォルト値（ユーザーが管理者モードで調整済み）: `--playmat-scale: 1.42`, `--playmat-pos-x: 0%`, `--playmat-pos-y: 0%`。山札・捨て場・エターナルは`scale:1`、`--deck-pos-x: 4.1rem; --deck-pos-y: 0.7rem; --discard-pos-x: -4.1rem; --discard-pos-y: -4.4rem; --eternal-pos-x: 4.1rem; --eternal-pos-y: -4.4rem;`。手札は`--hand-a-pos-y: -1.5rem; --hand-b-pos-x: -0.3rem; --hand-c-pos-y: -2.9rem; --hand-d-pos-x: 0.3rem;`（他は0）。ロックエリアは`--lock-top-pos-y: -1.8rem; --lock-bottom-pos-y: 1.8rem; --lock-left-pos-x: -1.9rem; --lock-right-pos-x: 1.8rem;`（盤面から少し離す方向、他は0）。
  - **手札がゾーンの見えない境界で上端を切られる問題**: `.zone`に共通の`overflow: hidden`があり、`.player-zone`（手札ゾーン）はそれを上書きしていなかったため、`--hand-*-pos-y`で手札を大きくずらすとゾーンの外にはみ出した部分（特に上端）が直線でスパッと切れて見えるバグがあった。`.pile-zone`は既に`overflow: visible`で上書き済みだったのと同じ理由・同じ直し方で、`.player-zone`にも`overflow: visible;`を追加して解決。
    - 副作用として、`overflow: visible`にしたことで手札がゾーン名ラベル（「プレイヤーC」等）の上に重なって隠すケースが出た（DOM順で名前ラベルが先、手札が後なので手札が手前に描画されるため）。`.label`に`position: relative; z-index: 5;`を追加し、常に手札より手前に出るようにして解決。
  - **山札・エターナル・捨て場の枚数表示をゾーン外ラベルから山自体の表示に統合**: 従来はゾーンの上に「山札（61枚）」のような独立した`<div class="label">`があったが、ロックエリアや手札を動かしてゾーン自体の位置がずれると、このラベルだけ置き去りになって浮いて見える問題が出た。ゾーン外のラベルは廃止し、枚数を`stack-top`（山の上面、名前が表示されている場所）に`.stack-count`という小さい行として統合表示するようにした（`buildPileZone`/`buildCardStack`参照。`buildPileZone`から`label`引数自体を削除しシグネチャを簡略化）。
  - **ロックエリアが見えない問題・真の決着（重要なハマりどころ）**: 上記のz-index除去やlock-thickness引き上げをやっても、ユーザーの環境では「ロックエリアだけが常にプレイマットの下に隠れる」という報告が最後まで残った（山札・捨て場・エターナルは`buildArena()`の呼び出し順序を先頭に変えたことで解決済みだったが、ロックエリアだけ直らなかった）。詳しく調査すると、`elementsFromPoint()`では`.lock-slot`が確かに最前面と判定され、`getComputedStyle()`でも色・ボーダー・box-shadowはすべて正常、祖先要素にも`clip-path`/`mask`/`overflow`/`opacity`等の怪しい設定は一切なかった。それでもスクリーンショット上は描画されない、という「ロジック上は正しいのに見た目だけ壊れている」状態だった。最終的に、`.playmat-bg`（z-index:1）・`.board`（z-index:2）・`.lock-area`（z-index:3）という具合に、重なり得る要素全部に一貫した明示的な`z-index`（＋`position: relative`）を割り振ることで即座に解決した。
    - **教訓（最重要）**: complex 3D transform階層（`perspective`+`rotateX`+`preserve-3d`が何重にもネストした構造）の奥深くにある、`position`指定のないflexコンテナは、DOM順・スタッキングコンテキストの理屈上は正しくても、ブラウザの合成（compositing）が期待通りにならないことがある。z-indexは「なるべく使わない」方が安全とは限らない。むしろ**「重なる可能性のある要素全部に、意図した重なり順を表す明示的なz-indexを必ず割り振る」方が安全**、というのがこのプロジェクトで得た結論（前述の「z-index: 0だけ足したら壊れた」という経験則は、"z-indexを一部の要素だけに足す"のが問題であって、"z-indexを使うこと自体"が問題ではなかった、というのが正しい理解）。
- **駒・カードのドラッグ操作を実装（[src/state.js](src/state.js) 新規追加）**: これがこのアプリで初めて「実際に状態が変わる」機能だったため、プロジェクト最初期に決めていたアーキテクチャ方針（状態変更は全てactionとして表現し、現在の状態+action→次の状態、という純粋な形にする）をここで導入した。
  - `state.js`: `{ tokens: [...], piles: { deck, eternal, discard } }`という最小限の状態。`tokens`は駒・手札カードを統一的に扱う配列（各要素は`{ id, kind: "piece"|"card", location, ... }`）。`location`は`{zone:"cell",row,col}` / `{zone:"lock",side,index}` / `{zone:"hand",player}`のいずれか。山札・エターナル・捨て場は個々のカードを持たず、残り枚数のカウンタのみ（実カードデータがまだ無いため）。`moveToken`/`sendTokenToPile`/`drawFromPile`という3つのaction関数のみを公開し、内部の`reduce()`でイミュータブルに次の状態を作る。将来docs/game-state-design.mdの本格的なGameStateへ拡張し、オンライン非同期化時も同じactionをサーバー同期にそのまま使う想定。
  - `main.js`: `render()`が`getState()`の中身に応じて盤面・手札・山の中身を毎回組み立て直す形に変更（ダミー固定データを渡す方式から、状態駆動の描画に変わった）。
  - **ドラッグの実装方式**: 盤面全体が`perspective`+`rotateX`の3D空間の中にあるため、ドラッグ中の要素をその3D空間の中で座標計算するのは複雑。そこで、ドラッグ中はカーソルに追従する「ゴースト」要素を3D空間の外（`document.body`直下）に浮かせて表示するだけにし、実体は動かさない。ドロップ時に`document.elementsFromPoint(clientX, clientY)`でその座標にある要素を調べ、`closest()`で盤面マス(`.cell`)／ロックスロット(`.lock-slot`)／手札エリア(`.hand-area`)／山(`.pile-zone`)のどれに該当するか判定し、該当すればactionをdispatchして`render()`で全体を再描画する（該当する要素が無ければ何もせず、ゴーストだけ消して元の位置に戻ったように見せる）。この方式なら3D変形を一切気にせず実装できる。
  - ルール適用は一切しない（Phase 1の方針通り）。駒はどの盤面マス・ロックスロットへも自由に移動でき、カードは手札↔盤面マス↔山（山札/エターナル/捨て場）のどこへでも自由に動かせる。山からのドラッグは「引く」動作として扱い、手札へ落とした時だけ有効（残り枚数を1減らし新しいカードトークンを生成）。
  - 自分(A)以外の手札・駒も含め、すべてドラッグ可能にしてある（1人でのテストプレイでは全座席を自分で操作するため）。
  - マウス操作は`pointerdown`/`pointermove`/`pointerup`で実装（`touch-action: none`も付与済みなのでタッチ操作でも動くはずだが、実機での確認はまだ）。
  - 検証時のハマりどころ: ブラウザ自動操作ツールの疑似ドラッグ（マウスの座標だけを動かす方式）では、スクリーンショット座標と実際のビューポート座標の縮尺がズレていて、狙った要素の外に離してしまい「何も起きない」ように見えることがあった。実際のアプリの不具合ではなく、検証コード側の座標計算ミス。`PointerEvent`を`dispatchEvent`で直接発行して座標をgetBoundingClientRect基準で厳密に計算し直したところ、駒の移動・手札→捨て場・山札からの引き（山札→手札）・駒→ロックスロットのすべてで正しく状態が更新されることを確認できた。
  - **ユーザー報告バグ1: 自分の手札だけドラッグできない（重要なハマりどころ）**: リリース後、自分(A)の手札カードだけがドラッグ操作を受け付けず、プレイヤーB/C/Dの手札は問題なく触れるという報告があった。原因は各`.piece`/`.hand-card`/`.stack`要素に直接`pointerdown`リスナーを付けていたこと。`document.elementFromPoint()`（ネイティブのヒットテスト、実際のpointerdownイベントがどの要素に届くかを決める仕組み）で自分の手札の座標を調べると、実際に画面に描画されている`.hand-card`ではなく、その奥にある何もリスナーの無い`.zone-bottom`（平坦なコンテナ）が返ってきた。一方`document.elementsFromPoint()`（複数形。見た目通りの重なり順のリストを返す）では正しく`.hand-card`が最前面と判定される——つまり**見た目の描画（compositing）は正しいのに、ネイティブのヒットテストだけが実際と食い違っていた**。自分の手札は`rotateX(-40deg)+translateZ(2.4rem)`で大きく持ち上げられ、テーブルの傾き(42deg)とほぼ打ち消し合ってカメラにほぼ正対する角度になっており、他の座席（`rotateX(18deg)`で盤面側へ伏せている）より3D変形の度合いが大きい。深くネストした3D transform階層でのヒットテストという、ロックエリアの表示バグ（同セッション内の別の問題、上記参照）よりさらに厄介な、ブラウザの別のバグ・限界に遭遇したと考えられる。
    - **解決策**: 個々の要素に`pointerdown`を直接付けるのをやめ、`#game-table`に1つだけリスナーを付け、その中で`document.elementsFromPoint(e.clientX, e.clientY)`（信頼できる方）を使って「実際に何がクリックされたか」を自前で判定する方式（`findDraggableAt()`）に統一した。ドロップ先の判定は元々この方式だったので、開始・終了の両方で一貫した仕組みになった。
    - **教訓**: 深い3D transform階層では、ブラウザの「見た目の描画」と「ネイティブのヒットテスト（クリック判定）」が食い違うことがある——しかも今回はどの辺が食い違うか予測しづらい（左手札は問題なかった）。ネイティブのイベント委譲（要素に直接listenerを付けて`e.target`に頼る）を信用せず、`elementsFromPoint()`で自前判定する方式の方が、この種の3D UIでは安全。
  - **ユーザー報告バグ2: 山から引いたカードが手札に反映されないことがある**: 「実際には引けているのに画面に反映されない、別の場所に成功する操作をした瞬間にまとめて反映される」という報告があった。原因は`onDragEnd()`内の`pileSource`（山からのドラッグ）の分岐で、`drawFromPile()`（state更新）は呼んでいたが、その後`render()`を呼び忘れていたこと（手札へ落とせなかった場合の`else`節にだけ`render()`があった）。stateは正しく更新されるがDOMには反映されず、次に別の操作が`render()`を呼んだ時に、それまで溜まっていた分がまとめて表示されていた。`pileSource`分岐の末尾で無条件に`render()`を呼ぶよう修正。
    - **教訓**: state+action方式では「dispatchしたら必ずrenderする」を徹底しないと、こういう「stateは正しいのに画面だけ古い」バグが起きる。分岐が増えるほど呼び忘れの経路ができやすいので注意する。
  - **ユーザー報告バグ3件（初回リリース直後）**: (1) 手札カードをドラッグしている間、掴んでいるはずのそのカードが手札に残ったまま(ゴーストと二重に)見える。(2) カードをロックスロットにドロップしても置かれず手札に戻ってしまう。(3) カードを盤面49マスに置いても何も描画されない。
    - (1)の原因と修正: ドラッグ中は元のDOM要素をそのまま残し、カーソルに追従するゴーストを別途重ねて表示していただけだった。`startTokenDrag()`でドラッグ開始時に元要素へ`visibility: hidden`を付けるようにした（`onDragEnd`は常に`render()`で全DOMを作り直すため、明示的な後始末は不要）。
    - (2)の原因と修正: `findDropTarget()`のカード分岐が`.hand-area`/`.pile-zone`/`.cell`しか判定しておらず、`.lock-slot`への該当が無かったため、ロックスロットへ落としても`dropTarget`が`null`になり「何も起きず手札に留まる」という結果になっていた。駒・カード共通で`.cell`と`.lock-slot`を先に判定し、カードだけ追加で`.hand-area`/`.pile-zone`も判定する形に統一。
    - (3)の原因と修正（一番本質的な欠落）: 盤面マス・ロックスロットの上の駒を描画する`renderPieceTokens()`が`token.kind === "piece"`のトークンしか処理しておらず、カードが盤面/ロックへ移動しても描画する処理が最初から存在しなかった（stateは正しく更新されるが、対応する描画コードが無かった）。カード用の簡易な見た目（`.board-card`、置かれたマス/スロットにフィットするだけの平たいカード）を追加し、`renderPieceTokens()`を`renderBoardTokens()`に一般化して駒・カード両方を描画するようにした。あわせて、ドラッグ開始判定（`findDraggableAt`）にも`.board-card`を追加し、一度盤面に出したカードを再度つまめるようにした。
    - **教訓**: 「stateのlocationとして定義したゾーン」と「実際に描画コードが対応しているゾーン」がズレていないか、機能追加のたびに両方を見比べて確認する必要がある。今回は駒についてだけ実装して満足し、カードの「盤面/ロックに置く」経路の描画を作り忘れていた。
  - **ドロップ先のハイライト表示を追加**: ドラッグ中、今離すとどこに置かれるかが分かるよう、対象のマス/ロックスロット/手札エリア/山札等を黄金色の枠と光彩でハイライトするようにした。`findDropTarget()`が返す値を`{ zone, ... }`から`{ location, el }`（実際に該当するDOM要素も含む）に拡張し、`pointermove`のたびに前回ハイライトしていた要素からクラスを外し、新しい対象に`.drop-target-active`を付け替える（`updateDropHighlight()`）。レイアウトに影響しないCSSクラスの付け外しだけなので副作用はない。
  - **ロックエリアと手札の干渉問題・真の原因と解決（重要なハマりどころ）**: カードをロックエリアへドロップしようとしても置かれず手札に留まる、という報告があった。ユーザーが`--hand-*-pos-*`で手札を盤面中心から遠ざける調整をしたが直らず、追加したドロップ先ハイライト機能（上記）でようやく可視化できた：手札の当たり判定（`.hand-area`）がロックエリアの帯とほぼ同じ画面位置に重なって表示されていた。
    - **根本原因**: 手札を持ち上げる`transform`（`translate(pos-x,pos-y) rotateX(...) translateZ(...)`）を、それまで子要素の`.hand-fan`にだけ付けていた。すると**見た目**（`.hand-fan`とその中の`.hand-card`）はカメラ側へ大きくリフトされて描画されるのに、ドラッグの当たり判定に使っている`.hand-area`自身は一切transformされない「平らな机の上に置かれたままの」位置に取り残される。つまり「見た目のカードの位置」と「クリック/ドロップ判定の位置」が別の場所にズレてしまい、`.hand-area`（見た目上は空っぽに見える場所）がたまたまロックエリアの帯とほぼ同じ画面位置に重なっていた。`--hand-*-pos-*`で見た目のカード位置を動かしても、当たり判定の位置（transformされていない`.hand-area`自体の位置）は変わらないため、直らなかった。
    - **解決策**: 持ち上げtransformを`.hand-fan`ではなく`.hand-area`自身に付け替えた。transformは子要素からのレイアウト計算には影響しない（親の`.player-zone`内でのフレックス配置は変わらない）ため、見た目は一切変わらず、当たり判定の箱だけが正しく「見た目通りの位置」に来るようになった。
    - あわせて、`.hand-area`のサイズも見直した。以前は「扇が伸びる方向」が常に`width:100%`（盤面と同じ幅）、「伸びない方向」も固定の`5.2rem`/`9rem`だったため、実際に見えている手札の範囲よりずっと広い当たり判定になっていた。`--hand-{a,b,c,d}-size`（扇が伸びる方向、手札3枚を基準に枚数へ比例して自動で伸縮、`main.js`の`buildPlayerZone`でJS側から計算）と`--hand-{a,b,c,d}-thickness`（伸びない方向、固定値）の2種類を管理者モードから調整できるようにし、カード1枚分程度まで絞れるようにした。
    - **教訓**: 3D transformを子要素に付けるか親要素に付けるかで、見た目は同じでも「その要素自身の当たり判定がどこに来るか」が変わる。当たり判定に使う予定の要素には、見た目のリフト用transformも一緒に持たせておくべき（別の要素に分散させない）。
  - **山札から盤面マス／ロックスロットへ直接ドロップできない**: `onDragEnd`の`pileSource`分岐が「手札へ落とした時だけ`drawFromPile`する」という設計になっていた（山から直接盤面へ置く操作は意味が曖昧だろうと判断し、意図的に制限していた）。ユーザーから「山札からカードをマスやロックエリアに置けない」という要望があり、他の操作は全てルール適用なしの自由な移動なのにここだけ制限されているのは一貫性がないと判断し、`zone !== "pile"`（山以外ならどこでもOK）に緩和した。山(pile)自体への直置きだけは今後も禁止のまま（piles型は個々のカードを保持しない残り枚数カウンタのみのため、置き場所として使うとカードが行方不明になる）。あわせて、山から盤面マス／ロックスロットへ直接引いたカードは表向き（`faceUp: true`）になるよう`state.js`の`drawFromPile`を調整した（手札への場合のみ、自分(A)かどうかで表/裏を分ける従来のロジックのまま）。
  - **山札・捨て場・エターナルのドロップハイライトが実物とズレて大きすぎる**: ハイライト対象を`.pile-zone`（グリッド上の枠、実際の山より広く、山自体は`transform: translate/scale`でズラして配置されているためズレる）にしていたのが原因。`findDropTarget`のpile分岐で、`.pile-zone`ではなくその中の実際の`.stack`要素（見た目の山そのもの）を返すように変更し、ハイライトのサイズ・位置が実物と完全に一致するようにした。
- **実際のカードデータを反映（[src/cards-data.js](src/cards-data.js) 新規追加）**: それまでカードは全て中身の分からない灰色の板だった。docs/cards.md・docs/rulebook.mdから実際のカード名・色・枚数を抽出し、外部データファイルとして持たせた（CLAUDE.mdの技術スタック方針「カードデータはコードに埋め込まず外部データとして持つ」に沿う）。
  - 通常カード19種・合計112枚: 赤橙黄緑青桃紫は各色2種×7枚＝98枚、虹（なないろの欠片）1種×7枚、無色（白黒）4種で合計7枚（ユーザー確認：色落ちキャットのみ1枚、他3種＝なないろの巨光/白の意思の覚醒/黒の契約の烙印は2枚ずつ）。ルールブックには「黒3種類・各1枚」とあるが、これは既知の誤記（実際は黒2種類）で、今回の枚数もその修正を反映している。
  - エターナルカード7種・ファーストカード7種（各色1種）もデータ化したが、今回はまだ実際のゲーム内で使っていない（ファーストカードの配布・ロックなどのセットアップ自動化はPhase1のスコープ外、エターナル山は実カード7種をシャッフルして保持するようにした）。
  - `state.js`: `createInitialState()`でNORMAL_CARDSをcount分だけ展開してシャッフルした配列を山札とし、そこから各プレイヤーの初期手札を実際に配るように変更（以前はダミー枚数のみで山札の枚数と無関係だった）。`piles`は単なる残り枚数のカウンタから、実カードidの配列（末尾＝一番上、popで引く・pushで積む）に変更。これにより捨て場に捨てたカードも身元を保ったまま、後で引き直すと同じカードが出てくるようになった（山札・エターナル・捨て場を合わせた合計は常に112/7に保たれる）。
  - 表示: 自分の手札・表向きの盤面/ロックのカードは実際のカード名＋色で表示する（`.is-color-*`、`--color-*`を流用）。相手の手札は引き続き中身を明かさない裏向き表示のまま（`token.faceUp`ではなく`isSelf`で判定、自分の手札は常に見える・他人の手札は常に見えない、という従来の割り切りを維持）。捨て場はルール通り「表向きに積む」場所なので、一番上のカードの名前・色をそのまま`stack-top`に表示するようにした（山札・エターナルは裏向き積みのため中身は明かさない）。
  - **今回のスコープ外だった効果テキストは、後述の実物画像反映で実質解決した**（画像自体に到達効果・手札効果・フレーバーテキストまで描かれているため）。
- **実物のカード画像を反映、カードの表裏をダブルクリックで切り替え可能に**: `画像素材/`配下に実物のカード画像（通常カード19種・エターナルカード7種・裏面3種）が用意されていたため、`assets/cards/`にカードidをファイル名としてコピーして使うようにした（プレイマットと同じ理由で、実際の絵柄はgit管理・公開リポジトリには含めない。`.gitignore`に`/assets/cards/`を追加）。
  - `cards-data.js`に`getCardImagePath(cardId)`/`getCardBackImagePath(cardId)`を追加（裏面は通常カード用・エターナルカード用の2種類があり、`cardId`が`"eternal-"`で始まるかで判別）。画像には既にタイトル・色・効果テキストが描き込まれているため、`.is-color-*`によるテキスト＋色分けの簡易表示は丸ごと廃止し、`background-image`で画像を敷くだけにした（`.hand-card`/`.board-card`/山の`.stack-top`・`.stack-front`）。山札・エターナルの山も裏面画像を敷いて、より物理カードの見た目に近づけた。
  - **表裏のデフォルトを見直した（ユーザー要望）**: 従来は山や手札から盤面/ロックへ移動すると自動的に表向きになっていたが、「手札や山札からドロップする時は基本裏面、自分の手札に加わる時だけ自分に見えるよう表面」という物理カードの直感に合わせて逆にした。`state.js`に`faceUpForLocation(location)`という共通関数を作り、`MOVE_TOKEN`（既存カードの移動）・`DRAW_FROM_PILE`（山から引く）の両方でこれを使って表裏を決めるようにした（移動先が`hand`かつ`player === "A"`の時だけ表向き、それ以外は裏向き）。
  - **場のカードはダブルクリックで表裏反転できるようにした（後に判定方式を修正、下記参照）**: 新しい`FLIP_TOKEN`アクション（`state.js`の`flipToken(tokenId)`）を追加。手札のカードは対象外（手札は常に「自分だけ見える／他人には見えない」という固定表示のため、個々にめくる概念がない）。
  - **ロックエリアの色を塗りつぶしから枠線のみに戻した**: 表示バグ調査時に視認性確保のため`slot.style.background`を色で塗りつぶしていたが、z-index修正で本質的な表示バグが解決していたため不要になっていた。塗りつぶしを外し、`.lock-slot`のCSS既定（暗い半透明背景）＋着色した枠線＋うっすらしたグロー(box-shadow)だけの控えめな配色に戻した（`main.js`の`buildLockArea`）。
- **「山札一覧」ビューアを追加（[src/deck-viewer.js](src/deck-viewer.js) 新規）**: テキストデータ(cards-data.js)と実際のカード画像がちゃんと紐づいているかをユーザー自身が目視確認できるようにする、開発用のデバッグツール。画面右上の「📋 山札一覧」ボタンから開くモーダルで、通常カード19種・エターナルカード7種それぞれについて「サムネイル画像・タイトル・枚数」を一覧表示し、各カードの「補足」ボタンを押すとdocs/cards.mdの「補足:」欄（画像には印刷されていないルール解釈の補足説明）を開閉できる。`cards-data.js`の各カード定義に`note`フィールドを追加してこの補足テキストを持たせた（効果本文自体は画像に描かれているため引き続きデータ化していない）。管理者モードと同様、ゲーム本編のUIではなく開発用ツールという位置づけ。
- **カード裏面画像が「角だけアップになる」バグの真因を特定・修正（重要なハマりどころ）**: 前回「ファイルは正しい、表示サイズの問題では」と暫定回答していたが、ユーザーが「裏面画像の角だけがアップして描画されている」と正確に症状を言語化してくれたことで根本原因を特定できた。
    - **原因**: CSSの`background`ショートハンドは、明示的に指定していないサブプロパティ（`background-size`/`background-position`/`background-repeat`等）も暗黙に初期値へリセットしてしまう。`.hand-card.is-facedown`・`.board-card`・`.board-card.is-facedown`・`.pile-deck`/`.pile-discard`/`.pile-eternal`が`background: <グラデーション>`という形でフォールバック背景を指定しており、これらは`.hand-card, .board-card, .stack-top { background-size: cover; ... }`という共通ルールより詳細度が高い（またはファイル内で後に書かれている）ため、後から効いていた`background-size: cover`を暗黙に`auto`へ戻してしまっていた。結果、JS側で`element.style.backgroundImage`だけを上書きしても、サイズは原寸(auto)・起点は左上(0% 0%)・繰り返しはrepeatのままになり、小さな枠の中に画像(433×433px)の左上の角だけが原寸大で表示される、という症状になっていた。
    - **解決策**: 該当箇所をすべて`background:`ショートハンドから`background-image:`ロングハンドに変更した。ロングハンドは指定したサブプロパティ(background-image)しか変更しないため、他のルールで設定した`background-size: cover`等が無事に効くようになる。
    - **教訓**: CSSショートハンドプロパティ（`background`/`font`/`border`等）は「指定していない部分は前の値を引き継ぐ」のではなく「指定していない部分も初期値にリセットする」という仕様のため、複数の場所で同じプロパティ系統を別々に指定する設計（今回のような「共通ルールでbackground-sizeだけ指定、個別ルールでbackground-imageだけ指定」）をする時は、個別ルール側は必ずロングハンドを使う。
  - **ダブルクリックでの表裏反転が効かない問題も同根のバグだった**: 手札の当たり判定ズレ（`.hand-area`の項参照）と全く同じ理由で、ネイティブの`dblclick`イベントも3D階層内でのヒットテストに頼っているため正しく発火しないことがあった。`dblclick`リスナー自体を廃止し、ドラッグ開始判定（`elementsFromPoint`ベースの`findDraggableAt`、確実に動作することが分かっている）に統合。同じ`.board-card`トークンに対して400ms以内に2回`pointerdown`があったら、ドラッグの代わりにflipToken()を呼ぶ、という自前のダブルクリック検出にした。
  - **ロックスロットの枠線太さを管理者モードで調整可能に**: `--lock-slot-border-width`（デフォルト0.1rem、以前は固定3px）を追加し、`border: 3px solid ...`だった記述を`border-width`/`border-style`/`border-color`に分割した。
  - **プレイヤーB/Dの名前ラベルを90度回転**: 盤面の左右の辺と平行に読めるよう、`.zone-left .label { transform: rotate(90deg); }` / `.zone-right .label { transform: rotate(-90deg); }`を追加（transformはレイアウトに影響しないため、回転してもゾーン内の配置自体は変わらない）。
- **プレイヤー名ラベルの位置を管理者モードで調整可能に**: `--label-{a,b,c,d}-pos-x/y`を追加。B/Dは回転(rotate)の後にtranslateを重ねる形（`rotate(90deg) translate(x, y)`）にし、回転済みの見た目の向きのままtranslateで動かせるようにした。
- **ドラッグ中のカード/駒が実物と違って見える問題を修正**:
  - カードのドラッグゴーストが常に紺色の汎用プレースホルダーになっていた。`createGhost()`/`startPileDrag()`のゴースト生成時にも`background-image`を実際のカード画像（表向きなら顔、裏向きなら裏面）で設定するようにした。捨て場からのドラッグは表向きに積まれている実物画像、山札・エターナルからのドラッグは裏面画像を使う。
  - 駒のドラッグゴーストが立方体ではなく平らな正方形(`.drag-ghost-piece`)になっていた。3D空間の外(document.body直下)に置く都合上、本物の`.piece`をそのまま持ってきても盤面のperspective/rotateXが無いと立体的に見えない。`perspective: 1150px`を持つouter要素と、盤面と同じ傾き(`--table-tilt`)を持つinner要素の入れ子(`.drag-ghost-piece-outer`/`-inner`)を新設し、その中に`buildCubePiece()`で作った本物の駒をそのまま入れることで、ドラッグ中も立方体の見た目を保てるようにした。
- **山札の厚みが枚数に応じて変化しているように見えない問題の真因を特定・修正（重要なハマりどころ）**: `--stack-height`の値自体は枚数に応じて正しく計算されていたが、それを表現する`.stack-front`（山の側面）が実際には高さ0に潰れて全く見えていなかった。
  - **原因1**: `.stack-front`に`filter: brightness(0.6)`が残っていた。これは駒の壁面で既に踏んだのと全く同じ「filterはpreserve-3dを無効化する」バグ。`box-shadow: inset 0 0 0 999px rgba(0,0,0,0.4)`（箱全体を覆う半透明の黒）に置き換えて解決。
  - **原因2（filterを直しても直らず、真因はこちら）**: `.stack`から`.game-table`に至る祖先の`.pile-zone`（山札/エターナル/捨て場ゾーンの共通クラス）が`transform-style`を明示指定しておらず既定値の`flat`のままだった。駒側の祖先チェーン（`.cell`→`.board`→`.arena`→`.game-table`）は全て`preserve-3d`なのに対し、山側だけこの`.pile-zone`が素通りできていなかった。`.pile-zone`自体はtransformを持たない（＝自分自身が回転等をしているわけではない）要素だが、それでも間に`transform-style: flat`の要素が挟まっていると子孫の3D変形（`.stack-front`のrotateX）が壊れて描画されなくなることが、駒との比較実験で判明した。`.pile-zone`に`transform-style: preserve-3d;`を追加して解決。実際に山札を15枚引いて厚みが目に見えて薄くなることを確認済み。
  - **教訓**: 「自分自身はtransformを持たない要素」であっても、`transform-style: flat`（既定値）のままだと3D階層の途中に挟まるだけで子孫の3D変形を壊すことがある——手札(`.hand-area`)の当たり判定のケースで学んだ「transformの無い要素は3Dに対して透過的」という理解は、正確には「他要素からの見た目上のヒットテストには影響しないが、preserve-3dの伝播そのものは別問題」だったとわかった。3Dで組んだ構造をコピー/流用する時は、祖先チェーンの`transform-style`が全て`preserve-3d`になっているかを逐一確認する必要がある。
- **場・ロックエリア間の移動でカードの表裏が変わってしまう問題を修正**: 前回導入した「移動先に応じて表裏を決める」ロジック(`faceUpForLocation`)を`MOVE_TOKEN`に単純適用していたため、マス→マス・マス→ロック・ロック→ロックなど「既に場に出ているカードを場の中で動かすだけ」の操作でも毎回裏向きにリセットされてしまっていた。`state.js`に`isTable(location)`（zoneがcellまたはlockかどうか）を追加し、「移動元と移動先が両方とも場/ロックエリア」の場合は表裏を変更しない（既存の`faceUp`をそのまま維持する）よう修正。手札↔場/ロックエリアの移動（新しくカードが場に出る／手札に加わる）の時だけ、引き続き`faceUpForLocation`で表裏を決め直す。
- **プレイヤー名ラベルの調整レンジを拡大**: `--label-*-pos-x/y`の可動域が±8remで、ユーザーがすでに上限(-8rem)まで使い切っていたため、±24remまで拡大した。
- **山札の側面を無地のライトグレーに変更**: `.stack-front`（山の側面）に一番上のカードと同じ画像を敷いていたため、薄い帯に画像が引き伸ばされて見苦しかった。`buildCardStack()`で側面には画像を敷かないようにし（`pileClass`も外し、常に`.stack-front`のみのクラスにした）、CSS側で無地のライトグレー(`#cbd5e1`)を背景色にした（既存の暗くする`box-shadow`はそのまま残るので、天面より少し暗いグレーの側面になる）。
- **駒・マス・カード・山札等にホバーハイライトを追加**: 駒がカードの上に乗っている時など、「今クリックしたら何が掴めるか」が分かりにくいという要望に対応。`findDraggableAt()`と同じ優先順位（駒＞カード＞山）で判定する`findHoverTarget()`を新設し（空のマス/ロックスロットもホバー対象になる点だけ異なる）、`#game-table`の`pointermove`でカーソル直下の対象に`.hover-active`（水色の枠線、ドラッグ中の黄金色ドロップ先ハイライトとは別役割）を付け外しする。ドラッグ中(`dragSession`がある間)はドロップ先ハイライトと役割が被るため休止する。
  - **ハマりどころ**: 当初`.hover-active`に`box-shadow`も付けていたが、`.piece`等ハイライト対象自身が既に別の`box-shadow`を持っていると、後から効いた方のルールで完全に上書きされ、光彩が全く見えなくなっていた（box-shadowは複数ルールの値を自動合成しない）。`outline`は他のどのルールも使っていないプロパティなので、確実に見えるよう`outline`のみで表現するようにした。
- **場のカードを同じマスに重ねると下に潜り込むことがある問題を修正**: `renderBoardTokens()`は`state.tokens`配列の順番通りに`appendChild`するため、配列内で後ろにあるトークンほど画面上で手前（一番上）になる。しかし`MOVE_TOKEN`の実装が`Array.prototype.map()`でその場更新するだけだったため、トークンの配列内での位置（＝重なり順）が「最後に動かした順」ではなく「元々作られた順」のままになっていた。後から同じマスに重ねたつもりのカードが、実は配列内では手前に居た別のカードより「後ろ」に留まり、見た目上潜り込んで見えることがあった。`MOVE_TOKEN`を「動かすトークンを配列から一旦取り除き、更新した上で配列の末尾に追加し直す」実装に変更し、常に「最後に動かしたものが一番上」になるようにした。
- **自分の手札が増えると盤面ごとレイアウトが崩れる問題を修正（重要なハマりどころ）**: `.hand-area`の幅は手札の枚数に比例して自動で広がる仕様（`--hand-a-size`）だが、これが`.player-zone`の通常のフロー内（フレックスの子要素）に置かれていたため、手札が多い時にその大きな幅が`.player-zone`自身の幅、さらには`.game-table`のgridトラック（auto幅の中央列。`.arena`・`.zone-top`・`.zone-bottom`が共有）にまで影響し、盤面(`.arena`)側が押し出される形でズレてしまっていた。`.hand-area`を`position: absolute`にして通常のフローから完全に外し、`.player-zone`自身の中心(50%, 50%)を基準に`translate(-50%, -50%)`で自己センタリングするよう変更（4方向すべての持ち上げtransformの先頭に追加）。これにより手札の幅がどれだけ変わっても周囲のレイアウト計算に一切影響しなくなり、手札12枚まで実際に増やして盤面中心とのズレが実質0になることを確認した。
- **山の側面が一部の角度で透明に見える問題を修正**: `.stack`は側面(前面)を1枚(`.stack-front`)しか持っておらず、駒(`.piece`)のような4面（前後左右）の壁が無かった。見る角度によっては存在しない面（特に右側）の奥が透けて見えていた。駒と同じ「床+4面の壁」の技法で`.stack-back`/`.stack-left`/`.stack-right`を追加し、正しい直方体にした（無地のライトグレー、面ごとに`box-shadow`の濃さを変えて陰影をつけている）。
- **駒のホバーハイライトを、外枠ではなく立方体の各面が光る表現に変更**: `.piece.hover-active`の外枠(outline)を無効化し、代わりに`.piece.hover-active .piece-face`（上面+4つの壁、個別のdiv）それぞれに`box-shadow`の光彩を付けた。`.piece-face`はそれ自体は他でbox-shadowを使っていないため、`.hover-active`本体で踏んだ「box-shadowは複数ルールの値を合成しない」問題を回避できる。
- **表向きカードのホバー拡大プレビューを追加**: 自分の手札・表向きの盤面/ロックのカード・捨て場の一番上のカード（表向き積みのため中身が分かる）にカーソルを乗せると、実物画像を拡大表示する`#card-preview`要素を新設した。`getVisibleCardId(el)`（`getPreviewImagePath`から切り出した共通ヘルパー、右クリックメニューとも共用）が対象の種類に応じてstateから実際のcardIdを引く（DOM上の`background-image`文字列を読み取るのではなく、必ずstateを正とする設計を踏襲）。サイズは`--card-preview-size`（デフォルト20rem、管理者モードで調整可）。位置決めは`left`+`bottom`（`top`ではなく）を使うことで、「拡大の起点は左下端に固定し、サイズが変わってもその起点はズレない」という要望を素直に満たせる。画面右端をはみ出す場合だけ表示方向を反転する。
- **カードの右クリックに専用メニューを追加**: `#game-table`上では常にブラウザ標準の右クリックメニューを出さないようにし（`contextmenu`を`preventDefault()`）、中身の分かるカード（`getVisibleCardId`が非nullを返す対象）を右クリックした時だけ「カード補足を見る」という専用メニューを表示するようにした。クリックすると`docs/cards.md`の補足テキストを表示するモーダルが開く。ダブルクリックと同じ理由で、`contextmenu`イベントのネイティブ`e.target`は信用せず、`findHoverTarget()`（elementsFromPoint()ベース）で対象を判定している。
- **駒のサイズを少し小さく、盤面のカードを少し大きく**: 駒がカードの上に乗ると下のカードがほぼ見えなくなるという指摘に対応。`--piece-size`を2.4rem→2.1remに、`.board-card`の`inset`を8%→3%にした。
- **駒にスキン画像を反映**: `画像素材/駒スキン/`（7色分）を`assets/pieces/`にコピーし（プレイマット・カード画像と同じ理由で、`.gitignore`の`/assets/pieces/`で公開リポジトリには含めない）、`buildCubePiece()`で単色塗りつぶし(`background`)ではなく`background-image`でスキン画像を敷くようにした。上面・4つの壁すべてに同じ画像を敷き、壁ごとの陰影（既存の`filter`/`box-shadow`）はそのまま効く。
- **プレイヤーCの名前ラベルを180度回転**: `.zone-top .label`のtransformに`rotate(180deg)`を追加（B/Dのrotate(90deg)/(−90deg)と同じ考え方）。
- **ロックエリアに置いたカードを、そのロックの持ち主から見て読みやすい向きに回転**: `.lock-top .board-card { transform: rotate(180deg); }`、`.lock-left`は90deg、`.lock-right`は-90deg（`.lock-bottom`＝自分Aは元々正しい向きなので変更不要）。座席配置（手前=A、左=B、奥=C、右=D）に合わせた回転角。
- **捨て場が空の時の塗りつぶしを透明に変更**: `.pile-discard`の紺色グラデーションを`background-image: none; background-color: transparent;`にした（ロックスロットの「うっすら枠だけ」と同じ考え方）。カードが入れば実物画像がinline styleで上書き表示される。
- **ファーストカードのパイルを新規追加（プレイマット右上）**: これまでデータ(`FIRST_CARDS`)だけ存在し実際のゲームには未接続だった（Phase1のセットアップ自動化スコープ外としていた）。ユーザーからの要望で、エターナルカードと同じ「シャッフルされた1つの山」として、盤面グリッドの空いていた右上（`.zone-first { grid-column: 3; grid-row: 1; }`。deck=左上, eternal=左下, discard=右下, first=右上で4隅が埋まった）に追加した。山札・エターナルと同様に裏向き積み（`assets/cards/back-first.png`、`getCardBackImagePath()`に`"first-"`接頭辞の判定を追加）。プレイヤーへの自動配布・ロックは引き続き未実装で、ドラッグで手動配布する運用。あわせて「山札一覧」ビューアにもファーストカードのセクションを追加し、`FIRST_CARDS`各定義に`note`（docs/cards.mdの補足）を追加した。
- **同じマスに重なったカードに「+N」バッジを追加、ホバーで一覧拡大表示**: `getCardStackGroups()`でトークンをlocationごとにグループ化し（駒は数えず`kind:"card"`のみ）、2枚以上重なっているグループの一番上のカードに`.stack-badge`（例:「+2」）を付けるようにした（`renderBoardTokens`）。バッジは`findHoverTarget()`で最優先に判定され、ホバーすると`#stack-preview`に重なっている全カードを横一列で拡大表示する。各カードは自分自身の`faceUp`に従って表向き/裏向きの画像を出す（下に潜んでいる裏向きカードの中身を、一覧表示によって覗けてしまわないようにするための配慮）。
- **「山札一覧」をスクロール不要のグリッド表示に再設計**: 1カード1行の縦積みレイアウト（33件で画面に収まらずスクロール必須だった）から、サムネイル中心のグリッド（`grid-template-columns: repeat(auto-fill, minmax(5rem, 1fr))`）に変更し、パネル幅も`min(68rem, 96vw)`に拡大した。タイルをクリックすると、そのタイルの直後（`grid-column: 1 / -1`で全幅）に補足テキストが開閉する。サブタイトルの「（データと画像の紐づき確認用）」は削除。
- **「+Nバッジのホバー一覧表示」機能を撤去し、右クリックメニュー方式に置き換え（ユーザー判断による仕様変更）**: ホバーで重なりカードを横一列拡大表示する機能が正しく動作しないと報告があり、かつ「何枚も重なっていると横一列表示自体が原理的に厳しい」との判断で、デバッグではなく設計変更として対応した。`getStackPreviewEl`/`showStackPreview`/`#stack-preview`/`.stack-preview-card`を全て削除。代わりに、右クリックメニューに「重なっているカードを見る」を追加し、クリックすると要クローズの`#stack-modal`（`flex-wrap`+`max-height`+`overflow-y: auto`で何枚重なっても画面内に収まる一覧）が開く方式にした。新設した`getStackTokensAt(el)`が、右クリック対象（`.board-card`または`.stack-badge`）の属する重なりグループ（2枚以上）の全トークンidを`getCardStackGroups()`から引く。
- **右クリックメニューを単一項目決め打ちから配列ベースに変更**: 「カード補足を見る」を出すか「重なっているカードを見る」を出すか（あるいは両方）を対象に応じて動的に決める必要が出たため、`showContextMenu(clientX, clientY, cardId)`だったシグネチャを`showContextMenu(clientX, clientY, items)`（`{label, onClick}`の配列）に変更した。`initContextMenuHandlers`側で`getVisibleCardId`と`getStackTokensAt`それぞれの結果に応じてitemsを組み立てる。
- **カード補足モーダルに拡大画像を追加**: `showCardNoteModal(cardId)`が今までタイトル+補足テキストのみだったのを、`getCardImagePath(cardId)`の画像(`.card-note-image`)を先頭に追加した。
- **「+N」バッジを`getVisibleCardId`の対象に追加**: バッジ自体を右クリック/ホバーした時にも、その下の一番上のカードと同じ扱い（表向きなら中身が分かる）になるよう、`getVisibleCardId`の先頭に`.stack-badge`の判定を追加した（バッジの`dataset.stackTokens`の最後のidが一番上のトークン）。これによりバッジへのホバーでも通常の`#card-preview`拡大表示が出るようになった（横一列プレビューの代替）。
- **駒スキン画像の角に隙間が見えるバグを修正**: `.piece-face`（駒の5面共通の基底クラス）に`border-radius: 0.2rem`が付いていたため、正方形のスキン画像がその分内側に丸められ、立方体の角で隣接面との間に隙間が見えていた。単色塗りつぶし時代の名残だったため、border-radiusごと削除した。
- **山札・エターナル・捨て場・ファーストカードの山の上面から、常時表示の名前・枚数テキストを撤去し、ホバー時のみ表示するツールチップに変更**: `buildCardStack()`から`nameEl`/`countEl`（常時表示だった`.stack-top`の子要素）を削除し、代わりに新設した`getPileTooltipText(el)`（`.stack[data-pile]`要素から`PILE_CONFIG`のラベルと`state.piles[pileKey].length`を組み立てる。捨て場は空でなければ一番上のカード名を表示）と、それを表示する`#pile-tooltip`（`updatePileTooltip()`、`updateHover`から呼ぶ）を追加した。
- **カード拡大プレビューが画面上端で見切れるバグを修正**: `positionPreviewPanel()`は元々画面右端のはみ出し（left/right反転）にしか対応しておらず、盤面奥（画面上部）のカードをホバーすると、`bottom`基準で上方向に伸びるプレビューパネルが画面上端(`y=0`)より上にはみ出て見切れていた。カーソルY座標とパネルの実測高さから「上方向に広げると画面上端を割るか」を判定し、割る場合だけ`bottom`基準ではなく`top`基準（カーソルの下方向に広げる）に切り替えるようにした。
- **「山札一覧」のカードタイルクリックを、インラインのテキスト展開から拡大画像+補足のモーダル表示に変更**: `deck-viewer.js`の`buildCardTile`が返していた`{tile, note}`（クリックでnoteのdisplayを切替）をやめ、クリックで`showCardModal(def)`（deck-viewer.js内に新設した専用モーダル、画像+タイトル+補足+閉じるボタン）を開く方式にした。

- **カード補足モーダル（右クリック・山札一覧共通）を、画像の下にテキストが続く縦積みから、画像の右隣にテキストが並ぶ横並びレイアウトに変更**: スクロールをなるべく減らしたいという要望に対応。`#card-note-modal`/山札一覧の専用モーダルとも、`display:flex; flex-wrap:wrap;`のcontent行の中に画像(固定幅14rem)とテキスト列(`flex:1`)を並べ、画面が狭い時だけ自動的に縦積みにフォールバックする。
- **エターナルの山の表示名を「永久」→「エターナルカード」に変更**: `PILE_CONFIG.eternal.label`のみの変更（ホバー時のパイルツールチップに使われる）。

- **ファーストの山の表示名を「ファースト」→「ファーストカード」に変更**: `PILE_CONFIG.first.label`のみの変更（エターナルと同じくホバー時のパイルツールチップに使われる）。
- **カード補足モーダル（右クリック・山札一覧の両方）の閉じるボタンを、下部の「閉じる」テキストボタンから右上の丸型✕ボタンに変更**: 新設した`.modal-close-x`（`position: absolute; top/right`、モーダル本体は元々`position: fixed`なのでそのまま基準にできる）を両モーダルで共用。画像+テキスト横並びレイアウトとの相性を考えて採用。

- **カード補足モーダル（右クリック）もモーダル外クリックで閉じられるように**: 山札一覧のカードモーダルは元々`#deck-card-modal-backdrop`という全画面backdropにclickリスナーを付けて閉じる仕組みが既にあったが、右クリックの`showCardNoteModal`にはbackdropが無く、外側クリックで閉じられなかった。同じ「透明な全画面backdrop＋そのclickでclose()」パターンを追加（`#card-note-modal-backdrop`、透明のままダイアログのdimはしていない）。

- **「セットアップウィザード」を追加（[src/game-setup.js](src/game-setup.js) 新規、docs/rulebook.mdの「Game Preparation」を再現）**: 画面右上に「🎲 セットアップ」ボタンを新設し、開くと以下5つのボタンを持つパネルが出る。
  - **０：プレイ人数選択、白黒カード確認**: プレイ人数(2/3/4)を選ぶダイアログ（管理者モードのトグルがONなら、人数の代わりにA/B/C/D各座席を自由にチェックボックスで選べるモードに切り替わる）。白黒（無色）カードを山札に含めるかのチェックボックスもここにあり、説明書の「初めてプレイする時は外すと簡単」という推奨に合わせてデフォルトは含めない。
  - **１：ファーストカードを配り、駒を配置する**: 実行の最初に必ず`resetGame()`（新設のRESET_GAMEアクション）で盤面を完全に空にする（ユーザー確認の上、ルールブック通り「初期手札なし」から作り直す設計にした。既存の起動時デフォルト値=`HAND_START`の仮配布はテスト用データとして手を付けず、あくまでこのウィザードを通した時だけ本来のルール通りの状態になる）。その後、参加座席の時計回り順に`ファースト`の山から1枚ずつ引き、そのカードと同色のロックエリアへ表向きでロックし（`SETUP_ASSIGN_FIRST_CARDS`）、同じ色の駒をそのプレイヤーのゲートに置く。
  - **２：盤面にカードを配置する**: 山札（白黒カード含む/含まないはステップ0の設定に従う）をシャッフルし、場の7×7＝49マスに1枚ずつ裏向きに配置する（`SETUP_FILL_BOARD`）。再実行しても安全なように、まず場のカード（駒は除く）だけ一旦取り除いてから配り直す設計。
  - **３：スタートプレイヤーを決める**: 参加座席から無作為に1人選び、モーダルで表示するだけ（状態には残さない。ターン管理の仕組み自体がまだ無いフェーズ1のため）。
  - **１〜３を一気に行う**: 上記を順番に実行する。
  - 座席(A/B/C/D)⇔盤面の辺(bottom/left/top/right)の対応・7色の並び順・ゲートの座標は、main.js/state.js両方から必要になったため`board-layout.js`（新規）に切り出し、重複によるズレを防いだ。
  - 配布時のアニメーションは今回は見送り（ユーザーとの合意により、まず配布ロジックを固めてから別途追加する）。
  - 座席の自動/自由選択の切り替えは、CSS変数のスライダー群とは性質が異なる「挙動の切り替え」のため、admin.jsに`manualSeatMode`という単純なbool値＋チェックボックスUIとして追加した（`isManualSeatMode()`をexport）。

- **セットアップウィザードのUIを「別ダイアログ」から「パネル内展開→決定で差し替え」方式に変更**: 「０：プレイ人数選択、白黒カード確認」を、以前は縮小されたボタンを押すと別のモーダルが開く形にしていたが、「設定しないと１〜３ができないことが分かりやすいように」という要望で、パネルを開くと最初から設定フォームが展開された状態になり、「決定」するまでは１〜３のボタン自体が存在しない（ensureConfigによる後追いチェックではなく、そもそも表示しないことで強制する）方式に変更した。決定後は「⚙ 設定を変更（０に戻る）」でいつでもフォーム表示に戻れる。`game-setup.js`内に`bodyEl`（パネル内の可変領域）と`renderPanelBody()`（フォーム/ステップボタンの差し替え）を新設。
- **ターン管理を導入（state.js）**: これまでセットアップウィザードの手順3「スタートプレイヤーを決める」は無作為に選んだ結果をモーダルで見せるだけで状態には残していなかったが、`state.activePlayers`（手順1で確定した参加座席）と`state.turnPlayer`（現在の手番）を新設し、実際に状態として持つようにした。新規アクション`SET_TURN_PLAYER`（手順3が呼ぶ）、`NEXT_TURN`（`activePlayers`を時計回り順に絞り込み、現在の`turnPlayer`の次へ進める。末尾の次は先頭に戻る）を追加。
- **手番プレイヤーを目立たせる演出を追加**: 手番プレイヤーのロックエリア全体に、ゆっくり柔らかく明滅するグロー（`@keyframes lock-area-turn-glow`、`.lock-area`自体は背景を持たないが`box-shadow`は背景の有無に関係なく外側に描画されるためこれだけで光る）を、名前ラベルには点滅＋文字色変更（`@keyframes label-turn-blink`）を追加。`buildLockArea(side)`は`SEAT_TO_SIDE[turnPlayer] === side`、`buildPlayerZone`は`player === turnPlayer`を見てそれぞれ`.is-turn-player`クラスを付け外しする。
- **「ターンを次のプレイヤーへ渡す」ボタンを新設**: 開発者向けツール（管理者モード＝左上、山札一覧/セットアップ＝右上）とは離し、プレイヤー自身が操作するゲームプレイ上のボタンとして画面右下に配置（`#end-turn-button`）。`state.turnPlayer`がnull（セットアップ手順3が未実行）の間は非表示。押すと`nextTurn()`を呼び、次のプレイヤー名を表示するボタン自体のテキストも毎回`render()`内の`updateEndTurnButton()`で更新する。
  - 座席⇔盤面辺の対応・7色の並び順に加え、時計回りの座席順を扱う`SEAT_ORDER`・座席ラベルの`SEAT_LABELS`も含め、これらは全て`board-layout.js`にまとめてあるため、ターン管理のような新しい機能追加でもmain.js/state.js/game-setup.js間でズレる心配がない。

- **手番グローの色を管理者モードから切り替え可能に**: `.lock-area.is-turn-player`の発光色をハードコードせず`--turn-glow-rgb`というCSS変数（`rgba(var(--turn-glow-rgb), 透明度)`という形でbox-shadowから参照）にし、admin.jsに「ロックエリアの手番グローを白色にする（オフ=黄色）」というチェックボックスを追加した（デフォルトは黄色寄り`255, 224, 130`、オンで`255, 255, 255`）。
- **「相手ゲート侵攻ボーナス」(docs/rulebook.md「Gate Invasion Bonus」)を自動処理化（[src/gate-invasion.js](src/gate-invasion.js) 新規）**: 「ターン終了」ボタンが押された瞬間、手番プレイヤーの駒が参加中の別プレイヤーのゲートに乗っていれば（自分のゲート・空席のゲートは対象外）、ルールブック通り①手札を半分（端数切り捨て）奪う→②エターナルカードを1枚無作為に獲得しロック→③自分のゲートのカードを回収してゲートに帰還、の3段階を、それぞれ内容を予告する確認ポップアップ（OK押下で実行）を挟みながら自動で順番に処理する。条件を満たさなければ何もせず通常通りすぐ次のプレイヤーへターンが渡る。
  - ②では、ロック先のスロットに既に何か（ファーストカードを除く）があれば先に手札へ加えるルールも再現した。エターナルカードの山が空の場合は「もう残っていません」という文言に切り替わる（ルール「無くなったら以降もらえない」）。
  - どの駒が誰のものかを判定するため、`SETUP_ASSIGN_FIRST_CARDS`が生成する駒トークンに新しく`player`フィールド（座席）を持たせるようにした（従来は`color`のみで、駒と座席の対応が取れていなかった）。
  - 新規state.jsアクション: `GATE_INVASION_STEAL_HAND`・`GATE_INVASION_ETERNAL`・`GATE_INVASION_RETURN_HOME`。乱数選択（奪うカードの抽選・エターナルの山の一番上）は呼び出し側(gate-invasion.js)が事前に行い、確認ポップアップの文言に反映してからreducerに渡す設計（reducer自体は受け取ったid通りに動くだけの純粋な処理）。
  - `board-layout.js`に、ゲートの辺からその持ち主の座席を逆引きする`SIDE_TO_SEAT`を追加。

- **駒のホバー/掴む当たり判定エリアの広さを管理者モードから微調整可能に**: 駒の見た目（立方体）はそのままに、判定だけを広げたり狭めたりしたいという要望に対応。`buildCubePiece()`が生成する`.piece`の中に、新しく透明な`.piece-hitbox`（`--piece-hitbox-scale`でサイズだけ独立に拡縮、中心基準）を追加した。findDraggableAt/findHoverTargetは`.closest(".piece")`で判定しているため、この子要素をクリック/ホバーしても正しく親の`.piece`（＝実際の駒トークン）に解決される。admin.jsに「駒の当たり判定（ホバーすると発光する範囲）」というスライダー（0.5〜2.5倍、デフォルト1＝見た目と同じ）を追加。
- **相手ゲート侵攻ボーナスの自動処理を「手番プレイヤー限定」から「参加中の全プレイヤー対象」に修正**: ルールブックに「あなたのターン、相手のターンどちらでも発生する」とある通り、何らかの効果で自分のターンでなくても相手ゲートに駒がいることはセブンではあり得るため、「ターン終了」ボタンを押した瞬間のチェック対象を手番プレイヤー1人に限定せず、参加している全プレイヤー（時計回り順）に広げた。該当者が複数いれば順番に1人ずつボーナス処理を行ってから、最後に１回だけターンを次のプレイヤーへ進める。`gate-invasion.js`の`runGateInvasionIfNeeded(attacker, done)`を`runGateInvasionsIfNeeded(done)`（引数なし、内部で全参加プレイヤーを走査）に変更。
- **手番プレイヤーの名前ラベルの点滅速度を、ロックエリアの明滅と同じ2.6秒に統一**: 別々のタイミングで点滅しているとちぐはぐに見えるため、`label-turn-blink`のアニメーション時間を`1.1s`→`2.6s`（`lock-area-turn-glow`と同じ）に変更。
- **カードが誰かの手札に加わった時、「何を得たか」を知らせるトースト通知を追加（[src/hand-announcer.js](src/hand-announcer.js) 新規）**: `announceHandPickups(player, pickups)`が、手札に入る直前にそのカードが「公開情報」（捨て場から取得した／既に表向きだった）だったか「非公開情報」（山札等から裏向きで引いた／相手の手札から奪った）だったかに応じて出し分ける。公開情報、または受け取った本人が自分(A)の場合は画像＋名前入りの詳細なトーストを、それ以外（本人以外が非公開情報を得た場合）は「プレイヤー●が非公開のカードをN枚手札に加えました」という事実だけのトーストにする（複数枚まとめて手に入る場合は公開/非公開が混在しても1つのトーストに収める）。3.2秒で自動的に消えるか、クリックで即座に消せる。
  - 呼び出し箇所: `main.js`の`onDragEnd`（山からのドロー・盤面/ロックからの移動、両方とも手札が行き先の時）、`gate-invasion.js`の3ステップ全て（①相手の手札を奪う＝常に非公開、②ロック先から弾かれた既存カード＝常に公開、③自ゲートのカード回収＝各カード自身のfaceUpに従う）。

- **ロックエリアに置いたカードは原則、表向きにするよう修正**: `faceUpForLocation(location)`が今まで`zone:"lock"`を「場」と同じ裏向き扱いにしていた（ルール「ロックする：カード1枚を...表向きで置くこと」に反していた）。ロック行き先は常に`faceUp:true`にするよう`MOVE_TOKEN`・`DRAW_FROM_PILE`双方の表裏決定ロジックを修正（`MOVE_TOKEN`は「場・ロック間の移動は表裏を変えない」という既存の例外より、ロック行きの表向き化を優先するよう条件を並べ替えた）。
- **駒の下にカードがあると駒の「下の方」が掴めなくなるバグを修正（重要なハマりどころ）**: `findDraggableAt`/`findHoverTarget`は`document.elementsFromPoint()`が返す要素配列を1回のループで「要素ごとに駒→カード→...の優先順位を判定して最初に一致したものを返す」実装だったため、たまたまその座標の一番手前（先頭）の要素が`.board-card`（駒の当たり判定を追加する前から存在する、セルにほぼ密着した大きな要素）で、かつその奥（配列の後ろの方）に`.piece`（今回追加した`.piece-hitbox`含む）があっても、ループが`.board-card`の時点で確定して返ってしまい、駒より優先度の低いはずのカードが先に選ばれてしまうことがあった。駒の可視な立方体は3D持ち上げ(`translateZ`)で上寄りにずれて描画されるため、見た目の「上の方」ではこの競合が起きにくく「下の方」でだけ再現していた。修正として、要素配列を種類（駒→盤面カード→手札カード→山）ごとに全体を舐めてから確定する2段階ループに変更し、描画順に関係なく常に優先順位通りに解決されるようにした。
- **駒の当たり判定の広さを管理者モードから調整可能に**: `buildCubePiece()`が生成する`.piece`の中に、見た目には影響しない透明な`.piece-hitbox`（`--piece-hitbox-scale`で中心基準に拡縮）を追加。管理者モードに「駒の当たり判定（ホバーすると発光する範囲）」スライダーを追加した。
- **管理者モードのパネルを大幅に整理**: 項目が増えて縦に長くなりすぎていたため、各セクションを`<details><summary>`で開閉できるようにし（デフォルトは全て閉じた状態）、パネルを開いた直後の見た目を大幅にコンパクトにした。
- **パネル/モーダルの閉じ方を「右上✕ボタン＋外クリックでも閉じる」に統一（[src/ui-helpers.js](src/ui-helpers.js) 新規、`createModalCloseX`/`createBackdrop`）**: 管理者モード・山札一覧・セットアップウィザード・重なりカード一覧モーダルの4つに適用（管理者モード・セットアップウィザードは常駐ツールなので背景は暗くしない透明backdrop、山札一覧は情報モーダルなので暗くするbackdropのまま）。以前の下部「閉じる」ボタンは撤去。今後追加するパネル/モーダルもこの部品を使って統一する方針。
- **カード獲得ポップアップの大きさ・表示時間を管理者モードから調整可能に、✕ボタンで即座に消せるように**: `--hand-pickup-toast-scale`（デフォルト1.3、以前より大きめ）・`--hand-pickup-toast-duration`（秒、デフォルト5秒、以前の3.2秒より長め）を追加し、管理者モードに「カード獲得ポップアップ」グループとして追加。トースト右上にも`.modal-close-x`を追加（クリックで即座に消せる。トースト全体クリックでの即消しも従来通り残している）。
- **画面右下に「1枚ドロー」ボタンを追加**: 手番プレイヤーが山札から1枚引いて自分の手札に加える簡易ショートカット。「ターンを次のプレイヤーへ渡す」ボタンと同じく、`state.turnPlayer`が無い間は非表示。
- **山札が空になった時の自動補充を実装（docs/rulebook.md「こんな時は」）**: 「捨て場のカードをそのまま裏向きにして山札とする。シャッフルはしない。」を再現。山札が空かつ捨て場にカードがある状態で「1枚ドロー」を押すと確認モーダルが出て、OKすると新規`REFILL_DECK_FROM_DISCARD`アクションが捨て場の山を（物理的に「ひっくり返す」動作にあたるため並び順を反転させて）山札に丸ごと移し、捨て場を空にしてからそのままドローを続行する。
- **7色ロック達成による勝利モーダルを実装（[src/victory.js](src/victory.js) 新規）**: `main.js`の`render()`終盤で毎回`checkForVictory()`を呼び、参加中の各プレイヤーについて自分のロックエリアの7色スロットが全て埋まっているかを判定、初めて達成した瞬間だけ金色に光るグロー＋トロフィー絵文字の勝利モーダルを表示する（一度出したプレイヤーは同じ対戦中は出し直さない。セットアップウィザードの手順1でリセットした時だけ`resetVictoryTracking()`で記録を忘れる）。

- **駒スキンをいつでも選び直せるように（[src/piece-skins.js](src/piece-skins.js) 新規）**: 画面右下に「🎨 駒スキン」ボタンを新設（自分の駒がまだ無い＝セットアップ未実施の間は非表示）。押すと自分の駒の色（ファーストカードで固定、変えられない）に対応する4種類（標準＋追加1〜3）のスキン画像をスワッチで選べるモーダルが開く。`画像素材/駒スキン追加/`にあった3セット×7色を`assets/pieces/${color}-{1,2,3}.png`としてコピーし（他の駒画像と同じ理由でgit管理外）、選択は色ごとに保持する（`skinIndexByColor`）ため、同じ色を引いた別のプレイヤーにもそのスキンが適用される。`buildCubePiece(color)`は`getSkinImagePath(color)`経由で画像パスを解決するよう変更。
- **山札・エターナル・ファーストの山が0枚でも満杯に見えるバグを修正**: `buildPileZone`が常に`config.backImage`をimagePathとして渡していたため（捨て場だけ0枚の時にnull扱いする特別分岐があったが、他の山には無かった）、0枚でも裏面画像がそのまま表示され続けていた。全ての山で0枚ならimagePathをnullにし、`buildCardStack`側でもnullの時は`background-image:none;background-color:transparent;`を明示設定してCSS側のフォールバック色（`.pile-deck`等の暗いグラデーション背景）を打ち消すようにした（捨て場が空の時と同じ「中身が無いと分かる」見た目に統一）。
- **山札切れの自動補充モーダルをOKを押すまで閉じないように変更**: 他のパネル/モーダルは「✕＋外クリックで閉じる」に統一したが、このモーダルはゲーム状態に関わる必須の確認のため、意図的にその対象から外し、`showConfirmModal`から✕ボタン・外クリックでの閉じる仕組みを削除した（`createBackdrop`のonClose引数にno-opを渡すことで、暗い背景は表示しつつクリックしても閉じないようにしている）。
- **相手の手札にカーソルを合わせると枚数だけ表示されるように**: 中身は非公開のまま、`getHandTooltipText(el)`（`.hand-card`かつ`is-self`でない時だけ）が「プレイヤー● 手札N枚」を返し、既存のピール用ツールチップ（`updatePileTooltip`）にそのまま乗せる形で統合した。
- **自分の手札枚数を画面左下に常時表示（`#self-hand-status`）**: 手札が扇状に重なって数えづらいため、自分（A）の手札枚数だけを常に見える小さなステータスとして追加した（他のプレイヤーの手札枚数はこれでは分からず、上記の相手手札ホバーでのみ分かる）。
- **自分のターン中、自分の駒も自分の駒の色でゆっくり柔らかく発光するように**: ロックエリア・名前ラベルの手番演出とは別に、`renderBoardTokens`で`token.player === "A" && turnPlayer === "A"`の時だけ駒に`.is-my-turn-glow`を付与し、`--piece-turn-glow-color`にその駒自身の色（`var(--color-${token.color})`）をJSから設定、CSSの`box-shadow`アニメーションで発光させる（ロックエリアと同じ2.6秒周期）。

- **「１〜３を一気に行う」完了時にセットアップウィザードを自動で閉じるように**: `game-setup.js`に`closePanel`という参照を持たせ、`initGameSetup()`内の`close()`をそこに登録、`runAll()`の最後に呼ぶようにした。
- **手番プレイヤーの駒の発光が自分(A)にしか効いていなかったバグを修正**: 前回「自ターンの時に自分の駒も発光」を実装した際、条件を`token.player === "A" && turnPlayer === "A"`と自分限定にしてしまっていた。ロックエリア/名前ラベルの手番演出と同じく「手番プレイヤー本人の駒」を対象にすべきだったため、`token.player === getState().turnPlayer`（座席を問わない）に修正。B/C/Dのターンでもそれぞれの駒が光ることを確認済み。
- **プレイヤー名・アバターを自由に変更可能に（[src/player-identity.js](src/player-identity.js) 新規）**: `getPlayerName`/`setPlayerName`・`getPlayerAvatar`/`setPlayerAvatar`を追加し、これまでコード各所にハードコードされていた`SEAT_LABELS[player]`の参照（main.js・game-setup.js・gate-invasion.js・hand-announcer.js・victory.js）を全てこれ経由に統一した。これにより名前を変更すると盤面のラベル・ターン終了ボタン・各種ポップアップ・トースト等、表示箇所すべてに反映される。アバターは画像素材が無いため、絵文字12種（🦊🐸🐙🦉🐲🦄🐧🦁🐺🐢🐬🦋）のダミーセットから選ぶ簡易実装（`AVATAR_OPTIONS`）。
  - 画面左下の自分専用ステータス（`#self-hand-status`）を、アバター（クリックで選択モーダル）＋名前（クリックでその場編集、Enter/blurで確定）＋手札枚数の3点セットに拡張した。
  - 盤面上にも各プレイヤーのアバターを追加（`buildPlayerZone`内、`.player-avatar`）。「相手のアバターは手札の後ろ側に」という要望に対応するため、DOM順を名前ラベル→アバター→手札の順にし（手札が後から描画される＝重なった時に手前）、位置・サイズを管理者モードの新グループ「プレイヤーアバターの位置・サイズ」から調整できるようにした（`--avatar-size`共通1つ＋`--avatar-{a,b,c,d}-pos-x/y`）。
- **「盤面拡大」ボタンを追加**: 画面右下に新設。押すと、プレイヤーA（手前）のロックエリアが画面下端、プレイヤーC（奥）のロックエリアが画面上端にほぼ収まる倍率まで拡大する。もう一度押すと通常の全体フィット表示に戻るトグル式。実装は「A側ロック〜C側ロックの中間点」を`transform-origin`に動的設定してからscale3d()で拡大する方式（中間点の画面上の位置を変えずに、その区間だけを上下対称に引き伸ばせる）。`fitTableToViewport()`を`applyNormalFit()`/`applyBoardZoomFit()`に分岐させ、`boardZoomed`フラグで管理。

- **「盤面拡大」ズームの位置を管理者モードから微調整可能に**: 理論上はA側ロック〜C側ロックの中間点を基準にscale3d()するだけでちょうど収まるはずだったが、手札・アバターの飛び出しやブラウザのレンダリング誤差で実際には手前のロックエリアが画面下端で見切れることがあった。`--board-zoom-margin`（拡大率の余白、小さいほど余白が増える）・`--board-zoom-offset-x/y`（transformにtranslateとして追加する微調整分）を新設し、管理者モードの新グループ「盤面拡大ボタンのズーム位置調整」から変更できるようにした。offset-yを変えると実際に見切れを解消できることを確認済み。
- **ロックエリアの色の存在感を弱める調整肢を拡充**: 枠線の太さ（既存の`--lock-slot-border-width`。範囲を0.02〜0.4remから0〜0.4rem・step0.01に広げ、枠線を完全に無くすことも選べるようにした）に加えて、新しく`--lock-slot-glow-scale`（色付きグローの強さ。枠線を細くしてもcurrentColorのbox-shadowグローだけ強いと結局目立ってしまっていたため）を追加。両方とも管理者モードの「ロックエリア」グループから調整できる。
- **プレイマットを複数用意し、セットアップ時に選べるように（[src/playmat.js](src/playmat.js) 新規）**: プレイマット画像の保存先が`画像素材/プレイマット/`に変わり、白・青・黒の3種類が用意されたため、`assets/playmats/{white,blue,black}.png`としてコピーした（他の実物画像素材と同じ理由でgit管理外、`.gitignore`に`/assets/playmats/`を追加）。セットアップウィザードの手順0のフォームに「プレイマット」選択（ラジオボタン、デフォルト白）を追加し、決定した瞬間に盤面へ即反映される（`.playmat-bg`の背景画像を固定のCSS指定からJS側のinline styleに変更）。
- **ロックしていても使えるカード（ファーストカード・エターナルカード）をロックエリア内で目立たせる**: 該当カードがロックゾーンにある間だけ`.is-usable-while-locked`を付与し、斜めの光の帯が定期的に（4秒周期で）カードを横切る「キラッ」演出と、同じタイミングでの控えめな金色グローの明滅を追加した（`buildFlatCard`でcardIdの接頭辞`first-`/`eternal-`かつ`location.zone === "lock"`の時だけ付与）。

- **管理者モードで調整された値をコードの新デフォルトとして反映**: ユーザーが管理者モードの「出力をコピー」で調整した内容（`--lock-slot-border-width: 0.01rem`・`--lock-slot-glow-scale: 0.25`・`--board-zoom-margin: 0.92`・`--board-zoom-offset-y: -1.7rem`）を、style.cssの`:root`デフォルト値とadmin.jsの各スライダーの`default`値の両方に反映した（「リセット」ボタンを押してもこの調整後の状態に戻るように）。なお「プレイヤーアバターの位置・サイズ」グループは前回のセッションで既に管理者モードに追加済みだったため、変更不要だった。
- **「盤面拡大」ボタンを2段階トグルに変更**: 押すたびに「盤面拡大」→「もっと拡大」→「元に戻す」と3段階を巡回するようにした。両レベルとも基準点（A側ロック〜C側ロックの中間点）は共通だが、倍率・位置の微調整分だけレベルごとに別のCSS変数（1段階目=`--board-zoom-{margin,offset-x,offset-y}`、2段階目=`--board-zoom-2-{margin,offset-x,offset-y}`）を持たせ、管理者モードにレベルごとの調整グループを分けて追加した（2段階目は「もっと拡大」なのでmarginのデフォルトを1.3＝画面より大きく引き伸ばす設定にしてある）。ボタンの見た目も2段階目だけ橙色にして1段階目（緑）と区別できるようにした。

- **駒スキンを左下の自分専用ステータスエリアに統合**: 以前は右下の独立したボタンだったが、アバター・名前と同じステータスエリアに移動した。実際の`buildCubePiece()`をそのまま使い、ドラッグ中のゴースト(`.drag-ghost-piece-outer/-inner`)と同じ「perspective+盤面と同じ傾きを持つ入れ子」のテクニックで、3D空間の外でも本物の駒を立方体のまま小さく表示する（クリックで従来通りのスキン選択モーダルが開く）。`piece-skins.js`から独自の呼び出しボタンを撤去し、データ管理とモーダルだけを持つ形にリファクタした。
- **画面左上にゲームタイトルを表示**: 「7 SHADES OF S:EVEN remake」（旧・管理者モードボタンがあった場所。オプションメニューへの統合で空いたスペースを使った）。
- **右上に「⚙ オプション」ボタンを新設し、その中に「管理者モード」を統合（[src/options-menu.js](src/options-menu.js) 新規）**: 以前は左上に専用の呼び出しボタンがあったが、オプションメニュー配下のドロップダウン項目にした。`admin.js`は自前の呼び出しボタンを持つのをやめ、`openAdminPanel()`という開閉トリガーだけを外部提供する形にリファクタした。
- **ロック中でも使えるカードの強調演出を2種類から選べるように**: 前回実装した「斜めに光る帯」は存在感が強すぎたため大幅に細く（35%→6%幅）した上で、新たに「そのロックスロットの色をした小さな球がカードのふちに沿ってゆっくり周回する」演出（`effect-orbit`、新デフォルト）を追加。球の動きはCSS`offset-path`等のモーションパスAPIに頼らず、4隅を等間隔のキーフレームにして直線的に結ぶ方式（確実に全ブラウザで同じ見た目になる）。管理者モードの「ロック中でも使えるカードの強調演出」でどちらか切り替えられる（`getUsableLockedEffect()`）。
- **管理者モードで新たに調整された値を反映**: `--board-zoom-2-offset-y: -2.5rem`、プレイヤーアバターの座席ごとの位置（A/B/C/Dそれぞれ）を、style.cssの`:root`デフォルトとadmin.jsの`default`値の両方に反映した。

> **注（コンテキスト削減のための整理・2026-08-28／2026-09-05／2026-09-06／2026-09-09）**: 続き486以前（2026-07-19〜2026-09-08）の詳細な変更履歴は [docs/dev-log.md](docs/dev-log.md) へ移動しました（内容は削除せず移動しただけ）。過去の経緯・不具合の詳細を調べる時はそこを grep してください。以下は続き487以降（直近の作業）です。

### 2026-09-08（続き487）：ゴメンナサイの発動を「ど派手」に（CPUが使った時に「防いだ！」が出ていなかったのも直した）／豆知識の囲みを外して高さを固定／戦績システムの段位ランキングにログイン導線

ユーザー要望4件（＋質問2件は下記の申し送り）。

- **【ゴメンナサイの発動演出】ユーザー要望「使用されるタイミングはど派手にしたい」→「やろう！！」**:
  相手の7色目（＝勝利宣言）を止めたうえに完成間近のロックを1枚奪う、試合中で最大の逆転なのに、
  今までは「防いだ！」（盾＋衝撃波）だけで**発動そのものの宣言が無かった**。
  `playGomennasaiDeclaration()` を新設し、**全画面が紫に染まる → カードが斜めに滑り込んで大きく止まる
  → 紫と金の輪が2重に弾ける → 「ゴメンナサイッ！」の大きな名乗り**、を1.5秒で見せる。
  - **重い装飾は使っていない**（`filter` / `backdrop-filter` は全画面に掛けるとGPUの弱い端末で
    1フレームを丸ごと持っていく＝続き450の実測。#339で重さが問題になっている今はなおさら）。
    **塗りと transform だけ**で組んだ。body直下なので盤面のWebGL描画のON/OFFにも2D表示にも左右されない。
  - **見つけたついでの穴**: `cpuUseGomennasaiOnFinalLock`（CPUが使う経路）には
    **`playBlockedEffect` の呼び出しが無かった**——「防いだ！」は人間の経路にだけ入れてあった。
    同じ見え方に揃えた。
  - **オンラインでも相手・観戦者に出る**: 既存の `broadcastBlocked` の payload に `via: "gomennasai"`
    を足すだけにした（online.js は payload を素通しするので**あちらは無改造**）。受け手は
    `via` を見て「宣言 → 防いだ！」の順に再生する。
  - **実測**: 覆いが `rgba(88,28,135,0.78)`、カード214px、文字の不透明度0.97、輪の遅延0.16s。
    スクリーンショットでも紫の染まり・カード・名乗りを確認。例外0件。
  - **未確認（申し送り）**: `playGomennasaiDeclaration` は main.js の内部関数で外から呼べないため、
    **本物の対局で最後のロックを止める場面**は通していない（CSSと配線＝4か所の置換は確認済み）。
    実機で一度見てほしい。

- **【豆知識】ユーザー指摘「背景はさすがにダサくないですか？w 分量によって枠のサイズが変わるのも嫌」**:
  そのとおりなので**囲み（背景・枠線・角丸・内側の余白）を全部やめ**、文字だけを大きく中央に置いた。
  高さは `height: 5.25em`（3行ぶん）で**固定**し、中で上下中央に置く——1行でも3行でも箱が動かないので、
  下のキャンセルボタンが跳ねない。
  - **「ぼかす」案は採らなかった**。全画面のぼかしは上と同じ理由で重い。塗りも敷かない方が軽くて綺麗。
  - 自分が前回足したライトモードの規則が**既存の規則と重複していた**ので取り除いた（実害は無かったが、
    同じ宣言が2か所にあると後から片方だけ直す事故になる）。

- **【戦績システム】ユーザー判断「ログインしてほしいので、ログインを促す感じに」**（続き486で段位を
  一番上に持ってきた結果、未ログインだと一番上が空になる件）: 「表示されません」で終わらせず、
  **理由＋その場でログインできるボタン**（`loginWithGoogleForStats()`）を出すようにした。
  ログイン済みで記録が無い時は従来どおりの一文。実測でボタンと onclick を確認。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  構文チェック通過、CSSブレース平衡、`game.gomennasai.*` の翻訳キーが ja/en とも同数。
  お知らせ（changelog.js）に ja/en とも3件追記。
  サーバー側の変更は無い＝SQL実行・Edge Function の再デプロイは不要。
  戦績システム（BATTLE-log）は別リポジトリで別コミット（index.html のみ）。

- **申し送り（ユーザーからの質問2件・未着手）**:
  1. **「対戦直前に今回戦うのはこのメンバーです、を出すのはどう？」**（Apexのチャンピオン部隊の画面が例）。
     材料は揃っている——`activePlayers` の座席・`getPlayerName` / `getPlayerAvatar`・駒の色・
     ランク戦なら `so7_ranked_players` の段位。**対局開始の合図**（`turnPlayer` が初めて入る瞬間＝
     `initTutorialAutoStart` が使っているのと同じ引き金）で1回だけ出せばよい。
     入れるなら「席の順に1人ずつ、その人の色で名前・アバター・段位が現れる」形が素直。
     ただし**対局開始が数秒遅くなる**ので、長さ（2〜3秒）と飛ばせるかはユーザーに確認してから。
  2. **「Googleログインでアカウント名を拾わないようにした方がいい？」** → 下記の回答を参照。
     結論としては**拾わない方がよい**（本名が既定の表示名になり得るため）。次に着手する。

### 2026-09-08（続き488）：Googleのアカウント情報を自動で使わない（名前は読み取り自体をやめ、写真の自動適用もやめた）／対戦開始前の「今回のメンバー」紹介

ユーザー判断2件——「Googleでのアカウント名を拾わないようにした方が良いですか？→着手してください」
「対戦直前のメンバー紹介はA案（2〜3秒しっかり見せる・タップで飛ばせる）で！ペットも入れれるといい」。

- **【Googleの情報】調べたら「名前」は既に使っていなかった（記録）**: `getGoogleDisplayName()` の
  呼び出しは**「初回ログインの案内を出すか」の判定1か所だけ**で、表示名には使われていなかった
  （続き…の「Googleの名前を一切出さない」で対応済み）。**実際に自動適用されていたのは
  プロフィール写真の方**——`openFirstLoginProfileModal` の冒頭で `setPlayerAvatar(seat, googleAvatarUrl)`
  を無条件に呼んでいた。Googleの写真は本人の顔写真であることが多く、しかも**戦績システム（公開サイト）の
  一覧にもそのまま出る**ので、名前と同じ理由で自動では入れない形に変えた（選択肢としては残す）。
  - **名前は読み取り自体をやめた**: `getGoogleDisplayName()` は常に null を返す。将来うっかり
    使われる余地を無くすため。**関数は消さない**——利用者の端末のキャッシュに古い import が
    生きていると「読み込みに失敗しました」で機能ごと起動しなくなる（続き423の実害）。
  - **初回案内の判定を「Googleの情報が取れたか」→「初めてのログインで、ゲストでない」に変えた**。
    名前を読まない以上あの判定は使えないうえ、**どのログイン方法でも名前を決める機会ができる**
    ＝本名が既定になる経路が無くなる。
  - モーダルの文面に「Googleの名前や写真が自動で使われることはありません」を1行足した（ja/en）。
  - **既存のテスター17人には影響しない**（この案内は `so7_user_profiles` の行がまだ無い＝
    初回ログインの時だけ出るため）。

- **【対戦開始前の「今回のメンバー」】**: `playMatchIntro()` を新設。参加している席を時計回り順に、
  **その人の駒の色**で縁取ったカードが1枚ずつ（220msずつ遅らせて）現れ、アバター・名前・
  「あなた／対戦相手」・**ペット**を並べる。2.8秒で自動的に片付き、**画面のどこかをタップすれば
  すぐ飛ばせる**（開いた直後の合成クリックで消えないよう250ms待ってからクリックを受ける）。
  - **中央のお知らせと喧嘩しないようにした**: `beginBoardAnimation()` で囲ってあるので、
    #266「画面の中央は一度に1つ」に自然に乗り、**紹介が終わるまで「◯◯のターンです」の告知が待つ**
    （続き471で告知も順番待ちの列に並べてある）。演出の自動解除は15秒なので2.8秒のこれは詰まらない。
  - 引き金は `turnPlayer` が入った瞬間（tutorial.js の `initTutorialAutoStart` と同じ）。
    物語チュートリアルの対戦と、自己対戦（両席が疑似CPU＝スモークテスト）では出さない。
  - ここでも **filter / backdrop-filter は使っていない**（塗りと transform だけ）。
  - **実測**: 4人戦で「今回のメンバー」＋4枚（名前・あなた/対戦相手・席ごとの色）＋案内文が出て、
    3.9秒後には自分で片付いている。ペットを選んでいる時だけペットが並ぶ（既定は「なし」）。
  - **書き間違いを1つ実測で見つけた**: `petSpriteSrc(petOpt)` と**引数の形を間違えて**渡していた
    （正しくは `petSpriteSrc(sprite, "front", "static")`）。画像が404になり、足しておいた
    `onerror` で消えて「ペット0」になったことで気づけた。**他の呼び出し箇所を見てから書くこと**。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件、構文チェック通過、
  CSSブレース平衡、`game.intro.*` の翻訳キーが ja/en とも同数。
  お知らせ（changelog.js）に ja/en とも3件追記。サーバー側の変更は無い＝SQL実行・再デプロイ不要。

- **申し送り**: メンバー紹介は**すべての対戦（CPU練習を含む）で出る**。ユーザーはA案だけを選び、
  「ランク戦だけ」（提案時のC案）は選ばなかったのでその通りにしてある。毎回のCPU練習で
  くどく感じるようなら、ランク戦・オンラインだけに絞るのは1行で切り替えられる。

### 2026-09-08（続き489）：「今回のメンバー」を画面の縦割りに作り直した／**置換の両端アンカーが別々の関数に当たり main.js を1712行消していた**（構文チェックを通していなかった）

ユーザー要望「もっとかっこよくしたいです。画面全体を４等分したような感じで！アバターも丸枠無くして
がっつりめいっぱい表示で！」。続き488の「横並びのカード」から、**画面を人数ぶんに縦割りし、
1人1枚の縦長パネルにアバターを枠なしで全面表示**する形へ作り直した。

- **見た目**: `.match-intro` を `display:flex`、`.match-intro-panel` を `flex:1 1 0` にしたので、
  2人戦なら2分割・3人なら3分割・4人なら4等分に**自動でなる**（人数ごとの分岐は書いていない）。
  アバターは `position:absolute; inset:0` ＋ `object-fit: cover` で面いっぱい（絵文字アバターは
  `font-size: 9rem` で中央）。上辺にその人の駒の色の帯、下から同じ色がにじむ。名前は下の幕の上。
  席の順に 220ms ずつ遅らせて下から迫り上がる。
- **常に最前面のボタンが1つ乗っていた**（実測で `#self-status-bug-report` が z-index 100055）。
  z-index の競り合いにすると壊れやすいので、紹介の間だけ `body.match-intro-active` を付けて
  CSSで引っ込める形にした。
- **スマホでの文字の小ささ**（申し送りにしていた件）もここで直した。ステージは端末に関係なく
  1600x900 の固定なので、rem を上げれば全端末で同じだけ大きくなる——名前 1.5→2rem、
  席の見出し 0.62→0.85rem、ペット 3.4→4.4rem、見出し 1.15→1.5rem。

- **【今回いちばんの事故】置換の両端アンカーが、別々の関数に当たっていた**。
  `const row = document.createElement("div");` 〜 `root.appendChild(row);` を丸ごと差し替える
  つもりだったが、**先頭のアンカーが `announceDrawTargetsForEffect` の中の同じ行に当たり、
  末尾のアンカーが `playMatchIntro` の行に当たった**ため、その**間にあった1712行が消えた**
  （関数にして数十個ぶん）。しかも「`node --check` 通過」と自分で書いていたが、**main.js は ESM
  なので `.js` のままでは正しく検査できず、実際には一度も通していなかった**。
  発覚したのはスクリーンショットを撮ろうとした時で、ブラウザが `SyntaxError: missing ) after
  argument list` を出していた（それも最初は自分のプローブのせいだと思い込んで遠回りした）。
  - **作法（今回から徹底する）**: ①**両端アンカーで挟む置換をしない**。どうしても範囲で消すなら、
    **行番号を出してから、範囲の中身に「その関数だけが持つ文字列」が在ることと、
    「他の関数の文字列」が無いことの両方を検査**してから切る（今回の splice スクリプトはそうした）。
    ②**ESMの構文チェックは `.mjs` にコピーしてから `node --check`**。`.js` のままだと import で
    落ちるか、素通りする。③**検証は必ず実際にブラウザで開く**。テストが通っても、画面が
    起動しないことがある（今回 `npm test` 58/58 も check-undeclared も素通りした——
    どちらも main.js を読み込まないため）。
  - 復旧は `git checkout -- src/main.js`（幸い未コミットだった）→ 行番号を検査する形で置換し直し。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  CSSブレース平衡（3254）、`node --check`（.mjs へコピーして）通過。
  **実際にブラウザで4人戦を開始して撮影**——PC(1280x760)・スマホ(932x430) とも
  **パネル4枚・例外0件・最前面ボタンの不透明度0**。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-08（続き490）：「今回のメンバー」のペットを大きく／この画面のレイアウトを管理者モードから調整できるようにした（プレビュー付き）

ユーザー要望「ペットももっと大きくしませんか？」「管理者モードにこの画面のレイアウト調整入れましょう」
「２人対戦、３人対戦の場合の表示も見たいです」。

- **ペット 4.4rem → 7rem**（実画面で 57px → 90px）。
- **決め打ちだった寸法を全部 CSS 変数にした**（`--match-intro-*`。既定値は style.css の `:root` に
  まとめてあり、admin.js の `default` と同じ数字にしておく決まり）——見せる長さ（秒）／ペットの
  大きさ・浮く高さ／名前／「あなた・対戦相手」／見出し／絵文字アバター／名前まわりを下端から
  上げる量／アバター画像の見せる位置（object-position。cover で切り取られる顔の高さを合わせる）／
  下の暗い幕の高さ、の10項目。
- **見せる長さは JS 側も CSS 変数から読む**（`MATCH_INTRO_MS` 定数を廃止して `matchIntroMs()`）。
  定数のままだとスライダーを動かしても長さだけ変わらない。
- **管理者モードにプレビュー付きのグループを追加**（オープニングの人魂と同じ `previewOnInteract` の形）。
  スライダーを触ると実際の紹介画面が出る——**対局中ならその面子で、そうでなければ4人ぶんの見本**。
  `registerMatchIntroPreviewHelper`（admin.js）に main.js が `previewMatchIntro` を注入する。
  既に出ている間は二重に開かない。

- **検証（実測）**: 管理者パネルを実際に開き、**`data-key` でペットのスライダーを掴んで input を発火**——
  紹介画面が出て（触る前は出ていない）、CSS変数が 7rem → 14rem に変わり、**画面のペットが実寸 179px に
  なる**（＝送る側だけでなく効いているところまで測った）。アバターは `object-fit: cover` で
  パネルを満たしていることも実測（698x786 の枠に 768x768 の画像）。
  **2人・3人・4人の3通りでブラウザから撮影**——パネル数 2/3/4、例外0件、常に最前面の不具合報告ボタンは
  不透明度0。`npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  CSSブレース平衡（3255）、`node --check`（.mjs へコピーして）を main.js・admin.js とも通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **検証のハマりどころ（記録）**: `page.evaluate` に渡す関数は**ブラウザの中で動く**ので、Node 側の
  変数（人数）はそのままでは見えない（`ReferenceError: SEATS is not defined`）。第2引数で渡すこと。
  もう1つ、`page.evaluate` の中では**動的 import が使えない**（Playwright が関数を包む都合で
  `SyntaxError: missing ) after argument list` になる）ので、`addScriptTag({ type: "module" })` で
  モジュールを読み込み、必要な関数を window に置いてから触る。

### 2026-09-08（続き491）：「対戦画面用」のペットの1枚絵（水彩画風）を、対戦開始前の紹介に使う

ユーザーが `画像素材/ペット/ペット_対戦画面用/` に8体ぶんの水彩画風イラスト（1254x1254・各1.3〜2MB）を
置いてくれた。**大きく見せる場所＝続き489〜490で作った「今回のメンバー」**で使うのが素直なので、そこに入れた。

- **配信用に変換した**（`assets/pets/portraits/<sprite>.webp`・8枚で計648KB）。手順は
  `tools/shrink-assets.mjs` と同じ考え方（Playwright の Chromium の canvas で再エンコード）:
  ①**透明な余白を切り詰めてから正方形に整える**——8体で余白の量がばらばら（中身が 798x1174 のものも
  1201x1220 のものもある）なので、そのまま並べると**同じ枠に入れても大きさが揃わない**。
  ②長辺512（`assets/pets` の上限と同じ）。1.3〜2MB → 61〜107KB。
  ・**画像はファイルサイズではなく「幅×高さ×4バイト」でメモリを食う**（#223 の教訓）。原寸のままだと
  　1枚 6.3MB で、4人ぶん並べば25MB——iPhone が落ちる側に寄る。
- **追従スプライトとは別物として持つ**（`petPortraitSrc(sprite)` を新設）。あちらは方向×モーションの
  小さなドット絵で、盤面を歩かせる用。1枚絵は正面の座り姿だけなので置き換えではなく**用途で使い分ける**。
- **素材が無い環境では従来のスプライトへ落とす**（読み込み失敗を1回だけ拾って差し替え、それも失敗したら
  要素ごと消す）。落ちた先はドット絵なので `.is-sprite` を付けて `image-rendering: pixelated` に戻す
  ——**1枚絵の方は滑らかに拡大させたい**ので、CSSの既定からは pixelated を外した。

- **検証（実測）**: 4人戦を実際に開始して紹介を撮影——`src` が `assets/pets/portraits/cubit.webp`・
  **実寸 512x512**・`image-rendering: auto`。**落ちる側も測った**——Playwright の route で
  1枚絵の配信だけを止めると、`assets/pets/cubit/cubit-front-static.webp` へ差し替わり、
  `is-sprite` が付いて `pixelated` に戻る（＝素材が無い端末でも壊れない）。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、CSSブレース平衡（3256）、
  `node --check`（.mjs へコピーして）main.js・pet-skins.js とも通過、例外0件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り**: 1枚絵は今のところ紹介だけで使っている。ペット選択・ショップ・戦績など
  「大きく見せる場所」が他にもあるので、必要なら同じ `petPortraitSrc()` を差し込むだけで足りる。

### 2026-09-08（続き492）：管理者モードに「押すだけ」の項目を作れるようにした（「今回のメンバー」のプレビューボタン）

ユーザー要望「実験用プレビュー表示ボタンも欲しいな」。続き490 のプレビューは **スライダーを触った時**
にしか出ないので、**値を変えずに今の見た目だけ確かめたい**という場面に応えられていなかった。

- **GROUPS の項目に `button: true` を足せるようにした**（`label` と `onClick` だけを持ち、
  スライダーを作らない）。既にある `previewButtonLabel`（スライダーの下に小さく付く試聴ボタン）とは別で、
  こちらは**単独の行として太いボタン1つ**になる。
- **`CONTROLS` から key の無い項目を外した**（`.filter((c) => c.key)`）。ここを忘れると
  リセット（`removeProperty(undefined)`）・スライダーの作り直し・そして**「出力をコピー」の
  テキストに `undefined: undefinedundefined;` の行が出る**。実際に出力テキストを読んで0件を確認した。
- 置いた場所は「✨ 演出」→「対戦開始前の『今回のメンバー』紹介（実験用プレビュー付き）」の**先頭**。

- **検証（実測）**: 管理者パネルを実際に開き、**ボタンをクリックして**——押す前は紹介が出ておらず、
  押すと**パネル4枚**の紹介が出る（ペットは `assets/pets/portraits/cubit.webp`）。
  出力テキストに `undefined` を含む行 **0件**。`npm test` 58/58 PASS、
  `node tools/check-undeclared.mjs` 0件（164ファイル）、`node --check`（.mjs へコピー）通過、例外0件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-08（続き493）：プレビューを「見ながら調整できる」ものにした（消えない・パネルより下・見本のペットを必ず出す）

ユーザー指摘「プレビューを見ながら調整したい。あとプレビューにペットがいない」。続き490〜492 の
プレビューは**本番と同じ出し方**だったので、実際には調整に使えなかった——3点とも直した。

1. **勝手に消えない**。本番は `matchIntroMs()`（既定2.8秒）で自動的に閉じるので、スライダーへ手を
   伸ばす前に消えていた。プレビューでは自動で閉じるタイマーを動かさない（閉じるのは押した時だけ）。
2. **管理者パネルより下に出す**（`.match-intro.is-preview { z-index: 1500 }`）。本番は 10670 で
   **パネル(2600)ごと覆っていた**ので、出ている間はスライダーに触れなかった。パネルの透明な背景(999)
   よりは上に置く——下にすると、紹介を押した時にその背景のクリック＝**パネルが閉じてしまう**。
3. **どの席にも見本のペットを出す**。ペットは「自分が選んでいる／相手の席の情報が届いている」時だけ
   出る作りで、**既定は「なし」**。つまり**ペットの大きさを調整したいのに1体も出ない**状態だった。
   プレビューでは `PET_OPTIONS` の絵を持つペットを席ごとに1体ずつ並べる。
- あわせて、プレビューでは `beginBoardAnimation()` を呼ばない。呼ぶと**開けている間ずっと対局の進行を
  待たせる**（#266 の中央の順番待ち。15秒で自動解除されるとはいえ、待たせる意味が無い）。
- 文言 `game.intro.previewHint` を ja/en 追加（本番の「画面をタップすると飛ばせます」とは別）。

- **検証（実測・管理者パネルのボタンを実クリック）**: `.is-preview` が付き z-index 1500、
  **ペット4体**（cubit / noxael / sept / rubel と席ごとに別）、パネルの上の点で最前面に来ているのは
  **パネル側**（＝触れる）。**実際にペットの大きさのスライダーを動かすと、プレビューのペットが
  90px → 154px に変わり**、その後も消えない（5.5秒後もまだ出ている）。例外0件。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  `node --check`（.mjs へコピー）通過、翻訳キー ja/en とも追加済み。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-08（続き494・私の回帰）：プレビューが見えない／不具合報告ボタンが消えた——固定の z-index で場所取りをしたのが誤りだった

ユーザー報告「プレビューが表示されない。オプションエリアの不具合報告ボタンも消えてる。なんで？」
（スクリーンショット付き）。**続き493 で私が入れた2つの変更が、そのまま2つの症状になっていた**。

- **原因①（見えない）**: 続き493 で「管理者パネル(2600)より下・その背景(999)より上」に置くつもりで
  プレビューを **z-index: 1500 の決め打ち**にした。ところが管理者パネルは**開いた場所によって
  自分から前へ出る**——`body.full-screen-page-active`（ホーム/マイページ/ランキング/図鑑）では
  **パネル2800・背景2799**、ショップでも2800/2799、タイトルでは50200/50150。つまりホーム画面から
  開くと、プレビューは**その透明な背景(2799)の下に潜って**まったく見えない。
  ・**実測で再現した**——ホーム画面で押すと `.match-intro` は在るのに、画面中央の最前面は
  　`admin-panel-backdrop` だった。
  ・**直し方**: プレビューは**本番と同じ高さ(10670)のまま**にして、出ている間だけ
  　`body.match-intro-preview-active #admin-panel { z-index: 10680 }` で**パネルの方を上へ出す**。
  　背景は下のままでよい（プレビューを押した時に閉じるのはプレビュー自身）。
- **原因②（ボタンが消えた）**: 紹介の間だけ付ける印 `match-intro-active` は、**常に最前面のUI
  （不具合報告ボタン、z-index 100055）を CSS で引っ込める**ためのもの。プレビューにもそのまま
  付けていたため、①で**見えないプレビューが開きっぱなし**＝印が外れず、**ボタンが消えたまま**に
  なっていた（本番は2.8秒で必ず閉じるので表に出ない）。
  ・**直し方**: プレビューでは**引っ込めない**（付ける印を分けた）。閉じ忘れても実害が出ない。

- **教訓**: **z-index を固定の数字で決め打ちしない**。相手（管理者パネル）は場面ごとに
  2600 / 2800 / 50200 と**自分から動く**ので、「この数字なら下に入るはず」は必ずどこかで破れる。
  上下関係を作りたいなら、**動かす側（今回はパネル）に印を付けて相対的に決める**。
  もう1つ——**「これを消す処理が必ず走る」前提でUIを隠さない**。隠す印は、隠したものが
  見えないまま残る事故に直結する（今回まさにそれ）。

- **検証（実測・ホーム画面から実クリック）**: 修正後——プレビューの z-index **10670**、
  画面中央の最前面が **プレビュー自身**、パネルの上の点では **パネルが最前面（＝触れる）**、
  **不具合報告ボタンの不透明度 1**（＝見える）。**本番の紹介は従来どおり**——パネル4枚・
  不具合報告ボタンの不透明度 0・2.8秒で自動的に閉じる。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  CSSブレース平衡（3258）、`node --check` 通過、例外0件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **ユーザーへの案内**: 既に消えてしまっている画面は、**アプリを開き直せば戻る**（印は
  読み込み直しで消えるため）。

### 2026-09-08（続き495）：豆知識が読めない（暗い画面にライト配色の文字色を当てていた）／アバターの見せる位置が動かない（切り取られる向きが逆だった）／紹介に段位バッジ

ユーザー報告3件。①②はどちらも**私が入れた指定が、実際の見え方と噛み合っていなかった**もの。

- **①「豆知識の文字が見にくいです。コントラストの問題？」（原因確定・修正）**: 続き487 で
  `body.theme-light .waiting-tips-body { color: #3a3326 }` を足していたが、**待機画面
  (`#ranked-waiting`) は配色設定に関わらず常に暗い**（背景は固定の暗い放射グラデーションで、
  ライト用の上書きが1つも無い）。つまりライト配色の人は**暗い地に暗い字**で読めなかった。
  ライト用の上書きを丸ごと削除（見出しの `#8a6d1f` も同じ）。ついでに同じ面にある
  `.ranked-waiting-notify-text` の `#3a3326` も同じ理由で削除した。
  ・**実測**: 既定／theme-light／theme-light-ingame の3通りとも、本文 `rgba(241,245,249,0.95)`・
  　見出し `rgba(250,204,21,0.95)`（＝明るいまま）。
  ・**教訓**: 「配色設定がライトだから文字を暗く」ではなく、**その文字が乗る面が明るいかどうか**で
  　決める。全画面の覆い（待機画面・演出の幕）は配色に追従しないものが多い。

- **②「アバター画像の見せる位置が変わりません」（原因確定・修正）**: 続き490 で
  `object-position: 50% var(--…-pos-y)` として**上下**を動かす作りにしたが、パネルは**縦長**、
  アバターの絵は**正方形**なので、`object-fit: cover` では**左右がはみ出して切り取られ、上下は
  1pxも切り取られない**——つまり上下をどう動かしても絶対に変わらない。
  ・**実測**: 4人戦のパネル 319x720 に 768x768 の絵 → 720x720 に拡大され、**横に401pxはみ出し／
  　縦は0**。動かせる向きは左右だけ。
  ・**修正**: 変数を `--match-intro-portrait-pos-x` に替え、`object-position: var(--x) 50%` に。
  　管理者モードのラベルも「0=左・100=右」へ。**既定は 50% のまま**（ユーザー指示
  　「いじっちゃったけどデフォのままにして」）。
  ・**A/Bで実測**: 50% → 0% にすると**パネルの絵そのものが変わる**（同じ場所を撮った画像の
  　バイト列が変化）。数値だけでなく**見た目が変わること**を測った。

- **③「ランクバッチは？」（できる範囲で実装＋できない範囲を明示）**: ランク戦のときだけ、
  名前の上に段位バッジ＋段位名を出す（`buildRankBadgeImage` を流用。大きさは管理者モードから）。
  ・**相手の段位は今のデータでは出せない**。席ごとに同期しているのは**名前・アバター・駒スキン・
  　ペットだけ**（online.js の roster）で、段位を返すのは `so7_ranked_get_self`（＝自分の分）しか
  　無い。全員ぶんを出すには「この対戦の参加者の段位をまとめて返す」RPC をサーバー側に足す必要が
  　あり、**SQLの追加＝ユーザーの手動実行**になるので、勝手にやらずユーザーへ提案した。
  ・段位の取得は通信なので、**先に場所だけ作って間に合ったら差し替える**（2.8秒で閉じるので、
  　間に合わなければ何も出さない＝レイアウトは崩れない）。
  ・プレビューでは席ごとに違う段位の見本を出す（大きさの当たりを付けるため）。

- **検証**: 上記の実測に加えて `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件
  （164ファイル）、CSSブレース平衡（3258）、`node --check` 通過、**管理者スライダーの既定値12項目が
  style.css の `:root` と一致**、例外0件。プレビューで段位バッジ4つ（ブロンズ〜プラチナ）が
  実際に読み込まれることも確認。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-08（続き496）：相手の段位も紹介に出した——**SQLの追加は要らなかった**（段位は元から誰でも読める設計だった）

ユーザー「やりましょう！」（続き495 で「相手の段位を出すにはサーバー側の追加が必要」と伝えた件）。
**着手して最初に分かったのは、追加が要らないということ**だった。

- **`so7_ranked_players` は作った時から SELECT が全員に許可されている**——
  supabase_setup_so7.sql に「ランクは公開情報（相手のランク表示・ランキング）なのでSELECTは全員可。
  書き込みは…RPC経由のみ」と明記されており、実際に戦績システム（BATTLE-log）も
  `from('so7_ranked_players').select('user_id, rank, …')` でそのまま読んでいる。
  ＝**新しいRPCもSQLの手動実行も不要**で、クライアントから直接読めばよかった。
  ・続き495 で「段位を返すのは so7_ranked_get_self（自分の分）しか無い」と書いたのは、
  　**RPC だけを見てテーブルのポリシーを見ていなかった**ための早とちり。
  　**「無い」と言う前に、テーブルのRLSと、同じデータを既に読んでいる場所を確かめる。**
- 実装は `fetchRanksForUsers(userIds)`（online.js）＝1回のクエリで参加者ぶんをまとめて取る。
  席→userId は既にある `getSyncedIdentity(seat).userId`（ロスター）から引ける。
- **別シーズンの記録は出さない**。`so7_ranked_players` は1人1行で、今シーズンまだ打っていない人の行は
  **前シーズンの段位のまま**残る（次に打った時に `so7_ranked_apply_delta` が繰り越す）。そのまま出すと
  古い段位を今の段位として見せてしまうので、自分の `season_id` と一致する人だけ出す。
- 自分の行は初めてのランク戦だとまだ無いことがあるので（作るのは `so7_ranked_get_self`）、
  同時に呼んでいるその結果で埋める。

- **検証（本物のオンラインのランク部屋・2クライアント）**: ゲストで2人ログイン→段位の行を作り→
  **相手のIDでも段位が読める**ことを確認（自分・相手とも rank 0 / season 2026-09）→
  ランク部屋を作って対戦開始→**両方の画面で紹介にバッジが2つ**（「ブロンズ」「ブロンズ」＝
  自分と相手）。例外0件。`npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件、
  `node --check`（.mjs へコピー）main.js・online.js とも通過。
  **サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。**

### 2026-09-08（続き497）：「CPUのターンが毎回10秒ほど足踏みする」の原因を実測で確定して修正／水彩画のペットをペット選択・ショップ・マイページの「背面の半透明」にも／開発にかかった時間の資料

ユーザーからの3件（①開発時間の資料 ②水彩画ペットを他の画面にも ③CPUの10秒の足踏み）。

- **【③・原因確定・修正】CPUのターンが毎回10秒止まっていた**。続き483 で「`diag-timeout-latch-retry`
  {stuckMs: 約10160} がほぼ毎ターン出ている」と申し送りにしていた件。**推測で直さず、実際にCPU戦を
  走らせてログを取った**——4人戦の自己対戦300秒で1件掴まえ、その前後にこう並んでいた:
  `diag-pseudo-cpu lockAutoPlay-check` → **同じ瞬間に** `diag-lock-click-skip
  {reason:"already-locked-this-phase"}` → **その10.3秒後**に `diag-timeout-latch-retry`。
  - **仕組み**: ロックの自動処理は「ロックできる札を選ぶ → `performLockPhaseClick` を呼ぶ →
    **await せずに true を返す**」形。ところが#266/#267 でフェイズの切り替えを演出・お知らせの後まで
    待たせているため、**ロックし終わってもフェイズは数秒 "lock" のまま残る**。その窓でもう一度
    ここへ来ると、`performLockPhaseClick` は `already-locked-this-phase` で正しく断るのに、
    **呼び出し側には true が返る**。turn-timer.js はそれを「1手打った」と見なしてラッチ
    （`timedOutAutoActionFired`）を立てるので、**持ち時間は動かず、次の手も出さない**——
    10秒の安全網（`STUCK_RETRY_MS`）が下りるまで**完全に停止**する。これが足踏みの正体。
  - **修正**: ロックの自動処理に入る前に `hasPlacedNewLockThisPhase(player)` /
    `hasDrawnMyDeckThisPhase()` を見て、**済んでいれば選ばない**。落ちる先は既存の
    「ロック/ハンドはスキップ」＝`"skip"` を返す経路で、そこは持ち時間を必ず更新するので足踏みしない。
    ついでに #334 で見えていた `diag-lock-click-skip` の連発（6秒で約30件）も出なくなる。
  - **教訓**: **「呼んだ」ことを「1手打った」として報告しない**。非同期の実行を fire-and-forget で
    呼ぶ関数が true を返すなら、その true は「受け付けられた」ではなく「呼んだだけ」でしかない。
    呼び出し側がそれを実行の証拠として扱うと、断られた時に誰も気づけない。
  - **A/Bで実測（決定的）**: ロックフェイズ中に（フェイズを進めずに）1枚ロック済みの状態を作り、
    `performPriorityTimeoutAutoAction()` の返り値を見た——**修正前は `true` ＋
    `diag-lock-click-skip{already-locked-this-phase}`／修正後は `"skip"` でログも出ない**。
    - **測定の記録**: 最初のA/B（中央をふさいで自己対戦150秒）は**修正前後とも0件**で、
      差が測れていなかった（150秒でロックが1回しか起きず、そもそも経路を通っていない）。
      **両方が同じ結果になった時は「差が無い」ではなく「測れていない」を先に疑う**（続き463と同じ罠）。
      決定打は「進行を止めて時間を固定し、狙った状態を手で作ってから1回だけ呼ぶ」形にしたこと。

- **【②】水彩画の1枚絵（続き491の `assets/pets/portraits/`）を、3か所の「背面の半透明」に流用した**
  （ユーザー提案「背面の半透明の部分に使うのはどう？」）。**どこも既にあった半透明の飾りの
  中身を差し替える形**なので、新しい層は増えていない。
  ・**ショップ**: `.shop-item-bg`（商品画像を拡大して敷く既存の背景）を、ペット商品だけ1枚絵にする。
  　`background-image` は読み込みの失敗を教えてくれないので、**先に `Image` で確かめてから差し替える**
  　＝素材が無い環境では今までどおりスプライトのまま。
  ・**ペット選択**: マスの背面に薄く敷く（`.pet-picker-bg`、`<img>` なので読み込み失敗で自分から消える）。
  ・**マイページ**: 巨大な半透明アバター（`.my-page-bg-avatar`）と並ぶ飾りとして、
  　装備中のペットの1枚絵を置く（`.my-page-bg-pet` / `PROFILE_LAYOUT["pet-bg"]`）。**ペットが
  　「なし」なら置かない**。avatar-bg と同じく「わざと画面からはみ出す飾り」なので、
  　クリック透過と位置合わせの範囲計算からの除外も同じ扱いにする（**この2か所を忘れると
  　『見えない枠』と全体のズレになる**——profile-layout-editor.js のコメント参照）。
  ・**実測**: ペット選択8体とも1枚絵（512x512・不透明度0.3）／ショップのペット7件とも `is-portrait`
  　（0.32）／マイページは装備時のみ1個・未装備0個。スクリーンショットでも3画面とも確認。例外0件。

- **【①】開発にかかった時間の資料**（Artifact として公開・URLはユーザーへ）。保存記録1,185件から
  「何分あいたら休憩と見なすか」を3通りで計算: **30分区切り388時間／60分428時間／120分513時間**
  ＝**およそ390〜510時間**。53日（2026-07-17〜09-08、休んだのは8/26の1日だけ）、1日の作業の幅は
  中央値15.6時間・最長20.6時間。累計の書き換えは追加201,357行・削除62,858行。
  時刻の分布は23時が最多（94回）。**この数字はアプリを書いていた時間の下限**で、ルール検討・
  素材準備・テスターとのやりとり・実機での試遊は含まれない旨も資料に明記した。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3265）、`node --check`（.mjs へコピーして）
  変更した5ファイルとも通過、お知らせ ja/en とも件数一致（64回・不一致0）。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **道具**: 置換ヘルパーを `scratchpad/rep2.mjs` に更新した（**行末を決め打ちせず LF → CRLF の順に
  試す**。style.css・board-3d.js は混在している＝続き471/481）。一致が1件でなければ例外は従来どおり。

### 2026-09-08（続き498）：「今回のメンバー」に位置調整を追加（管理者モード）

ユーザー要望「対戦開始前の『今回のメンバー』紹介（実験用プレビュー付き）で位置調整も入れたいです」。
続き490 で入れたのは**大きさ**の調整だけで、位置は「名前まわりを下端から上げる量」しか無かった。

- **足した6本**（既定はすべて 0 ＝ 今までと同じ見た目）: 見出しの上下（`--match-intro-title-pos-y`）／
  名前まわりの左右（`--match-intro-info-pos-x`。上下は既存の「下端から上げる量」が担当）／
  ペットの左右・上下（`--match-intro-pet-pos-x` / `-pos-y`）／段位バッジの左右・上下
  （`--match-intro-rank-pos-x` / `-pos-y`）。

- **ハマりどころ（今回の肝）**: 動かしたい3つ（見出し・名前まわり・ペット）は**どれも既に
  animation を持っていて、その animation が transform を最後まで保持する**——見出しと名前まわりは
  `fill-mode: both` で最終キーフレームの `transform: none` が残り続け、ペットは `infinite` で
  毎周 transform を書き換える。**素の `transform: translate(...)` を足しても100%上書きされて効かない**
  ので、**キーフレーム側に位置の変数を織り込んだ**（ペットのふわふわは `calc(pos-y - lift)` で
  位置を保ったまま浮く）。段位バッジだけは animation を持たないので素の transform でよい。
  ・続き435（手札の差し込み）で踏んだ「transform を持つ要素にキーフレームで足す時は元の
  　持ち上げも含める」と同じ形。**位置調整を足す前に、その要素が animation を持っていないか見る。**

- **検証（実測・管理者パネルの本物のスライダーを操作して、要素の座標の差を測った）**:
  ペット上下 y559→512／ペット左右 x115→153／名前まわり左右 x0→−38／見出し上下 y20→71／
  段位バッジ上下 y491→452 ＝**6本とも実際に動く**。ペットのふわふわも止まっていない
  （測るたびに y が数px揺れている）。出力テキストの `undefined` 行 0、例外0件。
  **管理者スライダーの既定値18本が style.css の `:root` と完全一致**（照合スクリプトで確認）。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  CSSブレース平衡（3265）、`node --check`（.mjs へコピー）通過。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-08（続き499）：「今回のメンバー」の調整値を既定として焼き込み／上限に張り付いていたスライダーを広げた（ランクバッジをもっと下げたい）

ユーザーから「出力をコピー」の内容＋「ランクバッチをもっと下げたいです」。

- **まず差分を取ってから焼き込んだ**（`scratchpad/diff499.mjs`）。出力469項目のうち、いまの
  `:root` / admin の既定と**実際に違うのは7項目だけ**で、全部 `--match-intro-*` だった——
  ペットの大きさ 7→**18rem**・位置 **-4.7 / 0.7rem**、段位バッジ 4→**9.1rem**・段位名 0.8→**1.3rem**・
  位置 **6.6 / 8rem**。style.css の `:root` と admin.js の `default` の**両方**へ入れた。
  ・**残りの21件は差分ではなかった**——単位の書き方の違い（css は `80`、admin は `80%`）や、
  　css 側が `var(--…-phone, 既定)` の形をしているもの。**見た目の値としては一致している**。

- **【本題】上限に張り付いていた**: 段位バッジの上下は **8rem＝スライダーの上限**、ペットの大きさも
  **18rem＝上限**だった。つまり「もっと下げたい」は**そもそも動かせない**状態。範囲を広げた——
  段位バッジの上下 ±8 → **−12〜24rem**／左右 ±8 → ±16rem／大きさ 10 → 20rem／段位名 2.5 → 4rem、
  ペットの大きさ 18 → 30rem／位置 ±8 → ±16rem。
  ・**気づき方**: 焼き込む前に「出力の値がスライダーの min/max と一致していないか」を見る。
  　一致していたら**そこで止められていた可能性が高い**＝範囲を広げるべきサイン。

- **検証（実測）**: 管理者パネルを実際に開いてプレビューを出し、既定のまま
  ペット18rem・段位バッジ9.1rem・上下8rem が出ることを確認。そのうえで**上下のスライダーを
  新しい上限(24rem)まで動かすと、段位バッジの画面上の位置が y378 → y582（204px下）へ実際に動いた**
  （修正前は8remで頭打ち）。スクリーンショットでも4人ぶんのパネルを確認。例外0件。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  CSSブレース平衡（3265）、`node --check`（.mjs へコピー）通過、
  **管理者スライダーの既定値18本が style.css の `:root` と完全一致**。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り（今回見つけたが直していない・別件）**: 差分を取る過程で、**style.css の既定と
  admin.js の `default` が食い違っている項目が2つ**見つかった——`--card-preview-size`
  （css 20rem / admin 32rem）と `--move-blink-duration`（css 3 / admin 7）。つまりこの2つは
  **管理者モードの「リセット」を押すと配信されている見た目とは違う値に戻る**。実害は小さいが、
  次に触る時にどちらへ揃えるかをユーザーに確認すること（勝手に変えるとどちらかの見た目が変わる）。

### 2026-09-08（続き500）：「今回のメンバー」の調整値を焼き込み／段位バッジをペットより手前に

ユーザーから「出力をコピー」の内容＋「ランクバッチのがペットより手前に描画されるようにして！」。

- **差分を先に取ってから焼き込んだ**（`scratchpad/diff500.mjs`）。469項目のうち**実際に違うのは2つだけ**——
  `--match-intro-rank-size` 9.1→**11.2rem**、`--match-intro-rank-pos-y` 8→**18.2rem**。`:root` と
  admin.js の `default` の両方へ入れた（照合スクリプトで18件・不一致0を確認）。
  ・残り21件は続き499と同じ「単位の書き方の違い」「css 側が `var(--…-phone, 既定)` の形」なので値としては一致。
  ・**既知の食い違い2件はそのまま**（`--card-preview-size` css 20rem / admin 32rem、`--move-blink-duration` css 3 / admin 7）。
    どちらへ揃えるかはユーザー確認待ち（続き499の申し送り）。

- **段位バッジがペットの後ろに隠れていた**: `.match-intro-info` の DOM 順が **段位バッジ → ペット** なので、
  後に来るペットが上に描かれる。**並び順は変えず**（変えると位置調整の変数の意味が入れ替わる）、
  `.match-intro-rank { position: relative; z-index: 2 }` / `.match-intro-pet { z-index: 1 }` で
  **重なり順だけを明示**した。どちらも既に transform（＝独自のスタッキング文脈）を持つので、
  z-index を与えるだけで確実に順番が決まる。

- **検証（実測・A/B）**: 管理者パネルのプレビューを実クリックで開き、**4パネルとも段位バッジとペットが重なっている**
  ことを確かめたうえで、重なりの中心を `elementsFromPoint` で見た——**4つとも手前は段位バッジ**（rankZ 2 / petZ 1）。
  同じ画面で **z-index を外すと「ペット」に戻る**＝この指定が効いていることまで測った。
  焼き込んだ既定値もそのまま出る（段位バッジ 11.2rem / 上下 18.2rem / ペット 18rem）。例外0件。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、CSSブレース平衡（3265）、
  `node --check`（.mjs へコピー）通過、お知らせ ja/en とも件数一致（65回・不一致0）。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-08（続き501）：オンライン4人戦の「食い違い」は**私の誤報だった**（測定が追いつかなかっただけ）／テスト側の欠陥を3つ塞いだ

ユーザー指示「オンライン対戦を回して」→2人戦 PASS →「今度は4人対戦」。**4人戦は記録上これが初回**
（過去21回の通しテストは全部2人戦）。1回目・2回目とも FAIL し、私は一度**「4人オンラインに食い違いの
不具合がある」と報告した。これは誤りだった**——診断を入れて測り直したら、原因は1台のPCで4画面を
動かした負荷で、アプリ側は無実だった。**推測で報告したものを、計測で自分から訂正した形**。

- **決め手になった数字**: 取り直し（`fetchAndHydrate`）1回にかかる時間——**2人戦の正常時 140〜300ms に対し、
  4人戦では 14,700〜23,900ms（50〜170倍）**。しかも4つとも `ok`（＝通信は壊れていない）。さらに
  **取り直している最中にもゲームが進んでいた**（p1 T2→T3、p4 T5→T6）。1人測るのに20秒かかる一方で
  ゲームは進み続けるので、**4人の絵が同じ瞬間を指すことが原理的に有り得ない**。食い違いの中身も
  全部「片方が数手ぶん遅れている」形（札が手札にある/もうロック済み、駒が1マス手前）だった。

- **【欠陥①】テストが「取り直しの結果」を測っていなかった**（今回の誤報の直接の原因）。失敗しても
  黙って次へ進む作りで、**「取り直しても直らない」のか「そもそも取り直せていない」のかが区別できない**。
  成功/失敗（理由つき）・所要ミリ秒・前後で状態が変わったか、の3点を必ず記録するようにした。
  ・**この診断器そのものを先に検証した**——食い違い判定を一時的に0秒にして2人戦を回し、記録が実際に
  　出ること（と正常時の基準値 140〜300ms）を確かめてから戻した。**45分待って「診断が出ていません
  　でした」を避けるため**。測定器を信用する前に測定器を測る。

- **【欠陥②】ゲームが進み続けている間の食い違いを「壊れた」と判定していた**。**「もう誰も動いて
  いないのに揃わない」時だけ本物の食い違いとする**よう直した（取り直しの前後でターン番号・行動数が
  進んでいたら、それは遅れであって壊れではない）。2人戦は取り直しが0.2秒で終わるためこの分岐に
  そもそも入らない＝**検出力は落ちていない**。あわせて**測定が追いつかず判定を見送った回数を最後に
  必ず報告**する（黙って見逃すと「4人戦は問題なし」という誤った安心になる）。

- **【欠陥③・これは本物のテストの不具合】簡易サーバーが EMFILE で落ちた**。1リクエストごとに
  `fs.createReadStream` でファイルを開く作りで、**4画面ぶんを20分以上配り続けて同時に開けるファイル数の
  上限に達し、テストごと停止**した（実測: `assets/pets/.../rubel-front-jump.webp` の読み込みで）。
  中身を一度メモリに読んで使い回す形にした（鍵に更新時刻と大きさを含めるので、テスト中に書き換えても
  古い中身は返さない＝`no-store` の意図は保つ）。**`test/smoke.mjs` にも同じ書き方があったので一緒に直した**。

- **【欠陥④】決着まで（--full）の制限時間が人数に比例していなかった**（900秒の固定）。すぐ上のコメントに
  「3-4人は1局が長いので制限時間を人数に比例」と**書いてあるのに、片方の枝にしか入っていなかった**。
  実測で4人戦は1ターンおよそ32秒＝15分では28ターンまでしか進めず、**まだ元気に動いている最中に時間切れ**
  になっていた（ログの末尾は到達効果を処理中で、停止ではない）。`1800000 * (人数/2)`＝2人30分・4人60分に。
  止まった時は無活動・同一ターンの見張りが先に捕まえるので、上限を延ばしても検出力は落ちない（続き439/480と同じ判断）。

- **結論と運用（ユーザーへ伝達済み）**: **オンラインの4人通しは、このPC1台では測れない**（実機は4台に
  分散するので負荷が別物＝アプリの問題ではない）。4人の検証は**ローカル4人戦**（ブラウザ1つで4席を回す
  ＝負荷1/4、ゲームのロジックは同じ経路）で行い、**オンライン特有の同期は2人戦で見る**、と分けた。

- **検証（実測）**: `node test/online-smoke.mjs 2 --full --isolated` **PASS（turn 23 で決着）**／
  `node test/smoke.mjs 4` **PASS**／`node test/smoke.mjs 4 --full` **PASS（turn 22 で決着）**
  ——**4人戦を決着まで通したのは初めて**。EMFILE の修正は、この15分超の連続実行で再発しないことを確認。
  変更したのは `test/` の2ファイルだけ＝**配信物（src/）に変更は無い**ので changelog は更新していない。
  サーバー側（Supabase）の変更も無い＝SQLの実行・Edge Function の再デプロイは不要。

- **教訓（今回の本体）**: **測れていない結果を「不具合」として報告しない。** 2回続けて FAIL した時点で
  私は原因を推測して報告してしまった。正しい順番は「**まず測定が成立しているかを確かめる**」で、
  それをやったら答えは1回で出た。続き463の「両方が同じ結果になった時は差が無いではなく測れていないを
  疑う」の親戚で、今回は**失敗した時こそ測定を疑う**という形だった。

### 2026-09-09（続き502）：BGM軽量化の調査で**「起動時にBGMは落ちてこない」ことが分かり、自分の説明が誤りだったと判明**／起動時の単体最大 home-bg.png(2.1MB) を WebP 化

ユーザーの「BGMを軽量化したい」から始めたが、**測ってみたら前提が違っていた**。

- **【自分の誤りの訂正】** 私は「ホーム画面の曲が7.2MB→1.3MBになるので**アプリを開いた時の待ち時間が変わる**」と
  説明した。**これは誤りだった**。実測（Playwrightで全リクエストを記録）すると、**起動時に落ちてくる音声は
  0バイト**——BGMは `new Audio()` を鳴らす直前に作る作りで、service worker もプッシュ通知専用で
  キャッシュ機能を持たない（`fetch` ハンドラも `caches` も無い）。**BGMの軽量化は通信量には効くが、
  起動の速さには関係しない。**
  - **教訓**: 「重いから遅いはず」は、**いつ落ちてくるかを測るまで言ってはいけない**。続き501で
    「測れていない結果を不具合として報告しない」と書いた翌日に、今度は**測らずに効果を約束していた**。

- **実測した起動時の内訳（初回・キャッシュ無し）**: 合計 **10.12MB**——プログラム(src) 5.34MB ／
  画像 3.15MB ／ three.js 0.66MB ／ カード画像 0.26MB ／ **音声 0.00MB**。
  - **測り方の注意**: 最初 `Cache-Control: no-store` の簡易サーバーで測ったら 13.85MB になり、
    `rubel-right-static.webp` が**8回**落ちてきていた。これは**テスト側の作り込みの誤り**で、
    本番はキャッシュが効くので同じファイルは1回しか落ちない。**ユニークなファイルの合計**で
    測り直して 10.12MB。**測定器の設定が結果を作っていないかを先に疑うこと。**

- **単体最大は `assets/home-bg.png` の 2104KB**。他の画像は既に WebP 化済みで、**これだけ PNG のまま
  残っていた**。quality 92 の WebP へ変換して **230KB（−89%）**。
  - **品質は目視で確認した**: 元・q92・q85 を同じ場所で 2〜3倍に拡大して並べ、**紙の質感と金線に
    差が分からない**ことを確認（q85=90KB も悪くないが、細い金線が多い絵なので余裕を取って q92）。
    可逆(lossless)は 1523KB にしかならず、この絵柄では割に合わない。
  - **元の PNG は消していない**。利用者の端末に**古い CSS がキャッシュされている**と背景が出なく
    なるため（続き423 の「公開済みファイルを消さない」と同じ考え方）。配信量は増えない。
  - 参照は2か所だけ（`src/style.css` の `background: url(...)` と `src/home-screen.js` の先読み）。
  - **実測（修正後）**: 起動時 **7.77MB**、`home-bg.webp` 230KB が落ち、`home-bg.png` は落ちない、
    404 なし、例外0件、`.home-screen` の背景が `assets/home-bg.webp` に解決され 1672x941 でデコード成功。

- **BGM側の調査結果（実装はまだ・ユーザー判断待ち）**:
  - 音声14MBのうち**96%がBGM4曲**。ビットレートが188〜256kbpsと過剰（victory-bgm は47秒で256kbps）。
  - **AAC(.m4a) 96kbps** へ変換して長さそのままなら 13.3MB→7.2MB。
    waiting-bgm を**ループに切り詰めれば 4.6MB**（−66%）。
  - **ループ点は推測せず測って決めた**: 1kHz に落とした波形の RMS で構造を出し（0:20〜4:40が本編）、
    8kHz・24バンドの周波数指紋の余弦類似度で継ぎ目を採点。**開始位置を変えても「113秒」が
    繰り返し最良**に出た＝**曲そのものが約113秒周期**。`1:08〜3:01` を採用し、**末尾3秒を先頭へ
    クロスフェード**して継ぎ目を消した（`acrossfade` で head/body/tail を組む）。ユーザー確認済み
    「つなぎ目気になりません」。
  - **「最初だけ気になる」への答え（未実装）**: このループは0秒目が**曲の途中の音・最大音量**から
    始まる。対策は**再生開始時のフェードイン**（アプリ側。既にフェードアウトの仕組みがある）。
    **ファイルに焼き込んではいけない**——ループのたびにフェードインして継ぎ目が復活する。
    「イントロ＋ループの2ファイル」は王道だが、繋ぎ目を無くすには Web Audio で全体を PCM 展開する
    必要があり **113秒＝約40MB**。#223 の端末メモリ問題があるので採らない。
    静かな箇所（0:51、音量0.25）から始める案も測ったが、**継ぎ目の一致度が 0.971→0.61 に落ちる**。
    **継ぎ目は113秒ごとに永久に聞こえ、出だしは1回だけ**なので 1:08 を維持する。

- **ついでに見つけた別件（未対応・ユーザーへ報告済み）**: お知らせ（changelog.js）の
  **2026-07-30〜08-28 の13件に英語が1つも無い**（`itemsEn`/`devItemsEn` が0）。英語併記のルールが
  できたのが 08-29 なので、それ以前の分が翻訳されないまま残っている。一般向けだけで68項目。
  **私の変更による回帰ではない**（今回の差分は15行の追加・削除0）。

- **ffmpeg をこのPCへ導入した**（`winget install Gyan.FFmpeg`、9.0.1）。以後、音声・画像の変換に使える。
  パスは `/c/Users/user/AppData/Local/Microsoft/WinGet/Links`（新しいシェルでは PATH に足す必要がある）。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  CSSブレース平衡（3265）、起動の実測は上記のとおり、例外0件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-09（続き503）：BGMをAAC(96kbps)へ・待機BGMと新曲をループ素材に切り出し（[tools/shrink-audio.mjs](tools/shrink-audio.mjs) 新規）／再生開始時のフェードインを追加

続き502 の調査を受けて実装した。ユーザー承認「やっちゃってください」。

- **変換**: mp3(188〜256kbps) → **AAC(.m4a) 96kbps**。**15.68MB → 5.59MB**（新曲を1曲足してなお、
  従来の13.3MBより減った）。AAC を選んだのは、mp3 と同じ音質を半分で出せて **iPhone を含む全ブラウザで
  確実に鳴る**から。Opus はさらに小さいが古い iOS で再生できない端末が残るので採らない。
  - waiting 7.22→**1.32**MB（113秒ループ）／blue 2.35→**1.05**MB（90秒ループ・新規）／
    game 3.22→1.68MB／opening 1.44→0.99MB／victory 1.44→0.55MB。
  - **元の mp3 は消していない**。利用者の端末に古い `sound.js` がキャッシュされていると 404 で
    BGM が鳴らなくなるため（続き423・home-bg.png と同じ判断）。配信量は増えない。

- **ループ素材の作り方（`tools/shrink-audio.mjs` に実装＋理由をファイル冒頭に明記）**:
  Suno等が出すのは「イントロ→展開→アウトロ」の“曲”で、そのまま `loop=true` にすると2周目の頭で
  必ず途切れる。**末尾 X 秒を先頭へ重ねる**——head=[S,S+X] / body=[S+X,S+L] / tail=[S+L,S+L+X] を作り、
  `acrossfade(tail→head) + body` を出力（長さはちょうど L 秒）。body の終わりが tail に自然につながり、
  その tail が head へ溶けるので繋ぎ目が消える。
  - **ループ点は耳ではなく測って決めた**: 1kHz に落とした波形の RMS で構造（イントロ/本編/アウトロ）を
    出し、8kHz・24バンドの周波数指紋の**余弦類似度**で「開始位置とその L 秒後がどれだけ似ているか」を採点。
    **待機BGM＝開始位置を変えても 113秒 が繰り返し最良（0.971）＝曲自体が約113秒周期**。
    本編は 0:20〜4:40 なので `1:08〜3:01` を採用。
    **青の国ドゥエン＝約75秒周期**（候補が軒並み0.99超）。75秒だと対戦中に繰り返しが目立つので、
    ほぼ同スコア（0.9931）の **0:03から90秒** を採った。
  - ユーザー確認済み「つなぎ目気になりません」。

- **フェードイン（`src/sound.js`）**: ユーザー報告「ホームに入って流れ始めるときだけ最初が気になる」。
  ループ素材は0秒目が**曲の途中の音・最大音量**から始まるので、無音から飛び込むと唐突に聞こえる。
  - **ファイルに焼き込んではいけない**——ループのたびにフェードインして、消した繋ぎ目が復活する。
    **再生開始時にアプリ側でかける**（`fadeInBgm`、1.4秒）。この注意は tools 側の冒頭にも書いた。
  - **タイマーはそのトラックのフェードアウトと共有する**。共有しないと、フェードイン中に `stop*Bgm` が
    呼ばれた時に打ち消し合えず、音量を下げる処理と上げる処理が同時に走り続ける。
  - **目標音量は毎回読み直す**（`getTarget`）。固定値で捕まえると、フェード中に音量スライダーを
    動かしても終了時に古い値へ巻き戻る（既存のフェードアウトのコメントが「フェード中の音量変更は
    飲まれるが停止するだけなので実害なし」と書いているが、フェードインでは実害になる）。
  - **勝利BGMだけフェードインしない**（一瞬で決まる演出なので立ち上がりを鈍らせない）。
  - **待機BGMは「既に鳴っていれば鳴らし直さない」既存のガードがある**ので、その時はフェードイン
    しない（途中で音量を0へ落として上げ直すと、聴いている側には一瞬途切れたようにしか聞こえない）。

- **【未接続・ユーザー判断待ち】`assets/sounds/blue-bgm.m4a`（青の国 ドゥエン）は用意しただけで、
  まだどこからも鳴らしていない。** ユーザーの依頼は「同じ要領でループ化」までで、**いつ流すか**は
  決まっていない（続き…で「7色ぶん作って自分の色で切り替える構想か？」と聞いたが未回答）。
  全プレイヤーが聴く音楽が変わる変更なので、勝手に配線しない。候補は
  ①自分の駒が青の時だけ対戦BGMをこれにする ②七色の戦場と交互/ランダム ③7色ぶん揃うまで保留。

- **ffmpeg** はこのPCへ導入済み（続き502）。`node tools/shrink-audio.mjs` で再変換できる。
  原盤は `音声/BGM/`（.gitignore 対象＝ローカルのみ）。

- **検証**: `npm test` 58/58 PASS、`node test/smoke.mjs 2` PASS、
  `node tools/check-undeclared.mjs` 0件（164ファイル）、`node --check` 通過。
  **ブラウザ実測**——m4a 3本が読み込まれ（.mp3 の要求は0件）、404なし、例外0件。
  フェードインは GainNode への実際の書き込みを追って **0 →（64段階・1.4秒）→ 目標値0.32** を確認。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-09（続き504）：加工済みBGMの控えを `音声/BGM（ループ編集、サイズ縮小等）` へ保存／**原盤フォルダがリネームされていてツールが壊れていた**のを、名前で決め打ちしない形に直した

ユーザー依頼「BGMファイルをここに保存してほしい」＋「青の国は現在保留。これはいったん別の
ユドコネ版で流す予定」。

- **原盤フォルダが `音声/BGM` → `音声/BGM（オリジナル）` にリネームされていた**（ユーザーが作業中に
  整理したもの）。続き503 で作った `tools/shrink-audio.mjs` は `音声/BGM` を決め打ちしていたので、
  **この時点で既に壊れていた**（次に走らせたら「原盤が無い」と言って何もしないところだった）。
  - **直し方**: `音声/` の中で「`BGM` で始まり、原盤ファイルが実在するフォルダ」を探す方式にした。
    もう一方の `BGM…` フォルダを自動的に控え先として使う。以後リネームされても追従する。
  - **教訓**: ユーザーが自分で整理するフォルダを、ツールが名前で決め打ちしない。
    **中身で判定する**（ここでは「原盤ファイルが実在するか」）。

- **ツールが控えも書くようにした**（`archive` / `mp3` オプション）。走らせるだけで
  `音声/BGM（ループ編集、サイズ縮小等）` に分かりやすい名前で残る。
  青の国だけ **mp3 も出す**——ユドンリウムコネクトが m4a を受け付けない可能性があるため。
  **m4a からの変換ではなく原盤から作り直す**ので二重圧縮にならない。
  - 控えの中身: オープニング_縮小のみ.m4a(1:25) / ゲーム時_七色の戦場_縮小のみ.m4a(2:24) /
    プレイヤー待機中_待機の水鏡_113秒ループ.m4a(1:53) / 勝利時_縮小のみ.m4a(0:47) /
    青の国　ドゥエン_90秒ループ.m4a(1:30) / 同 .mp3(1:30)。
  - `音声/` は `.gitignore` 対象なので、控えは公開リポジトリに入らない（確認済み）。

- **青の国 ドゥエンはゲームからは引き続き未接続**。ユーザー判断「現在は保留」。別途ユドコネ版で使う。

- **【重要な作業上の罠】このシェルは日本語パスの扱いが不安定**。同じセッション内で
  `ls 音声/BGM/` が成功したり `No such file or directory` になったりする（バイト列が
  `$'\351\237\263\345\243\260'` のように化ける）。さらに**ヒアドキュメントで .mjs を書くと、
  中の日本語リテラルが壊れることがある**（続き454 の「バックスラッシュが落ちる」と同種）。
  - **回避策**: スクリプトに日本語のフルパスを書かない。`process.cwd()` や
    `import.meta.dirname` を起点に、`fs.readdirSync` が返す**OSから来た正しい名前**で辿る。
    実際 `tools/shrink-audio.mjs` はこの形なので最初から正常に動いていた。

- **検証**: `node tools/shrink-audio.mjs` が原盤フォルダを自動で見つけて 15.68MB→5.59MB を再生成
  （**assets/sounds の5ファイルは同一内容で再生成され、git差分はツールのみ**＝AAC変換が決定的で
  あることの裏付けにもなった）。控え6ファイルの長さも実測で確認。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-09（続き505）：訪問記録に私（Claude）は載っていないことを実測で確認／#343 BGMが鳴らない——再現できないので**測れるようにしたうえで、自分が入れた変更のリスクを3つ潰した**／#342 ロック済みならスキップボタンを隠す

- **【ユーザーの質問】「ログイン履歴の(匿名/未ログイン)はあなたですか？」→ 私ではない（実測）**:
  `recordVisit()`（online.js）は **localhost / 127.0.0.1 / 0.0.0.0 では早期returnする**（管理者本人も
  除外）。私のテスト・プローブは**すべてローカルサーバー**（8795/8796/94xx番台）なので対象外。
  実際に本物のアプリをローカルで開いて通信を見張り、**so7_visit_log への書き込み0件**を確認した。
  ・「(匿名/未ログイン)」の正体は2つ——①user_id が null（ログインせずに開いた人）
  　②**ログイン済みだが so7_user_profiles に display_name が無い人**（ゲストログイン＝メール欄が「-」）。
  　ダッシュボードの表示は `row.display_name || "(匿名/未ログイン)"` なので、後者も同じ文言になる。
  　**メール欄で見分けられる**ことをユーザーへ伝えた。

- **【#343「なぜかBGMが鳴ってない」】再現できなかった。順番として、まず**測れるように**した**
  （続き473と同じ方針）。そのうえで、**続き503で私が mp3→m4a に変えたことのリスク**を潰した。
  ・**先に外堀を測った**: 本番の m4a 4本は **HTTP 200 / Content-Type: audio/mp4**、中身は
  　**AAC-LC 44.1kHz ステレオ**（ffprobe）＝iPhoneが再生できる形式。ファイルと配信は無実。
  ・**① 診断**: `diag-bgm`（鳴らし始めた3秒後に1回だけ。src / paused / readyState / エラー番号 /
  　gain / 目標音量 / masterBgm / AudioContextの状態 / hidden）と `diag-bgm-fallback` を追加。
  　次の報告で「そもそも始まっていない・音量0・AudioContextが止まっている・ファイルを再生できない」
  　のどれかが**推測なしで**分かる。
  ・**② mp3への自動フォールバック**: `createBgmAudio(name)` を新設し、m4a の error を1回だけ拾って
  　`assets/sounds/<name>.mp3` へ差し替える（続き503で元のmp3を残してあるので必ず在る）。
  　**実測**: m4a の配信を止めると `diag-bgm-fallback {code:4}` → **mp3 を取得して再生継続**
  　（診断ログの src が game-bgm.mp3 に変わり paused:false）。通常時は m4a のまま。
  ・**③【本物の欠陥・修正】画面が隠れた時に AudioContext を suspend しているのに、戻った時に
  　resume していなかった**。BGMは `createMediaElementSource` でこの context を通しているので、
  　止まったままだと**「再生中なのに完全に無音」**になる（`paused` では検知できない）。
  　iPhoneでアプリを切り替えて戻ると BGM だけ聞こえなくなる経路がそのまま残っていた。
  ・**④【もう1つの本物の欠陥・私の回帰】フェードインを「呼ばれた回数」で数えていた**
  　（続き503で私が入れた）。`setInterval(30ms)` × 47回 で1.4秒のつもりだったが、**報告の端末は
  　1フレーム135〜190ms**。タイマーが遅れた分だけフェードが間延びし、その間ずっとほぼ無音＝
  　**「BGMが鳴っていない」ように聞こえる**。**実際に経過した時間**で決める形に直した。
  　**A/Bで実測**: 鳴らし始めて3秒後の音量が **目標の半分(0.061/0.12) → 目標そのもの(0.12)**。
  ・**教訓**: 時間で終わるべきものを「回数」で数えない。回数は**端末が重いほど伸びる**ので、
  　速い開発機では正しく見えて、遅い実機だけで壊れる（＝一番見つけにくい形になる）。

- **【#342】ロックする札を選んだ後もスキップボタンが残っていた（修正）**: 続き437で
  `isPhaseTransitionPending()` の間は隠すようにしてあるが、**「ロックした」から「次のフェイズの
  開始が予約される」までの間**（演出・お知らせを待っている窓）はどちらの条件にも当たらず、
  currentPhase は "lock" のまま残るのでボタンが出続けていた。マイデッキボタン(#320)が既に使って
  いる3つ——送信中 / このフェイズで既にロック済み / ロックの代わりにマイデッキから引いた——を
  スキップ側にも足した。
  ・**A/Bで実測**: ロックフェイズ中は `block` → `noteLockSubmitInFlight(true)` で **`none`** →
  　戻すと `block`。
  ・**測れなかったこと（記録）**: 「実際にロックした直後」の窓は手元で作れなかった——この環境では
  　お知らせの列が空なのでフェイズが即座に次へ進んでしまう（中央を塞いでも同じだった）。
  　同じ判定をマイデッキボタンが既に使って効いているので配線は確かだが、**その窓そのものは未実測**。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  `node --check`（.mjs へコピーして）sound.js・phase-automation.js・changelog.js とも通過、
  お知らせ 66回・今日の回は ja/en とも 7/7 で一致（2026-07-30〜08-28 の13回に英語が無いのは
  続き502で報告済みの積み残しで、今回の変更とは無関係）。例外0件。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り（#344・ユーザーからの提案に未回答）**: 「手札から複数枚捨てる時、捨てる順に押して
  いって**最後に1回だけ確認**。選び済みをもう一度押したら**選択解除**」。現状は
  `requestHandCardChoiceForEffect` が1枚ごとに `confirmTouchAction` を挟むので、5枚なら確認が4回出る。
  実装するなら「順番付きの複数選択ピッカー」を新設し、engine 側の1枚ずつのループ（続き469）を
  それに置き換える形になる。ユーザーの判断待ち。

### 2026-09-09（続き506）：CPU戦でも白黒／ブースト／マイデッキ／自分の持ち時間を選べるように／#344 複数枚捨てる時を「順番に押して最後に1回だけ確定」へ／#345 は不具合ではなかった

- **【#345】「私のドムスネロの効果でCPUが2枚ドローした気がする」→ 不具合ではない（カードどおり）**:
  ドムスネロ（eternal-yellow）の手札効果は **「【追色１】２枚ドロー。相手全員は１枚ドロー。」**
  （card-text.ja.js の印刷文どおり）。ログも 16:36:52〜53 に **Aへ2枚／Cへ1枚**で、定義
  （`DRAW count:2 target:SELF` → `DRAW count:1 target:ALL_OPPONENTS`）と完全に一致していた。
  **CPUが引いたのは1枚**。「2枚引いたように見えた」のは、直前の自分の2枚ドローと続けて出るため。

- **【ユーザー要望】CPU戦でも「白黒あり／タイマーあり（実質自分だけ）／ブーストあり／マイデッキあり」**:
  CPU戦を始める画面（ホームの対戦選択）に「対戦ルール」として4つの入/切ボタンを足した。
  ・保存は `cpu-battle-state.js` の永続フラグ（localStorage＋`registerSyncedPref` でアカウントにも）。
  ・白黒・ブーストは `quickStart(count, includeBlackWhite, boost, ...)` の引数へ。
  ・マイデッキは `setupMyDeckMode` へ。**3人・4人戦でも全席ぶん配る**（1席でも欠けるとその席だけ
  　マイデッキから引けない）。あわせて「マイデッキなら必ず2人」の決め打ちを **`noirSeat` の有無**
  　（＝物語のエイドス戦かどうか）で判定する形に直した——人数を選べるCPU戦が2人に潰されるため。
  ・**タイマーだけ意味が特殊**: CPU戦ではCPUの自動プレイを動かすためにタイマー自体は常に動いていて、
  　**自分の席だけが対象外**になっている（turn-timer.js の `isSelfTimeLimitExempt`）。なのでこの設定は
  　「その免除をやめるか」。あわせて `setRopeBaseSeconds(HUMAN_BASE_SECONDS=900)` の上書きも止める
  　——15分にしてしまうと免除を外しても実質時間切れが来ない。
  ・**A/Bで実測**: 席数 2→3／白黒 0→2枚／ブースト札 0→6枚／マイデッキ 無し→A・B・C全員ぶん／
  　基本時間 900→30秒。免除が外れることも別に実測——**切＝残り−12秒でも何も起きずターン1で停止**、
  　**入＝時間切れで処理が進み手番がCへ**。例外0件。

- **【#344】手札から複数枚まとめて捨てる時の選び方（ユーザー提案どおり実装）**:
  ユーザー「捨てる順に押して行って最後に確認がいいかも！毎回これでいいかの確認は大変。またその際、
  選び済みのカードを再度クリックしたら選択解除とかが便利かな？」。**現状を確認したらそのとおりで**、
  `requestHandCardChoiceForEffect` は1枚選ぶごとに `confirmTouchAction` を挟むため、5枚捨てる場面では
  確認が4回出ていた。
  ・`requestHandCardsOrderedForEffect`（picker type `"handMulti"`）を新設。押した順に①②③…の番号が
  　付き、もう一度押すと外れて**後ろが繰り上がる**、最後に「これで捨てる（N/M枚）」を1回。
  ・engine 側（続き469の1枚ずつのループ）を丸ごと置き換えた。**古い helpers しか持たない呼び出し元
  　でも動くよう**、`pickHandCardsOrdered` が無ければ手札の並び順のまま捨てる（＝順番を選べない
  　だけで効果は不発にしない＝善処の原則）。
  ・CPUが選ぶ番は画面に何も出さず、その場で `chooseHandCardToken` を繰り返して順番を決める。
  　持ち時間切れの自動代行にも受け口を用意した（押した順は尊重し、残りだけ自動で並べる）。
  ・**本物の経路で実測**（カード開発モードからザ・ギャンブルの到達効果）:
  　何も押していない＝バッジ無し・「これで捨てる（0/12枚）」→ 3枚目を押す＝「1」・沈む →
  　1枚目も押す＝「2」→ **3枚目をもう一度押すと外れ、1枚目が「2」→「1」へ繰り上がる** →
  　2枚目を押す＝「2」→ 確定すると手札が空になり捨て場12枚・バッジ0・ボタン非表示。例外0件。
  ・常設テストも `pickHandCardsOrdered` を検査する形に更新し、**engine 側の呼び出しを外すと 57/58**、
  　戻すと 58/58 になることをA/Bで確認した（空振りのテストを増やしていない）。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3271）、`node --check`（.mjs へコピーして）
  変更した全ファイル通過、お知らせ 2026-09-09 の回は ja/en とも 9/9 で一致、翻訳キーの
  片側だけのものは既存の1件のみ（`phase.titleSuffix`＝今回とは無関係）。
  サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **検証のハマりどころ（次回のために・今回いちばん時間を使った）**: ザ・ギャンブルの捨てる場面まで
  ヘッドレスで到達させるのに手間取った。原因は3つとも**私のプローブ側**だった——
  ①カード開発モードのパネルを消さないと先へ進まない ②公開の選択モーダルのボタンが
  「残りN枚を**すべて**公開する」で、`/全部/` の正規表現に当たらず**毎回「盤面を見る」を押していた**
  ③色宣言のスウォッチは**同じ色をもう一度押すと外れる**ので、全部押すループが選択を打ち消していた。
  **決め手**: ザ・ギャンブルは `minCount: 2`＝**2色以上**なので、**7色すべて宣言すれば必ず当たる**
  （各色1回だけ押す）。ここに気づいてから一発で通った。

### 2026-09-09（続き507）：CLAUDE.md を作業ルールどおりに整理（続き486以前を docs/dev-log.md へ移動）／管理者モードの既定値の食い違いは私の誤報だった

- **【整理】** 作業ルールの「日付付き変更履歴が**およそ40エントリ**を超えたら古い方を
  [docs/dev-log.md](docs/dev-log.md) の末尾へ移す」に従った。**74エントリ・3932行**まで溜まっており、
  前回の整理（続き453）から**大幅に超過**していた（毎セッション全文が読み込まれるので、そのぶん
  作業に使える余地が削られる）。
  - **移したもの**: 続き433〜486 の **54エントリ**。dev-log.md は古い順（続き1が先頭・最新が末尾）
    なので、続き432 の直後へ時系列のまま追記した。本体には**続き487〜506 の20エントリ**を残した。
  - **結果**: CLAUDE.md **3932行 → 1136行（71%減）**。dev-log.md 22471行 → 25268行。
  - **検証（実測・機械的に照合）**: ①dev-log.md の元の中身が**そのまま先頭に残っている**（末尾へ
    足しただけ）②追記分が移動分と**完全一致** ③移動した本文が CLAUDE.md から消えている
    ④残した続き487以降が元と**完全一致** ⑤冒頭（概要・アーキテクチャ方針・作業ルール）が元と一致
    ⑥**続き番号の欠落0件**（移動前505件→移動後505件）⑦両方に重複して入っている番号0件。
  - 冒頭の注記と、作業ルールの切り出し履歴の行も新しい範囲（続き486以前・2026-09-09）に更新した。

- **【罠・前回と同じものを踏みかけた】見出しの番号を読む処理が1件だけ取りこぼしていた**:
  `### 2026-09-08（続き494・私の回帰）` のように**番号の後ろに文字が続く**見出しがあり、
  「（続き」と「）」で挟んで数値化する素朴な実装では `494・私の回帰` → NaN になって数え落とした
  （grep は74件・スクリプトは73件、と数が合わないことで気づけた）。**数字が続く間だけ拾う**形に直した。
  切り出し自体は文字位置で切っているので中身に影響は無かったが、**検証の数え方が壊れていた**＝
  「欠落0件」を誤って報告しかねなかった。**2つの数え方が食い違ったら、必ず両方を確かめる。**

- **【私の誤報の訂正】管理者モードの既定値の食い違い（続き499の申し送り）は存在しなかった**:
  「`--card-preview-size`（css 20rem / admin 32rem）と `--move-blink-duration`（css 3 / admin 7）が
  食い違っていて、リセットを押すと配信中と違う値に戻る」と2回にわたり申し送りにしていたが、**誤り**。
  style.css にはこの2つが**2か所ずつ**あり、**ファイル末尾の上書きブロック**（続き499のコメントに
  「このブロックだけが本当のデフォルト値」と明記されている）で 32rem / 7 になっている。CSSは同じ
  詳細度なら後に書かれた方が勝つので、**効いているのは admin.js と一致する値**だった。
  - **実測**: 本物のアプリを開いて `getComputedStyle(document.documentElement)` を読み、
    **`--card-preview-size: 32rem` / `--move-blink-duration: 7`** を確認。＝リセットしても
    配信中の見た目のまま戻る。**ユーザーの判断は不要**なので、この申し送りは取り下げる。
  - **原因**: 続き499/500 の差分スクリプトが **style.css の"前の方"の宣言だけを拾っていた**。
    同じ変数が複数回宣言され得るファイルで「css側の値」を1つに決めるなら、**末尾の上書きブロックを
    見るか、実際に計算後の値を測る**こと（今回のように実測が一番速くて確実）。

- **検証**: 変更したのは CLAUDE.md と docs/dev-log.md の2つだけ＝**配信物（src/）に変更は無い**ので、
  お知らせ（changelog.js）の更新もサーバー側の作業も不要（SQLの実行・Edge Function の再デプロイも不要）。

- **申し送り（現在の未着手・実機確認待ち）**:
  - お知らせ（changelog.js）の **2026-07-30〜08-28 の13回に英語が無い**（一般向け68項目・
    開発者向け181項目）。英語で遊ぶ人にはその期間だけ更新情報が空に見える。
  - **3人・4人戦の降参は未実装**（降参した人の駒・手札・ロックエリアの扱いをルールとして
    決める必要がある＝続き456）。
  - 実機確認待ち: #343 のBGMが直ったか（次の報告に `diag-bgm` が出る）／ゴメンナサイの発動演出
    （`playGomennasaiDeclaration`）／CPU戦の対戦ルール4つ／#344 の順番付きの捨て方。
  - `assets/sounds/blue-bgm.m4a`（青の国 ドゥエン）は**ゲームからは未接続**（ユーザー判断で保留中）。
  - Supabase の Site URL が `http://localhost:3000` のまま（続き476の申し送り④）。
  - GitHub Pro での非公開化は「宣伝を始める前／クラファン告知前／アナログ版発売前」に実施する約束。

### 2026-09-09（続き508）：ゴメンナサイの発動演出と「今回のメンバー」紹介を少し長く（どちらも管理者モードから変えられる形にした）

ユーザー要望「ゴメンナサイの発動演出→もう少し気持ち長くカードを表示させたいかも！／今回のメンバーの
表示ももう少し長くしたいかも」。**言われた数字を決め打ちで入れるのではなく、両方ともスライダーで
詰められる形**にした（他の演出と同じ扱いに揃える）。

- **「今回のメンバー」紹介**: `--match-intro-duration` は続き490 で既に管理者モードから変えられた
  ので、**既定値だけ 2.8 → 3.8秒**（style.css の `:root` と admin.js の `default` の両方）。

- **ゴメンナサイの発動演出**: こちらは **1.5秒の決め打ち**（`GOMENNASAI_DECLARE_MS` と、CSSの
  3つのアニメーション）だったので、`--gomennasai-declare-duration`（既定 **2.2秒**）を新設し、
  JS側は `gomennasaiDeclareMs()` で読む・CSS側は `calc(var(…) * 1s)` で読む形に揃えた。
  - **キーフレームの%はこの長さに対する割合**なので、伸ばすと「カードが大きく止まっている区間」
    （30%〜78%）もそのぶん伸びる＝要望どおり**カードを見ている時間が長くなる**。1.5→2.2秒で
    止まっている時間は 0.72秒 → 1.06秒。輪の弾け（1.1秒）は出だしに紐づく演出なので固定のまま。
  - **JSとCSSが同じ変数を見る**ので、片方だけ伸びて「絵が先に終わって幕だけ残る」ことは起きない。
  - 管理者モードに「ゴメンナサイの発動演出（実験用プレビュー付き）」を追加。**本物の対局で
    「相手の7色目を止める」場面まで行かないと見られない**ので、続き492 と同じ「押すだけ」の
    プレビューボタンを付けた（`registerGomennasaiPreviewHelper` で main.js から注入）。

- **検証（実測・A/Bまで）**:
  - CSS変数: `--match-intro-duration: 3.8` / `--gomennasai-declare-duration: 2.2`。
  - **管理者パネルの本物のボタンをクリック**して発動演出を出し、幕とカードの `animationDuration` が
    **2.2s**、カードの画像が `purple-sorry.webp`、文字が「ゴメンナサイッ！」。
    **1.65秒後にはまだ出ていて（＝修正前の1.5秒なら消えている）、2.55秒後には片付いている**。
  - 紹介の方は**受け取り側まで測った**（続き442の教訓）——`--match-intro-duration` を 1.2 に
    上書きすると、実際に出ている時間が **4132ms → 2104ms** に変わる＝JS側がこの変数を読んでいる。
  - `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
    CSSブレース平衡（3272）、`node --check`（.mjs へコピー）main.js・admin.js・changelog.js 通過、
    **管理者スライダーの既定値19本が style.css の `:root` と完全一致**、お知らせ ja/en とも件数一致
    （66回・不一致0）、例外0件。
  - サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **検証の道具についての記録**: お知らせの ja/en の件数を数えるのに、その場で書いた
  「`[` から `]` までの `"` を数える」式の集計を使ったら **2.5件・15.5件**という有り得ない数が出た
  （項目の文中に `]` や エスケープされた `"` があるため）。**既に `scratchpad/chk_changelog.mjs`
  という行単位の集計（バックスラッシュを使わない形）があり、そちらは正しく0件不一致を返す。**
  半端な数が出たら、それはデータではなく**数え方が壊れている合図**。既にある道具を先に探すこと。

### 2026-09-09（続き509）：勝利演出で色が灯る音が「ダサい」の**原因が特定できた**——打撃音用の合成関数を鐘に流用していた（鳴っている間に音程が滑り落ちる）

ユーザー報告「なんか勝利時にロックカードが一個ずつ光るときの音がダサい気がしてます」。
**推測で音を差し替えず、まず今どう鳴っているかをコードと実測で確かめた**。

- **原因（実測で確定）**: `playVictoryChime` は `schedulePartial` で組んでいたが、あれは**鼓動や
  「コツン」という打撃音のための関数**で、`osc.frequency.exponentialRampToValueAtTime(freq * 0.6)`
  ＝**鳴っている間に音程が0.6倍まで滑り落ちる**（打撃音ではそれが正しい）。鐘・鈴は音程が動かない
  ものなので、1音ごとに「ボヨン」と下がって安っぽく聞こえていた。
  - **実測（オシレーターへの指示を記録した）**: 以前の鳴り方は **523Hz → 314Hz へ滑る／1047 → 628**。
    部分音は2本だけ。**新しい鐘は滑り先が無く（null）、部分音は4本**。
  - あわせて、色が灯るたびに**到達効果音（カードに乗った時の音）を7回鳴らしていた**のも賑やかすぎた
    （続き465でユーザー指示により暫定で割り当てたもの。「専用の素材ができたらここだけ差し替える」
    と書いてあった箇所）。

- **直し方**: `scheduleBellPartial()` を新設——**音程を動かさず**、立ち上がりを速く、余韻を長く取る。
  鐘は「倍音ほど早く消える」ので、基音1.15秒／2倍0.8／3倍0.5／**4.2倍0.32（整数倍でない部分音＝
  金属らしさ）**という減衰を与えた。7色が同時に灯る最後の瞬間は、同じ音階から4音の**和音**にした
  （`playVictoryChimeChord`。ここだけ到達効果音のままだと鐘の中で浮くため）。
  - **耳で決めるものなので、聴き比べられる形にした**（続き492のプレビューボタンと同じ考え方）。
    管理者モード →「✨ 演出」→「🏆 勝利演出: 色が灯る音（聴き比べ）」に、
    **澄んだ鐘（既定）／鐘＋きらめき（華やか）／以前のまま**の選択と「▶ 試聴」ボタンを置いた。
    試聴は本番と同じ間隔（前半ゆっくり→後半速く）で7音鳴らし、最後に和音を鳴らす。
  - `victoryChimeStyle` は sound.js が持つ（音の話なので）。`"legacy"` を選んだ時だけ、
    victory-celebration.js 側も**以前どおり到達効果音を重ねる**（見た目の設定と違い、片方だけ
    元に戻ると噛み合わないため、2か所を同じ1つの値で切り替える）。

- **検証（実測・A/Bで送る側と受け取り側の両方）**:
  - **合成の中身**: オシレーターへの指示を記録して——以前=2本・**滑り落ちる**（523→314 / 1047→628）／
    鐘=4本・**滑らない**／きらめき=6本・滑らない／同時発光の和音=8本（4音×2倍音）。
  - **本物の勝利演出でA/B**（7色をロックした盤面で実際に演出を走らせ、音の再生回数を数えた）——
    **鐘: 合成音38・音素材3／以前のまま: 合成音16・音素材11**。差は**音素材がちょうど8回**
    （7色＋同時発光の1回）と**合成音が22本**（7音×2本増＋和音8本）で、設計どおりの数と一致した。
  - **管理者パネルの本物のUI**を操作——選択肢3つが出て、セレクトで `crystal` に切り替わり、
    「▶ 試聴」で**50本**の合成音が鳴った（7音×6本＋和音8本＝50＝きらめきの設計どおり）。
  - `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
    `node --check`（.mjs へコピー）sound.js・victory-celebration.js・admin.js・changelog.js 通過、
    お知らせ ja/en とも件数一致（66回・不一致0）、例外0件。
  - サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **教訓**: **打撃音のための合成関数を、余韻のある音（鐘・鈴・チャイム）に流用しない。**
  打撃音では「鳴っている間に音程が下がる」のが正しいが、同じ関数を鐘に使うと**必ず安っぽくなる**。
  音の不満は「素材が悪い」と考えがちだが、今回は**素材ではなく組み立て方**が原因だった。
  （素材を追加せずに直せたので、アートディレクターへの依頼も不要。）

- **申し送り**: 選んだ鳴らし方は**その場かぎり**（localStorage に保存していない）。ユーザーが
  「きらめき」を気に入った場合は、sound.js の `victoryChimeStyle` の既定を変えること。

### 2026-09-09（続き510）：勝利の音を低い「バフン」に（ユーザー指定）／**低い音は iPhone のスピーカーでは鳴らない**——最初の版は実測で「400Hz以上が0」だった／音の測り方そのものを作り直した

ユーザー要望「音はバフンって感じの低めの音がいいかも！」。続き509 で鐘にしたばかりだが、
**耳で決めるものなのでユーザーの指定が正**。既定を `whump`（低い「バフン」）に変え、鐘・きらめき・
以前のままは聴き比べの選択肢として残した。

- **作り**: 低音（本体・鳴りながら下がる＝打撃音では正しい）／中音／**空気の「フ」**（白色雑音を
  低い方だけ通した短い一吹き）の重ね。音程は 96→159Hz を7段、旋律にならない程度に上げる。
  同時発光は一段低く長い（74Hz・0.62秒）。

- **【最初の版は失敗していた・実測で判明】**: 最初は「スマホでも聞こえる芯」として
  `schedulePartial(freq * 6, …)` を1本足しただけだった。ところがあの関数は**鳴っている間に音程が
  0.6倍まで滑り落ちる**ので、足した芯（576Hz）が**70ms のうちに 346Hz まで下がって、狙った帯域から
  逃げてしまう**。実測すると **400Hz以上の成分が実質0**＝**iPhone本体では無音同然**だった
  （#237・続き381 と同じ罠を、同じ日に2回踏んだ——続き509 では「滑り落ちるのが原因」と自分で
  書いておきながら、対策の層に同じ関数を使っていた）。
  - 音程が動かない部品（`scheduleBellPartial`）で芯を2本（470Hz・780Hz）、さらに**帯域を絞った
    空気**（900Hz中心のバンドパス雑音）を足して作り直した。

- **【測り方を作り直した】** 最初の測定は、実際に鳴らしながら解析器で拾う形にしたが、**同じ音を
  測るたびに数字が変わった**（0.17→0.05 など）。短い音は取りこぼすので、この測り方では判断できない。
  `scratchpad/_soundlab.html` を作り、**OfflineAudioContext**（同じ入力なら必ず同じ結果を返す）で
  sound.js の本物の関数をそのまま録る形にした。sound.js は最初に作った器を使い回すので、
  **1音ごとにページを読み直す**。
  - 判断の物差しは「400Hzのハイパスを通した後のRMS＝**スマホのスピーカーで実際に出る分**」。
    これは #237 で使われたのと同じ考え方。

- **実測（すべて同じ物差し・再現する数字）**:
  ・**バフン 0.0060**（最初の版は 0.0039、その前の「芯なし」は 0）
  ・バフン(同時発光) 0.0069 ／ 澄んだ鐘 0.0224 ／ 以前のチャイム 0.0145
  ・**参照: 鼓動（#237でiPhone向けに手当てした音）0.0010** ／ 勝利の一撃 0.0097
  → バフンは**鼓動の6倍・勝利の一撃の6割**。最大振幅0.34で歪まない（1.0未満）。
  ・**本物の勝利演出**でも確認——7色ぶん鳴り、到達効果音は重ならず（音素材19＝雑音の層16＋他3）、例外0件。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（164ファイル）、
  `node --check`（.mjs へコピー）sound.js・admin.js・changelog.js 通過、お知らせ ja/en とも件数一致
  （66回・不一致0）。サーバー側の変更は無い＝SQL実行・Edge Function の再デプロイは不要。

- **教訓（2つとも今日の再発）**:
  1. **打撃音用の合成（音程が滑り落ちる）を、狙った帯域に置きたい音に使わない。** 置いたつもりの
     周波数から、鳴っている間に逃げる。
  2. **音を「鳴らしながら」測らない。** OfflineAudioContext なら同じ数字が必ず出る。
     揺れる測定で良し悪しを判断すると、直したか壊したかが分からない。

- **申し送り**: 選んだ鳴らし方は**その場かぎり**（保存していない）。ユーザーが別のものを気に入ったら
  sound.js の `victoryChimeStyle` の既定を変えること。「バフンが硬い／柔らかすぎる」場合は
  `scheduleWhump` の 470Hz・780Hz・900Hz の3層（スマホ用）の量で印象が変わる。

### 2026-09-11（続き511）：試遊の入口（`?trial`・[src/trial-entry.js](src/trial-entry.js) 新規）——登録なしでチュートリアル／CPU戦へ直行／戦績システムのボタンをそこへ向けた／クラファン向けに「アプリとアナログ版の違い」の確認メモを作成

ユーザー相談「アナログ版のクラファンページにデジタル版のリンクを載せたい。目的は支援前の試遊」→
提案した A（試遊用の入口）と B（違いの一覧）に「A,Bを行こう！」。

- **まず実際に「初めて来た人」の状態で開いて測った（遊び始めるまでが長かった）**:
  開く → サウンド設定（2画面）→ タイトル → 「ログイン」→「ゲストでログイン」→ 確認 → ホーム →
  CPUマッチ → ルール選択 → 開始。**CPU戦はログインしないと始められなかった**（2026-08-14 の
  ユーザー要望でタイトルの「CPU戦」ボタンを撤去したため）。ただし**ホームのCPU戦・物語チュートリアル
  自体はログインを必要としていない**（ホームへ行くための入口がログインだけだった）。

- **クラファン側の決定を先に読んだ（読み取りのみ）**: `D:/7 SHADES OF SEVEN remake クラファン/CLAUDE.md` に
  「テストプレイの入り口は戦績管理システム（BATTLE-log）を唯一の窓口にする（2026-09-11 ユーザー決定）」
  とあった。つまりページのリンク先は BATTLE-log で、そこからアプリへ飛ぶ。**アプリ側だけ作っても
  BATTLE-log のボタンが素のアドレスのままだと効かない**ので、ボタンの行き先も変えた（下記）。

- **A: 試遊の入口（`https://seven.asobuzz.net/?trial`）**:
  - タイトル画面で、ログインの小さなボタンの代わりに**試遊のパネル**を出す——「試遊版（開発中）」／
    「📖 遊び方を教わりながら遊ぶ」（`startEidosStory`＝物語チュートリアル）／「🤖 すぐにCPUと対戦する」
    （`startCpuBattle(2)`→閉じる→`runCpuBattleSetup`。ホームのCPU戦と同じ順番＝盤面を空にしてから閉じる）
    ／「ログインして、すべての機能を使う」。重いモジュールは**動的import**（opening-screen.js から
    静的な依存辺を作らない。旧CPU戦ボタン撤去時の方針に合わせた）。
  - **初回のサウンド設定モーダルは出さない**（`main.js`）。音量は既定のまま始まり、あとでオプションで変えられる。
    表示済みの印も付けないので、あとで普通に来た時には今までどおり出る。
  - **ログインへ進む時に `?trial` を消す**（`clearTrialParam`）。Googleログインはページを離れて
    「今のURL」へ戻ってくるので、残すとログインし終えた人にまた試遊パネルが出てしまう。
  - **ログイン済みの人（テスターが同じボタンから来た場合）は今までどおり**——既存の
    「ログイン済みならカード（オンラインで続ける）を自動で開く」処理が `showCard` を呼び、
    `showCard` がパネルを隠す。✕でカードを閉じるとパネルに戻る。
  - **スマホ**: 1600x900 を縮めて描くので 1rem が実画面7px前後（続き439）。そのままだと説明文が4〜5pxで
    読めなかった（撮影で確認）ので、`body.is-phone-device` では説明文を省き、ボタン・見出しを大きくした。
    **PC**: 22rem だと右端がタイトルロゴの「SHADES」に重なり説明文が読みにくかった（撮影で確認）ので
    18rem に細め、文言も1行に収まる長さへ短くした。

- **戦績管理システム（BATTLE-log・別リポジトリ）**: 「デジタルアプリ版をテストプレイする」ボタンの行き先を
  `https://seven.asobuzz.net` → **`https://seven.asobuzz.net/?trial`** に（index.html の1行のみ）。
  同リポジトリに身に覚えのない未追跡フォルダ（`ユドナリウムコネクト/`）があるが、**触っていない**。

- **B: 違いの確認メモ**を `D:/7 SHADES OF SEVEN remake クラファン/参考/デジタル版（Webアプリ体験版）とアナログ版の違い_2026-09-11.md` に作成
  （クラファンのセッションへ渡す用。公開リポジトリには置かない）。調べて分かった要点——
  - **「デジタル版」が2つの意味で使われている**: リターンの「デジタル版（ユドコネ）zip」と、TRY IT 節の
    「デジタル版の体験版」（＝このアプリ）。支援者が「無料アプリ＝リターンのzip」と読める余地がある。
  - **ブーストモードの「効果なしのファーストカード」はアナログの内容物に無い**（説明書の内容物＝
    通常112・エターナル7・ファースト7・サマリー4）。**マイデッキ戦**は `マイデッキ戦.txt` の**暫定ルール**で、
    その中の**ONLYカードはアプリ未実装**——一方クラファンのリターンには「限定ONLYカード」がある。
  - クラファン側 CLAUDE.md の BATTLE-log のアドレスが**移転前の旧アドレス**のまま。
  - カード文（「到達効果」への統一6枚）と、入稿データの絵がイラスト差し替え前である件は「要確認」として記載。

- **検証（実測・初めて来た人の状態＝何も既読にせずログインもしない）**:
  - `?trial`: サウンド設定モーダル**出ない**／試遊パネル**出る**・ログインボタン**隠れる**／
    「ログインして…」でURLから `trial` が**消え**カードが出る→✕で**パネルに戻る**。
  - 「すぐにCPUと対戦する」: **ログインしないまま**タイトルが閉じ、参加席 A・C、盤面49枚・駒2、
    **21秒で手番Aが入る**（セットアップ演出の分）。スマホとして見せかけて**指でタップ**しても同様に始まる
    （手番が入るまで42秒＝端末を重く見せかけている分）。
  - 「遊び方を教わりながら遊ぶ」: タイトルが閉じ、エイドスの会話が出る。
  - **普通に開いた人は今までどおり**（サウンド設定モーダルが出る・ログインボタン・パネル無し）。
  - スマホ（844x390・タッチ）: パネルは画面内（上103〜下287）、ボタンの実寸42px・文字17.4px、説明文は非表示。
    英語（`?trial&lang=en`）も同様。PC: パネルの右端384px（ロゴに重ならない）、説明文は1行。
  - `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
    CSSブレース平衡（3279）、`node --check`（.mjs へコピー）通過、お知らせ ja/en とも件数一致（67回・不一致0）、例外0件。
  - サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り**:
  - **お知らせにクラファンの話は書いていない**（アプリ内のお知らせは公開情報なので、告知前に漏らさない）。
  - 試遊の入口から来た人の数は、今の訪問記録（`so7_visit_log` は user_id しか持たない）では区別できない。
    数えたくなったら列を足すSQLが要る（ユーザーの手動実行）。
  - 告知前の宿題（続き〜の申し送りのまま）: GitHub の非公開化／検索設定（robots.txt の Disallow のせいで、
    外部からリンクされると説明の無いアドレスだけが検索結果に出ることがある）。

### 2026-09-11（続き512）：チュートリアルの3件——物語を終えた人にエイドス戦で通常チュートリアルが再発／**スポットの穴の中も暗幕で暗かった（全手順）**／物語チュートリアルの説明枠がライト配色でも暗い

ユーザー報告（試遊の入口＝続き511 から物語チュートリアルを通した時に気づいたもの）:
「エイドス戦に入ると再度チュートリアルが発生します。しかも最初のチュートリアルはモーダルがダーク。
またエイドス戦時に始まる通常のチュートリアルの（3/16 ロックフェイズ）でロックアイコンがスポットされてません」。
**3件とも試遊の入口が原因ではなく、以前からあった**（テスターは既に印を持っていたので表に出ていなかった。
まっさらな状態で物語チュートリアルを通す道を作ったことで見えた）。**直す前に3件とも再現を実測した。**

- **① 通常チュートリアルが再発（原因確定・修正）**: 「見た」印が2つ別々だった——通常チュートリアル
  （tutorial.js）は `so7-tutorial-completed`、物語チュートリアルは eidos の `tutorial_completed`。
  物語を終えても前者が付かないので、エイドス戦で `turnPlayer` が入った瞬間に `initTutorialAutoStart` が
  通常チュートリアルを出していた。**実測**: eidos だけ済みの状態で対局を始めると出る（印は null）。
  - 修正を2か所に入れた: (a) `onOperationTutorialComplete`（物語側）で `markTutorialCompleted()` を呼ぶ
    （公開した。動的import＝両モジュール間に静的な依存辺を作らない）。(b) **自動表示の直前にも**
    eidos の `tutorial_completed` を見て、済みなら印を付けて出さない——別の端末で物語を進めた人
    （進捗はアカウントから同期される）は (a) を通らないため。こちらも動的import（admin.js の確認と同じ形）。
  - **実測（修正後）**: 同じ状態で対局を始めても**出ない**、印が `"1"` になる。

- **② スポットが照らされない（原因確定・修正・全手順に効く）**: 通常チュートリアルは
  **暗幕（`#tutorial-scrim`、全画面 rgba(0,0,0,.55)）の上に、スポット（穴の周りだけ box-shadow で暗くする）を
  重ねていた**ので、**穴の中も1枚目の暗幕で暗いまま**、外は二重に暗かった。**実測**: ロックのアイコンの中心で
  一番上の要素が `#tutorial-scrim`。明るい手札（2/16）は暗くても見えたので気づかれず、暗い色のアイコン
  （3/16）で「照らされていない」ように見えた。
  - 修正: スポットを出す時は暗幕を隠し（`scrimEl.style.display = "none"`）、暗さはスポットの box-shadow だけで
    作る。指す先が無い手順では今までどおり暗幕を出す。**物語チュートリアル側（tutorial-battle-ui.js）は
    元からこの形**（スポット中は暗幕を透明にする）だった。
  - **実測（修正後・PC とスマホ）**: 3/16 でアイコン中心の一番上が**アイコン自身**（暗幕は display:none）、
    1/16（指す先なし）は暗幕 block・スポット none。撮影でもアイコンが明るく見える。

- **③ 物語チュートリアルの説明枠が暗い（原因確定・修正）**: tutorial-battle-ui.js は自前の <style> で
  暗い配色を差し込んでおり、**ライト配色の指定が1つも無かった**（実測: ライト配色でも文字色 rgb(226,232,240)）。
  style.css に `body.theme-light #tutorial-battle-callout …` を足して**詳細度で**上書き（差し込み順に依存しない）。
  見た目は通常チュートリアルのライト配色に揃えた（クリーム地・茶の見出し・次へは #be185d）。浮かぶヒント
  （`#tutorial-battle-tip`）も同じ配色に。暗幕はダークのまま（通常チュートリアルと同じ方針）。
  - **実測（修正後）**: 地 rgba(255,253,247,.98)・文字 rgb(58,51,38)・見出し rgb(166,124,26)。撮影でも確認。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  CSSブレース平衡（3292）、`node --check`（.mjs へコピー）tutorial.js・eidos-story.js・changelog.js 通過、
  お知らせ ja/en とも件数一致（67回・不一致0）、例外0件。サーバー側の変更は無い＝SQL実行・再デプロイ不要。

- **ユーザーへ確認したいこと（申し送り）**: 試遊の入口の「すぐにCPUと対戦する」（説明文「遊び方を知っている方向け」）
  から入った**初めての人にも、最初の対局で通常チュートリアル（16手順）が自動で出る**（まっさらな人には
  どちらの印も無いため）。説明文と食い違うので、試遊のCPU戦では出さない方がよいか確認する。
  → **続き513で決着（出したまま）**。

### 2026-09-11（続き513）：試遊の入口の画面——「開発中のアプリです」を大きく／ログインを他の2つと同じボタンに／CPU戦の説明文をチュートリアルが出る事実に合わせた

- **続き512 の申し送りへのユーザー判断**:「誤って初心者が入る場合もあります。そのままでいいのでは？
  スキップボタンもあるし」→ **試遊のCPU戦でも通常チュートリアルは出したまま**（コードは変えない）。
  経験者はスキップ1回で済むが、初心者に案内が出ないと盤面で迷子になる——困り方が非対称なため。
  代わりに**説明文の方を事実に合わせた**:「遊び方を知っている方向けの1対1です」→
  「1対1の対戦。最初の案内はスキップできます。」（en も同様）。

- **ユーザー要望2件**:「ログインしてすべてを利用する的なボタンをほかの試遊のボタンと同等に目立たせよう」
  「最初にでっかくこれは開発中アプリですしっかり出そう」。
  - 先頭の小さな札（試遊版（開発中））→ **不透明な橙の囲み**（`.opening-trial-notice`）に
    「⚠ 開発中のアプリです」（パネル内で最大の文字・PC 19px／スマホ 21px）＋「不具合や内容の変更があり得ます。」。
    別のモーダルにはしていない（遊び始めるまでの操作を1つも増やさないため）。
  - ログインは下線付きの小さな文字（`.opening-trial-login-link`、CSSごと削除）→ **他の2つと同じ
    `.opening-trial-btn`**（地の色はCPU戦と同じ）＋説明文「オンライン対戦など。ゲストなら登録不要です。」。
  - 翻訳キーは**名前を変えずに**文言だけ替えた（`trial.badge` など）。古い opening-screen.js がキャッシュに
    残った端末でキー名がそのまま表示される事故を避けるため。新規は `trial.devNote` / `trial.loginDesc`。

- **実測で直したもの（2つ）**:
  ①スマホで「🔑 ログインして、すべての機能を使う」が**2行に折り返し**、そのボタンだけ高さ68px（他は42px）
  　になっていた → 「🔑 ログインしてすべてを使う」に短縮。
  ②囲みの幅を 20rem に決め打ちしたら、スマホ（パネル42rem）では逆に**狭くなって見出しが2行**に
  　なった → `width: 100%`（パネルに合わせる）。PC の日本語の説明文も「〜りす|る」で割れていたので短くした。

- **検証（実測・`scratchpad/probe_trial513.mjs`、何も既読にせずログインもしない状態）**:
  PC(1280x760)・スマホ(844x390 タッチ) × 日本語・英語の4通りで、**囲みの見出し・説明とも1行**、
  **ボタン3つが同じ高さ**（PC 45px／スマホ 42px・文字17.4px）、パネルは画面内（スマホ 上53〜下337）。
  スマホで**ログインのボタンを指でタップ** → `?trial` が消えてログインのカード → ✕で試遊パネルへ戻る。
  普通に開いた人にはパネルも囲みも出ない。撮影でも確認。例外0件。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  CSSブレース平衡（3294）、`node --check`（.mjs へコピー）opening-screen.js・ui-text.js・changelog.js 通過、
  お知らせ ja/en とも件数一致（67回・不一致0）。サーバー側の変更は無い＝SQL実行・再デプロイ不要。

- クラファン向けの確認メモ（`D:/7 SHADES OF SEVEN remake クラファン/参考/…違い_2026-09-11.md`）の
  入口の説明も新しい画面に合わせて更新した（公開リポジトリの外）。

### 2026-09-11（続き514）：タイトル画面の言語トグル（日本語／English）が**一度も押せていなかった**——`pointer-events:none` を受け継いだままだった／どこを押しても切り替わる形に

ユーザー要望「タイトル画面の左上に日本語英語表示がありますがこれを押すと言語が切り替わるようにしちゃおう！」。
最初は「今の言語（大きい方）を押しても何も起きない」だけの話だと思い、トグル全体で次の言語へ回す形に
変えた——ところが**実測したら、どこを押しても1回も切り替わらなかった**。

- **原因（実測で確定）**: `#opening-screen` は `pointer-events: none`（`.opening-screen-content` のコメントにある
  事情＝タイトル画面の空いた所を押しても下へ素通しするため）。子の `.opening-lang-toggle` は**それを受け継いだまま
  `auto` に戻していなかった**。`.opening-screen-content`（ログインのボタン一式）は明示的に戻してあるのに、
  トグルだけ抜けていた。**押した指はトグルを素通りし**、その点の最前面は START 前なら `.opening-start-gate`、
  START 後なら `#scene` だった（`elementsFromPoint` で確認。祖先の computed も seg／toggle／#opening-screen の
  3段とも `pe=none`）。つまり**以前の「切り替え先の小さい文字を押す」作りも、実際には一度も効いていなかった**。
  - **教訓**: `pointer-events: none` の親の下に**押せる物を足したら、その物に `auto` を書く**。見た目は正常に
    出るので、実際に押して（`elementFromPoint` で最前面を見て）確かめないと気づけない。
- **修正**: `.opening-lang-toggle { pointer-events: auto; cursor: pointer }`。クリックは各言語の文字ではなく
  **トグル全体**で受け、`SUPPORTED_LANGS` の次の言語へ回す（今の言語・切り替え先・枠の端のどこでも）。
  各言語の文字は `<button>` のまま（キーボードで押しても親へ伝わる）。枠にカーソルを乗せると
  切り替え先の文字が明るくなる（押すとそちらになる、の合図）。今の言語のカーソルも指のままにした。
- お知らせは「押しても反応しなかったのを直した」と事実どおりに書いた（最初は「小さい方しか押せなかった」と
  書いてしまっていたが、上の実測で誤りと分かったので直した）。

- **検証（実測・`scratchpad/probe_lang514.mjs`／原因の測定は `scratchpad/diag_lang514.mjs`）**:
  PC（マウス）・スマホ（844x390・指でタップ）とも、**START 前に今の言語を押す → en、もう一度 → ja**、
  **パネル表示後に切り替え先 → en／枠の右端 → ja／今の言語 → en**。毎回その点の最前面がトグル自身、
  `so7-lang` が保存され、試遊パネルの文言（ログインのボタン）も切り替わる。**START 前に押しても開始ゲートは
  出たまま**（先へ進まない）、パネル表示後もタイトル画面のまま。例外0件。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  CSSブレース平衡（3295）、`node --check`（.mjs へコピー）opening-screen.js・changelog.js 通過、
  お知らせ ja/en とも件数一致（67回・不一致0）。サーバー側の変更は無い＝SQL実行・再デプロイ不要。

### 2026-09-14（続き515）：**不具合報告を私（Claude）が直接読めるようにした**（ユーザーのChrome経由）／#346 マルメゴがめくる前に手札に見える／#347 マルメゴ失敗後にハンドフェイズが止まる／#348 iPhoneが熱い（3D盤面の常時描画）／#349 CPUの勝利演出が短い（再現せず・記録を追加）

ユーザー「不具合報告をあなたの方で確認できますか？」→「進みましょう！」。

- **【報告の読み方（今回から）】** 不具合報告は `so7_get_admin_bug_reports`（管理者メールのJWTだけが読める）でしか
  取れず、私は管理者としてログインできない（パスワード入力は禁止）。そこで **Claude in Chrome**
  （ユーザー本人の Chrome。管理者ダッシュボードにログイン済み）で `https://seven.asobuzz.net/admin-dashboard.html`
  を開き、そのページの Supabase セッションのまま **読み取りの RPC だけ**を呼んだ。
  - ブラウザ道具の出力は約1000字で切れるので、**一時的な全面ボタンを置いて実クリック**（クリップボードは
    利用者の操作が要る）→ `navigator.clipboard.writeText` → PowerShell の `Get-Clipboard` でファイルへ。
    保存先は**セッションの scratchpad（リポジトリの外）**＝報告にはプレイヤーの情報が入っているため。
    ユーザーのクリップボードは上書きされる（伝えた）。「対処済み」ボタン等には一切触っていない。
  - 報告1件＝行動ログ最大約12万字。`diag-board3d` 等の定期記録を grep で除いて、**報告時刻の前後だけ**読む
    （[[bug-report-paste-size]] と同じ考え方）。

- **#346「マルメゴで公開していく時、フリップする前にカードが手札にきてワクワクしない」（原因確定・修正）**:
  マルメゴは `publicDrawReturningTokens`（公開エリア＝手札の扇の末尾へ引く）→ `gambleReveal`（中央でめくる）の
  **逆の順番**だった。ザ・ギャンブルは #95 で既に `publicDrawThenReveal`（山にあるうちに中央でめくる→公開エリアへ）
  ＋ `begin/endPublicDrawDefer`（全部めくり終わるまで描画を遅らせる）に直してあったので、同じ形に揃えた。
  手札効果封じ・橙判定に要るトークンidは、公開エリアの前後の差分で特定する。
- **#347「マルメゴで失敗した時、ハンドフェイズで何もできないのにスキップされない」（原因確定・修正）**:
  #174 で「公開ドローした席はそのターンのハンドフェイズを自動で閉じない（引いた札を見る/使う間を残す）」に
  していた。マルメゴで橙が出て**引いた札ごと手札を全部捨てても、この抑止だけがターン終わりまで残っていた**。
  抑止は「公開エリアに引いた札がまだ残っている間」だけにした（マンズウッド等、札が残る場合は従来どおり）。
- **A/Bで実測（本物の経路・`scratchpad/probe_marumego.mjs`）**: カード開発モード→`runAutoHandEffect` で
  マルメゴを撃ち（コストの橙を実際に手札から押す・山の上4枚の2枚目に橙を仕込む）、100msごとに記録。
  変更した2ファイルだけ HEAD の版を配る `--old` と比べた——
  **旧: 各札が「画面の新札=1」になってから `flip=1`（めくる前に見えていた）／捨てた後 `手札=0 phase=hand` のまま止まる**。
  **新: めくっている間ずっと「画面の新札=0」／捨てた16秒後（お知らせの列が終わってから）`phase=move` へ進む**。例外0件。
  - プローブのハマりどころ: ①コストの札は**扇で隣の札（マルメゴ）が重なっていて**、中心を押すと別の札に当たる
    （`elementsFromPoint` の先頭で確認）→ その札が一番上に来る点を探して押す ②エターナルの札はトークンに
    なっていないので、エターナルの山の並びを入れ替えてから `drawFromPile("eternal", …)` で手札へ。
- **#348「今、すごいiPhoneの画面が熱いです」（有力な原因を修正・実機未確認）**: `board-3d.js` の `frame()` が
  **requestAnimationFrame のたびに毎回 `renderer.render()`** していた（何も変わらなくても最大60回/秒・iPhoneは
  dpr3で 2293x1290）。板の位置・色・テクスチャが変わるのは「作り直し（rebuild）／盤面の変形やカメラの値の変化／
  テクスチャの読み込み完了／キャンバスの大きさ変更」だけなので、その時だけ描く（`needsDraw`）。待機中は
  保険の作り直し（500ms毎）ぶんの約2回/秒になる。`diag-board3d` に **`drawsPerSec`** を足した＝次の実機の報告で効果が見える。
  - **実測**: ヘッドレス（GPUが無くCPUでWebGLを描くので60回/秒は出ない）で、待機中の描画 **旧3.6回/秒 → 新2回/秒**、
    盤面拡大の後も2回/秒。**描かなくなって絵が古いまま残らないか**も確認——カードを1枚捨て場へ動かすと中央のマスが空になり、
    盤面拡大も旧版と同じ見え方（撮影で比較）。
- **#349「CPUの勝利演出がだいぶはしょられてるように見えた」（再現せず・切り分けの記録を追加）**: iPhone相当
  （932x430・タッチ）とPCで、CPU(C)勝ち／自分(A)勝ちの演出を段ごとに計時した——**スマホ C 22.3秒 / A 22.9秒、
  PC C 15.1秒 / A 17.3秒、全段が出ていて、7色の光も全部画面内**。飛行演出オフ・視差効果を減らす もどちらも false。
  短く見える原因の候補は「演出中に画面を触って早送り（`skipRequested`）」「端末の視差効果を減らす」「端末が重くて動きが飛ぶ」
  なので、`diag-victory-celebration`（段ごとの時刻・触って早送りした時刻・light/flightDisabled/reducedMotion）を記録するようにした。
  handled-bug-reports には入れていない（直していないため）。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  `node --check`（.mjs へコピー）card-effect-engine.js・phase-automation.js・board-3d.js・victory-celebration.js・changelog.js 通過、
  お知らせ ja/en とも件数一致。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-16（続き516）：#348 の発熱対策が**実機で効いていた**（描画 2回/秒）／#350 iPhoneの長押しで文字選択／#351「このマスでいいですか？」が出ない——押したまま離した指で「今後表示しない」が押され得た

ユーザー「debug確認して」。続き515 の手順（Chrome経由・読み取りのみ）で新しい報告 #350・#351（YGM、iPhone、版 20260914）を読んだ。

- **【#348 の確認】** 2件とも新しい版で、`diag-board3d` の **`drawsPerSec` が 2.0〜2.4**（以前は変化が無くても最大60回/秒）。
  続き515 で入れた「変化がある時だけ描く」が実機で効いている。`diag-victory-celebration` はまだ0件（勝利の場面が無かった）。
- **#350「iPhoneで画面長押しするとたまにスクショのやつになる」（原因確定・修正）**: 添付画面では左下の名前・手札枚数と
  お知らせの文字が**青く選択**（選択の取っ手も出ていた）。長押しの抑止は盤面・画像・手札の固定オーバーレイ等に個別に
  付けてきたが、**ページ全体には付いていなかった**。`body` に `user-select:none; -webkit-touch-callout:none`、
  入力欄（input/textarea/select/contenteditable）だけ `text` に戻した。style.css を読むのは index.html だけ＝管理者ダッシュボードには影響しない。
- **#351「このマスでいいですか？の案内モーダルが出ない。設定はオンなのに」（有力な経路を修正・記録を追加）**:
  報告時の設定は on、同じ日の朝の #350 では **off**＝**日中に切り替わっていた**。ログには「確認を出した/答えた」が残らない
  （`tap-move` は答えた後に記録される）ので、この報告だけでは確定できない。
  - **見つけた経路**: 通常移動の確認は **pointerdown（指が触れた瞬間）で開く**。門番（`createOpenGuard`）は「開いてから400ms」
    だけなので、**指を400ms以上置いてから離すと、離した時のクリックが下のボタンに届く**＝#236 と同じ「今後表示しない」が
    知らないうちに押される形が、長押し気味のタップで残っていた。
  - **修正**: 確認のボタン3つとも「**このモーダルが開いた後に始まった** pointerdown からのクリック」だけ受け付ける
    （`ev.timeStamp >= openedAt`。detail===0＝キーボード/プログラムの `.click()` は対象外）。400msの門番もそのまま。
  - **記録**: `setCellConfirmEnabled(v, source)` に切り替え元（options / options-reset / dont-show / sync）を持たせ、
    変わるたびに `diag-cell-confirm-pref` を残す＝次に「勝手に切れた」ら、どこから切れたかがログで分かる。
  - **A/Bで実測（iPhone横 932x318・タッチ、`scratchpad/probe_351.mjs`、`--old` で cell-confirm.js と style.css だけ HEAD）**:
    pointerdown で確認を開き、CDP の本物のタッチで「今後表示しない」の上を0.7秒押して離す——
    **旧: 設定が false になり、確定(true)してモーダルが消える／新: 無視されてモーダルが残り、改めて「はい」をタップすると確定**。
    #350 は **旧: body・名前欄とも `auto`／新: `none`、入力欄は `text`**。記録も実際に出ることを確認（切り替え2回→2行）。
  - **正直な限界（ユーザーへ伝えた）**: この画面サイズの見せかけでは、49マスどれを押してもモーダルが**押したマスの中心には
    重ならなかった**（0/49）。指がずれる／モーダルの上に指が来る場合に起きる経路で、YGMさんの件が確実にこれだったとは言えない。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  CSSブレース平衡（3297）、`node --check`（.mjs へコピー）cell-confirm.js・options-menu.js・handled-bug-reports.js・changelog.js 通過、
  お知らせ ja/en とも件数一致（69回・不一致0）。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

### 2026-09-18（続き517）：試遊版の宣伝に向けて——検索に出す＋リンク表示（OGP）／試遊の入口から来た人数を数える（SQL追加・要手動実行）

PR戦略セッション（別のClaudeセッション）経由で、ユーザー（YGM）の決定が届いた——試遊版をクラファン（10/17開始）・
ゲムマの事前集客に使う。宣伝前の宿題 (a)「紋様」スキンの改名 (b) 新イラスト19点への差し替え（クレジットは SEVRAIL 名義、
表記はクラファン相談セッションと突き合わせる）(c) GitHub 非公開化（本人操作）、依頼 (d) 検索設定の案 (e) 試遊の人数を数えるSQL。

- **(a) 本人へ直接確認した結果、改名しない**（「クラファンページのコンポーネントのところでも載せている基本の紋様で試遊でいい」）。
  なお「スタンダード／標準」は既に skin.0（今の既定の駒）の名前なので、改名するとしても使えない。
- **(d) 本人判断「検索に出す＋リンク表示」**:
  - `robots.txt` を `Disallow: /` → `Disallow:`（全部読ませる）に。**Disallow で止めると検索エンジンは各ページの noindex を
    読めず、外部からリンクされた時に「説明の無いアドレスだけ」が出る**ので、隠したいページは HTML 側の noindex で止める。
    管理者ダッシュボードと 404 は noindex のまま（確認済み）。**robots.txt はドメイン直下の1枚なので、戦績システム
    （/BATTLE-log/。自前の noindex は無い）も今回から検索対象になる**（公式の窓口なので出る方が自然、とユーザーへ伝えた）。
  - `index.html` の noindex を外し、`description` と OGP（og:title/description/url/image、twitter:card=summary_large_image）を追加。
    og:url は `https://seven.asobuzz.net/?trial`。画像は `assets/og-image.jpg`（1200x630・83KB。タイトル画面の絵
    `assets/opening.webp` を切らずに収め、上下を絵の四隅の平均色＝紙の色で埋めた。`scratchpad/make_og.mjs`）。
    **お知らせ・文言にクラファンの話は入れていない**（告知前）。
- **(e) 試遊の入口から来た人を数える**:
  - `so7_visit_log` に `source`（"trial" / `?trial=cf` なら "trial:cf"。値は英数字・-・_ の16文字まで）と
    `visitor_key`（端末ごとの無作為な印＝localStorage `so7-visitor-key`。人数を数えるためだけで個人とは結び付かない）を追加
    （`supabase_setup_so7.sql` 末尾・`if not exists`）。数え方のSQL（日ごと・入口ごとの訪問数と人数／合計／その後ログインした人数）を
    同じ場所にコメントで置いた。
  - `recordVisit()` が両方を送る。**列を足すSQLがまだの間は insert が失敗するので、user_id だけで記録し直す**
    （訪問の記録そのものが止まらないように）。`getTrialSource()` を trial-entry.js に新設。
  - **ユーザーの手動実行が必要**。今回は**追加した部分だけ**を貼るよう伝えた（全文の再実行安全性は今回は機械的に確かめ切って
    いない——関数の中の delete と publication の行がスキャンに掛かったため。追加分は単独で安全）。
  - **宣伝の場所ごとに数えるには、リンクに値を付ける**（例 `?trial=x`）。ただしクラファンの窓口は戦績システム経由なので、
    クラファンから来た人を分けたい時は戦績システムのボタン側に値を付ける必要がある（未実施・申し送り）。
- **検証（実測・`scratchpad/probe_visit517.mjs`。本物のSupabaseへは送らず route で横取り、`seven.test` 名で開いて
  localhost の除外を避けた）**: `?trial` → `{"source":"trial","visitor_key":…}`／`?trial=cf` → `"trial:cf"`／
  普通 → `source:null`／**「列が無い」(400)を返すと2回目に `{"user_id":null}` だけで送り直す**。例外0件。
  `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  `node --check`（.mjs へコピー）online.js・trial-entry.js 通過。お知らせ（changelog.js）は変えていない（アプリ内で
  プレイヤーに見える変化が無いため）。**SQLの手動実行が必要**。Edge Function の変更は無い。
- **申し送り**: (b) は画像データの到着待ち。(c) は本人操作。(b)(c) が片づいたら PR戦略セッションへ「宣伝してよい状態になった」と伝える。

### 2026-09-18（続き518）：#353 色落ちキャットの下のジャンプ台が発動しない／#352 相手が離席した「相手が選ぶ」場面の選択が相手の画面に残る／#343 iPhoneで対局BGMが始まっていなかった（実機の記録で判明）

ユーザー「debug確認を！」。続き515 の手順（Chrome経由・読み取りのみ）で新しい報告 #352・#353（YGM=C、iPhone、
オンラインのランク戦で和太鼓neo=A と対戦、版 20260916）を読んだ。**2件とも同じ対局**の報告。

- **#353「色落ちキャットの下のジャンプ台が発動しなかった」（原因確定・修正）**: 到達したカードを手札に加える既定動作
  （moveAndSync）は、動いた後に下から現れた表向きのカードへの到達（露出到達＝コンボ）を起こす。ところが**自分で自分を捨てる**
  カード（DISCARD_SELF＝色落ちキャット・なないろの巨光）は discardAndSync で捨てるので、その呼び出しが無かった。
  runArrivalEffect で「到達したカード自身を盤面から捨てた」ことを控え、効果を全部処理し終えた後（手札に加える場合と同じ順番）で
  `helpers.triggerExposedArrival`（main.js が `maybeTriggerCardArrivalForExposedCard` を渡す）を **await** で呼ぶ
  （収穫と種まき #85 と同じ理由——下のカードの効果が済む前にターンの終わりへ進ませない）。
  - **A/Bで実測（本物の経路・`scratchpad/probe_353.mjs`、`--old` で engine と main.js だけ HEAD）**: 駒のマス(3,3)に
    表向きのジャンプ台を置き、カード開発モードで色落ちキャットを到達させた——**旧: キャットの後に何も起きない／新: 下の
    ジャンプ台が到達し、駒が(3,3)→(1,3)へ2マス移動、その先の合同建設の到達まで続く**。例外0件。
  - プローブのハマりどころ: 確認のボタンの文言は「✅ はい」（先頭に絵文字）。`/^はい/` では当たらず、ジャンプ台の
    行き先の確認で止まって「動かない」ように見えた（アプリではなくプローブの誤り）。
- **#352「相手が寝ちゃって、合同建設の置くのが置き去りに…起きて再開して…ムーブフェイズで移動先を選べなくなった」
  （原因確定・修正）**: この対局は途中でタイマーがオフ（23:01:51）＝疑似CPUの代行が無い。頼んだ側（C）は
  `delegateToPlayerForEffect` で返事を**90秒**待って諦め（`result:false`）、先へ進んでターンを渡した。ところが**頼まれた側
  （A）の画面ではマスの選択が開いたまま**（A のログ 23:08:54 に `pickerActive:true`、ゲート侵攻の演出も `picker:"cell"` で
  保留）。起きて選ぶと遅れて札が置かれ、選択が開いている間は盤面のタップを全部その選択が受け取るので、自分のターンの移動先が
  選べない。
  - **修正**: 頼まれた側にも期限（**85秒**＝頼んだ側より少し短く、こちらの「置かなかった」が先に届く）を持たせた。期限で
    `closeExpiredDelegationUi()` が開いている選択（activeEffectPicker）と「このマスでいいですか？」（新設の
    `cancelOpenCellConfirm()`、結果は null）を閉じる。期限の後に選ばれても置かない（合同建設・スラム上がりの役人の各段で
    `isDelegationExpired()`）。`requestCellChoiceForEffect` も期限切れなら選び直しを出さず、確認が null なら中止扱い。
    **端末が眠っている間はタイマーが止まる**が、戻った瞬間に遅れていたタイマーが走るので、起きた時にすぐ閉じる。
    頼んだ側からの「諦めた」の合図は足していない（眠っている端末には届かないので当てにならない）。
  - **A/Bで実測（`scratchpad/probe_352.mjs`。online.js に試験用の口を足して配り、自分宛ての依頼を直接届けた）**:
    **旧: 88秒後も選択が開いたまま、その後マスを押すと札が置かれる／新: 88秒後に選択が閉じ、押しても置かれない。
    確認を出したまま放置した場合も閉じて置かれない**。記録 `diag-delegate {phase:"receiver-expired"}` が出る。例外0件。
- **#343 の続き（BGM）**: この報告の記録で、**YGMさんの iPhone では対局開始3秒後のゲームBGMが `paused:true / t:0`＝
  始まっていなかった**（オープニング・待機BGMはタップ起点なので鳴っている。相手のPCでは鳴っている）。対局開始は同期の
  結果なので操作の外＝自動再生の制限で弾かれる。既に「タップのたびに取り直す」保険があったが **pointerdown だけ**で、
  仕様上、指で触った時の pointerdown は「音を鳴らしてよい操作」に数えられない（数えられるのは pointerup / touchend / click）。
  pointerup・touchend でも取り直すようにし、弾かれた理由を `diag-bgm-play-failed`（対局ごとに最初の3回）に残す。
  - **実測の範囲（正直に）**: 「操作起点でないと鳴らさない」設定のChromiumで、対局開始時に **NotAllowedError で弾かれる**
    ところまでは再現した（YGMさんの記録と同じ状態）。ただし Chromium では**旧版でも1回タップすれば鳴った**ので、
    iPhone（WebKit）だけの違いは手元では確かめられていない。追加した取り直しは鳴っていれば何もしないので害は無い。
    次の報告の `diag-bgm-play-failed` と、タップ後に鳴ったかで決着する。
- **その他の記録（実機）**: 描き直し `drawsPerSec` はほぼ 2.1〜2.8（#348 の対策は引き続き効いている。時々 10〜20 は
  演出中）。`diag-victory-celebration`（#349）・`diag-cell-confirm-pref`（#351）はまだ0件（勝利の場面・設定の切り替えが無かった）。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  `node --check`（.mjs へコピー）main.js・card-effect-engine.js・cell-confirm.js・sound.js・changelog.js・handled-bug-reports.js 通過、
  お知らせ ja/en とも件数一致（70回・不一致0）。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要
  （委任の往復は既存の broadcast のまま、online.js も無改造）。

### 2026-09-19（続き519）：公開してよい「人数だけ」を返す関数 so7_public_counts()（AsobuzZ コンソールのホーム用・SQL要手動実行）

別のClaudeセッション（AsobuzZ コンソール＝投稿スタジオ）から、ユーザーの希望として依頼が届いた——コンソールのホームの
「公開しているページ」の札の横に「今日 N人」を出したい。訪問の記録（so7_visit_log）は公開鍵では読めず、読めるのは管理者だけの
so7_get_admin_stats なので、コンソール（公開鍵で5分おきに1回呼ぶ）からは取れない。

- **`so7_public_counts()` を追加**（`supabase_setup_so7.sql` 末尾。security definer・引数なし・anon/authenticated に execute）。
  返すのは **数字だけ**——`{ loginsToday, visitsToday, totalUsers }`。loginsToday＝今日の訪問のうち user_id がある人の重複なし人数、
  visitsToday＝今日の訪問数、totalUsers＝so7_user_profiles の件数。名前・メール・ID・時刻は出さない。
- **「今日」は日本時間の0時で切る**（`(now() at time zone 'Asia/Tokyo')::date::timestamp at time zone 'Asia/Tokyo'` 以降）。
  so7_get_admin_stats の visitsToday は `current_date`（UTC）なので**日本の朝9時で日付が変わる**——数字が合わないことがある（管理者画面の方は未変更）。
- 訪問の個々の記録を「誰でも見られる情報にしたくない」という元の方針（so7_visit_log の RLS）は変えていない。公開になるのは集計の数字だけ。
  管理者本人とローカル確認の訪問は、記録する側（recordVisit）で既に除いてある。
- **ユーザーの手動実行が必要**（追加した部分だけを SQL Editor へ）。配信物（src/）の変更は無い＝お知らせは更新していない。
  Edge Function の変更も無い。実際のDBでの実行確認はこちらからはできない（SQLの文法・日付の切り方は目で確認）。
- **申し送り**: 戦績管理システム（BATTLE-log・別フォルダ）の page_visits に user_id 列が無い件（あちらの supabase_setup.sql の
  2026-09-05 の追加が未実行らしい）は、コンソール側からの情報として**ユーザーへ伝えただけ**。流すかどうかは本人判断。

### 2026-09-21（続き520）：#354 チュートリアルで紫を「ドラッグでロック」すると案内が止まる／#355 対局が始まってもロビー（開始待ち）が出っ放し

ユーザー「debugないよね？」→「debug確認」。続き515 の手順（Chrome経由・読み取りのみ）で新しい報告 #354・#355 を読んだ。
**読み取りの RPC が statement timeout で落ちるようになっていた**（報告が355件＝ログが大きい）。ダッシュボードのページ自体は
描画できていたので、まずそこから最新5件の見出しだけ読み、その後 id>=354 だけに絞って取り直した。次からも同じ手が要る。

- **#354「チュートリアル中の紫のカードをロックしようとしたところ、進めなくなりました」（原因確定・修正）**:
  台本の「紫をロック」の場面は **handEffectCard（手札の紫をタップ）だけ**を待っていた。ところがプレイヤーは**ロックエリアへ
  ドラッグしてロックする**こともできる（報告のログもそちら＝`MOVE_TOKEN card-62 → lock bottom index 6`）。そうすると紫は
  手札から消えるので、待っているタップは二度と来ない＝そこで永久に止まる。
  - **直し方**: この場面に `advanceWhen: (state) => selfHasLockedColor(state, FIRST_TARGET_COLOR)` を足した（やり方が違っても
    **結果が同じなら先へ進める**）。あわせて `goToStep` に**入り口での一度きりの判定**を入れた——`onDriverState` は
    「状態が変わった時」にしか呼ばれないので、前の案内を読んでいる間に先にロックしてしまった人は誰にも拾われなかった。
  - **A/Bで実測（本物の経路・`scratchpad/probe_354.mjs`、`--old` で tutorial-battle.js だけ HEAD）**: 案内を進め、紫が手札に
    来たところで**タップせずドラッグでロック**——**旧: 「手札の「紫」のカードをクリックして…」の案内のまま永久に停止（紫は
    ロック済みなのに）／新: そのまま先へ進み、ハンドフェイズ（プレゼント）の場面まで到達**。例外0件。
  - プローブのハマりどころ: 案内の最初に見えるボタンは「← 戻る」。素直に先頭を押すと2画面を往復し続ける（**先へ進む方だけ**を押す）。
- **#355「観戦時、このモーダルが出っ放し。」（原因確定・修正）**: 添付画面は、進行中の盤面の上に「🌐 対戦ロビー／参加人数: 4人／
  草（冷笑系）さんがゲームを開始するのを待っています…」が residual で出ているもの。ロビーを閉じる合図が **「自分に座席が付いた時」
  だけ**だった（`onRosterChange` で `getMySeat()` を見る）。**座席は最大4つ**なので、5人目以降は座席がもらえないまま対局が始まる
  （Edge Function の `shuffled(memberIds).slice(0, seatsToUse.length)`）。その人と観戦者には合図が永久に来ない。
  報告の記録もこれと一致していた——`selfSeat:"A"` と出ているのは `getSelfSeat()` の**座席が無い時のフォールバック**で、実際には無座席。
  - **直し方**: 閉じる条件を「座席が付いた」だけでなく **「その対局がもう始まっている」（`getState().turnPlayer` が入っている）**
    にも広げ、ロビーを開いている間は `subscribe` でも見張る（座席が付かない人には roster の合図が来ないため）。
    観戦中は最初から開かない（`isSpectatingGame()`）。
  - **A/Bで実測（`scratchpad/probe_355b.mjs`）**: ロビーを開いた状態で「対局が始まった」状態にする——**旧: 出たまま
    （文面も報告の画面と同じ）／新: 閉じる**。例外0件。
  - **測れなかったこと（正直に）**: 本物のオンラインで**5人が同じ部屋に入って1人が座席をもらえない**ところまでは再現できなかった。
    1台で5画面を開くと読み込みが時間切れになる（続き501 の「4人ぶんはこのPCでは測れない」と同じ壁）。座席の割り当ては無作為なので、
    狙った人を無座席にするには何度も引き直す必要もある。**閉じる仕組みそのもの**は上のA/Bで確かめた。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、`node test/smoke.mjs 2` PASS、
  `node --check`（.mjs へコピー）tutorial-battle.js・online-ui.js・changelog.js・handled-bug-reports.js 通過、
  お知らせ ja/en とも件数一致（71回・不一致0）。サーバー側（Supabase）の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。
- **申し送り**: 座席をもらえなかった人は、対局が始まると**何もできないまま盤面を見ているだけ**になる（ロビーは閉じるが、
  「あなたは観戦になりました」等の案内は出ない）。文言を出すか、部屋の人数を4人までに制限するかは、ユーザーに確認してから。

### 2026-09-21（続き521）：満席の部屋は「観戦しますか？」に／観戦者を対局から完全に切り離した（送信口1か所で封鎖）／観戦中の視点切り替え／#356 選択待ちの最中に別の手札効果が始まる

ユーザー要望3件＋不具合報告1件。

- **【満席（4人）の部屋】**「そもそも5人目が入れないようにする。部屋を押したタイミングで5人になる時は、
  あふれたことを告げて『観戦にしますか？』と聞く」。これまでは**席が無いまま入れてしまい**、
  その人の画面に開始待ちのロビーが出っ放しになっていた（#355 の元をたどるとこれ）。
  - `joinRoom` の頭で参加人数を見て（`so7_games_list.member_count`）、`MAX_SEATS`(4) に達していれば
    `code:"room_full"` で断る。UI（部屋一覧・合言葉の両方）はそれを受けて確認を出し、
    「はい」なら **そのまま観戦に切り替える**。
  - **座席が無いまま対局が始まってしまった人**（既に入ってしまっている端末）も救う——
    `fetchAndHydrate` で「席が無いのに対局が始まっている」状態を見つけたら観戦者に切り替える
    （`diag-became-spectator`）。
  - **A/Bで実測**（部屋を通信だけで4人埋め、5人目を本物のアプリで）: **旧＝入ってしまう（席なし）／
    新＝`room_full` で断る**。さらに**本物の部屋一覧の行をクリック**して
    「この部屋は満席です（最大4人）。観戦しますか？」が出て、はいで観戦に入ることまで確認。

- **【観戦者を対局に関与させない】**「全員を対象にした効果で観戦者も対象になっていた」。
  - **原因**: `getSelfSeat()` は**観戦中「今見ている席」を返す**（盤面をその人の向きで描くため）。
    一方、委任（「あなたが選んでください」＝合同建設・パーティー等）の受け口は
    `getSelfSeat() !== player` で判定していたので、**観戦者がその席の本人として選択を求められ**、
    しかもその答えを送り返していた（本物のプレイヤーより先に届き得た）。
  - **直し方（要所）**: アクション送信は `callAction` の頭で既に止めてあったが、**ブロードキャストは
    素通りだった**。個々の送信箇所（30か所以上）を直すのは漏れるので、**チャンネルの送信口1か所**で
    塞いだ（観戦中は `bug_log_*` 以外を送らず `diag-spectator-blocked` を残す）。
    加えて、応答UIそのものを出さないよう委任の受け口と承認3種（接触・最終ロック・持ち時間）にも
    `isSpectatingGame()` の守りを入れた。
  - **A/Bで実測**（2人の対局を本当に開始し、3人目が観戦。見ている席あてに委任を送る）:
    **旧＝観戦者に選択モーダルが出る／新＝出ない**。送信口も別に測った——
    観戦者が送ろうとしたブロードキャストは**ホストに0回届き**、塞いだ記録が1件残る。

- **【観戦中の視点切り替え】** 観戦バーに参加者の名前ボタンを並べ、押すとその人から見た向きに変わる
  （`getSpectateViewSeat`/`setSpectateViewSeat`）。**実測**: ボタン「プレイヤーA」「プレイヤーC」が出て、
  押すと視点が A→C に変わる（旧版はボタン自体が無い）。

- **【#356】「①セレナーデを使用 ②パーティーをコストとして捨てる ③パーティーをロックしようとした
  ④できなかった」**: ログを追うと、セレナーデの**追色コストを選んでいる最中**（pickerActive:true）に
  `hand-effect: pink-party` が始まり、パーティーが**自身の手札効果で盤面に置かれて**いた
  ＝コストに払うつもりの札が手札から消え、その後ロックできなくなっていた。
  - 手札のタップ／場へのドラッグの両経路に「選択待ち中は新しい手札効果を宣言しない」守りを入れ、
    タップが候補なら**その選択の答えとして解決する**ようにした。
  - **ただし報告の経路は手元で再現できなかった**（タップもドラッグも capture 側のハンドラに
    吸われて届かない）。そこで**入口を問わず `runAutoHandEffect` の頭で止める**のを本命にした。
    「いつでも使える」割り込みは `isAnyEffectProcessingBusy` に `activeEffectPicker` が
    含まれており**選択待ち中はそもそも出ない**ので、ここで止めて困る正しい経路は無い。
    止めた時は `diag-hand-effect-blocked-while-picking` を残す＝次に起きたら入口が分かる。
  - **A/Bで実測**（本物のセレナーデのコスト選択を開いた状態で、同じ関数を呼ぶ別の入口＝
    カード開発モードからパーティーの手札効果を撃つ）: **旧＝`hand-effect: pink-party` が
    始まってしまう（報告のログと同じ形）／新＝始まらず、記録が1件残り、選択待ちは保たれる**。
  - **測定の記録**: 最初のA/Bは**旧・新とも「始まっていない」**で差が出なかった。
    続き463の教え通り「差が無い」ではなく**測れていない**と考え、確実に同じ関数へ届く入口に
    替えたら一度で差が出た。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3301）、`node --check`（.mjs へコピーして）
  変更した全ファイル通過、お知らせ ja/en とも件数一致（71回・不一致0）、例外0件。
  サーバー側（Supabase）の変更は無い＝**SQLの実行・Edge Function の再デプロイは不要**
  （満席の判定は既存の `so7_games_list` を読むだけ。サーバー側も元から非座席者のアクションは弾く）。

- **申し送り**: 観戦者のブロードキャスト封鎖は**新版だけ実測**した（旧版の同じ測定は、
  ゲスト作成が立て込んで失敗したため取り直していない）。塞ぐ仕組み自体は新版で数字が出ている。

### 2026-09-23（続き522）：エイドス戦で「接触をカウンターロックで防がれた瞬間に画面が固まる」——CPUが持ち主の選択待ちを、誰も解決しなかった

ユーザー報告「エイドス戦でエイドスが私の接触をカウンターロックで防いだタイミングで止まった。
不具合報告アイコンをはじめ画面のすべてがクリック不能。ホバー時の説明は出た」。

- **原因（コードで確定 → 再現で裏取り）**: カウンターロックの後半（「手札を１枚ロックしてもよい」→
  「どの札をロックする？」）は**防御側が選ぶ**ので `activeEffectPicker.owner` が**CPUの席**になる。
  CPUの選択は turn-timer の持ち時間切れ（`performPriorityTimeoutAutoAction`）が解決する作りだが、
  この場面で優先権を持っているのは**人間**で、CPU戦の自分の席は持ち時間の対象外
  （`isSelfTimeLimitExempt`）＝**時計が止まっている**。つまり解決する人が誰もいない。
  ・**「全部押せないのにホバーは効く」の説明**: CPU戦では「CPUが選んでいる間、人間がその選択を
  　押せないように」**click を capture で握りつぶす**ガードがある（owner が疑似CPU席の時）。
  　pointermove は握らないのでホバーだけ生き残る＝報告の症状と完全に一致する。
- **直し方（入口ではなく出口を塞ぐ）**: CPUが持ち主の選択待ちが **1.5秒**残っていたら、時計に
  関係なく `performPriorityTimeoutAutoAction()` で解決する見張りを main.js に1つ置いた
  （500ms間隔。`diag-cpu-picker-rescued` を最初の5回だけ記録）。カウンターロックだけを直すと、
  同じ形（CPUが選ぶ場面が人間の優先権中に開く）が他のカードで再発するため。
  CPU自身の手番では今までどおり持ち時間側が先に解決するので、挙動は変わらない。
- **A/Bで実測**（本物のCPU戦を開始し、CPU(C)の手札をカウンターロック＋桃/緑にして最強CPUに設定、
  自分(A)の手番で `requestContact` → CPUがカウンターロックを使う。500msごとに「画面のクリックが
  届くか」と「選択待ちが残っているか」を測定）:
  **旧＝12秒後も クリック届く=0／選択待ち=1（覆っているモーダルを押しても閉じられない）／
  新＝1秒後には 選択待ち=0・クリック届く=1 に復帰**（`diag-cpu-picker-rescued` が1〜2件）。例外0件。
- **検証のハマりどころ（記録）**: 最初 `quickStart(2)` で組んだ盤面では**CPUが何も判断しなかった**。
  CPU戦の自動処理は `startCpuBattle()` が立てる疑似CPUモード（`isPseudoCpuModeActive`）が前提で、
  quickStart だけでは有効にならない。さらに**既定の難易度「新人」はカウンターロックを使わない**
  （`cpuDecider` はブレイン非駆動なら false）ので、`setCpuDifficulty("master")` が要る。
  測定側も、最初は pointerdown で測っていて「クリックは届く」と誤判定していた——**握られているのは
  click** なので、症状を測るなら click を投げる必要がある。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、
  `node test/smoke.mjs 2` PASS、`node --check`（.mjs へコピー）main.js・changelog.js 通過、
  お知らせ ja/en とも件数一致（72回・不一致0）。
  サーバー側（Supabase）の変更は無い＝**SQLの実行・Edge Function の再デプロイは不要**。

- **不具合報告の「対処済み」**: ユーザー指示「あなたが確認した不具合報告は対応済みにしていい」に従い、
  **コードで直し済みの #346・#347・#348・#350・#351・#352・#353・#354・#355・#356** を
  `so7_set_bug_report_resolved` で対処済みにした（読み取りと同じくユーザーのChrome経由）。
  **#349（CPUの勝利演出が短い）は直していない**（再現せず記録だけ追加した件）ので未対処のまま残した。

- **申し送り（未着手の報告）**: #357 複数枚捨てる時の順番指定の総点検（ヴァディアン等）／
  #358 長タップの拡大サイズと左右の出し分け／#359 最後のロックをもっと派手に／
  #360 エイドス戦の接触アニメが不完全／#361 何かのたびに画角がズームする／
  #362 捨てる順番の番号がカードの重なりで見えない／#363 スリカエでCPUが未ロック色の札を渡してきた／
  #364「このマスでいいですか？」が出っ放しで進行した／#365 烙印ドローのモーダルが重複して出る。

### 2026-09-23（続き523）：#364 「このマスでいいですか？」が出っ放しでゲームが進む／#362 複数枚捨てる時の順番の番号が重なりで読めない

ユーザー指示「おすすめ順でいいです」に従い、不具合の側から着手。

- **【#364】確認が置き去りになる**: `confirmCellChoice` は「今あなたが選んでいるマス」への問いなのに、
  答えないまま**ターンが進んでも閉じる仕組みが無かった**。さらに `cancelOpenConfirm` は**1つ分しか
  覚えられない**ので、2つ目を開くと1つ目は誰にも閉じられない置き去りになる。
  - 直し方: ①新しく開く時に前のを閉じる ②開いた時のターン（`turnNumber`＋`turnPlayer`）を控え、
    `subscribe` で変わったら `null`（＝選ぶのをやめた）で閉じる（`diag-cell-confirm-stale` を記録）。
  - **A/Bで実測**: **旧＝ターンを進めても出たまま・返事も来ない／2回開くと画面に3枚残る。
    新＝ターンが進んだ瞬間に閉じて `null` が返る／同時に出るのは1枚だけ**。
- **【#362】順番の番号が読めない**: まず**どこなら見えるか**を測った——手札6枚の扇で、
  カード中央が見えているのは**6枚中1枚**（上端中央でも4/6）。位置を変えるだけでは足りないので、
  **選んだカード自体を前面へ出す**（`.is-discard-picked` に `z-index: 30`）＋番号を上端へ＋少し大きく。
  - **A/Bで実測**（本物のクラスでバッジを付け、バッジ中心の最前面が自分のカードかを数えた）:
    **旧 1/3 → 新 3/3**、バッジの実寸 25px → 30px。
  - **測定のハマりどころ**: 最初 `elementFromPoint` がバッジ自身を返すか見ていたが、
    バッジは `pointer-events: none` なので**絶対に返らない**＝旧も新も0で差が出なかった
    （続き463の「同じ結果は測れていないを疑う」）。**親のカードが最前面か**で測り直して差が出た。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、
  `node test/smoke.mjs 2` PASS、CSSブレース平衡（3301）、`node --check` 通過、
  お知らせ ja/en とも件数一致。サーバー側の変更は無い＝SQLの実行・Edge Function の再デプロイは不要。

- **申し送り**: 次は #365（烙印ドローの確認モーダルが二重に出る）→ #363（スリカエでCPUが未ロック色を渡す）→
  #361（画角が勝手にズームする）→ #360（接触アニメが不完全）の順を予定。#357〜#359 は要望。

### 2026-09-23（続き524・ユーザー指摘で本体に辿り着いた）：#364 の真の問題は「答えていないのに進むこと」——確認中が「処理中」に数えられていなかった

ユーザー指摘「タイマー制でないのに、そもそも選択してないのに進むことが問題な気がしますがどうでしょうか？」。
**そのとおりで、続き523 で私が直したのは結果（置き去りの掃除）だけだった。**

- **見つけたこと**: 自動ターン終了の判定（`computeShouldEmphasize`）と、その元になる
  `isAnyEffectProcessingBusy()` は、**「このマスでいいですか？」の返事待ちを1つも見ていなかった**。
  この確認は `activeEffectPicker` を使わない別立てのモーダルなので、`pickerActive` にも入らない。
- **危険な窓が具体的に分かった**: 効果のマス選択（`requestCellChoiceForEffect`）は
  **選択が確定した瞬間に `activeEffectPicker` を null にしてから**、その後で確認を await する。
  つまり**確認が出ている間は pickerActive=false**＝自動進行から見て「誰も何もしていない」状態だった。
  通常移動の確認は `isMovePhaseActive()` がまだ真なので偶然守られていた（＝カード効果の方だけ穴）。
- **直し方**: cell-confirm.js に `isCellConfirmOpen()` を足し、`isAnyEffectProcessingBusy()` と
  `computeShouldEmphasize`（**同じ判定を自前で並べ直しているので両方に要る**——続き83と同じ構造）
  に入れた。診断 `diag-should-emphasize` にも `cellConfirmOpen` を足したので、次に同じ報告が来たら
  「確認中だったのに進んだ」かどうかがログで分かる。
- **A/Bで実測**（本物のCPU戦・自動処理ON、自分の手番のムーブフェイズで確認を開いて放置）:
  **旧＝「処理中とみなす」false／新＝true**。20秒放置しても手番は動かない（両方）。
- **正直な限界**: **「実際にターンが進んでしまう」ところまでは再現できていない**。通常移動の確認は
  上記のとおり別の条件で守られており、効果のマス選択の窓は到達効果の処理中フラグ等と重なるため、
  手元では自動終了が走る条件を作れなかった。**入れた守りは「返事待ちは処理中」という原則として正しく、
  実測で効いている**が、報告の現象がこの窓だけで説明できるとは言い切れない。記録を足したのはそのため。

- **検証**: `npm test` 58/58 PASS、`node tools/check-undeclared.mjs` 0件（165ファイル）、
  `node test/smoke.mjs 2` PASS、`node --check` 通過、続き523 の置き去り対策も引き続き有効
  （ターンが進めば閉じる・同時に1枚だけ）。サーバー側の変更は無い＝SQLの実行・再デプロイは不要。
